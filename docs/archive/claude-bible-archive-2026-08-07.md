# CLAUDE.md ARCHIVE — verbatim snapshot taken 2026-08-07, before the slim-down restructure

> **Last updated:** 2026-08-07 21:10 UTC
>
> This is the complete, unedited CLAUDE.md as it stood on 2026-08-07, preserved before the file was
> restructured into a lean operating doc. Every fixed-issue row (#120–#163 and earlier), every
> session "Shipped" narrative, and the full 2026-08-05/06 audit + deploy record live here in full detail.
> The prior archive (per-tier build checklists, issues #1–#57) is `claude-bible-archive-2026-07-13.md`.
> Note: the snapshot's first ~19 lines are orphaned issue-table rows that had been accidentally pasted
> above the document title — preserved as-is for fidelity.

---
| 138 | **Six empty states told users to wait for automation that never happens** — one per feature in the dispatcher gap. Relationship Insights: *"They build up automatically as you send and receive email"*; Knowledge Graph: *"AlecRae builds it automatically"*; plus Attachments, Productivity, Sentiment and Delegation. None of it happens. `relationship_insights` is a **read-only table** — nothing anywhere inserts into it (verified across apps/, services/ and packages/). Each panel is empty for every user, permanently, while telling them to keep waiting: the same false-automation claim as Encryption's "encrypted automatically", repeated six times. Also found by diffing every declared `/ai-engine` subpath export against its importers: **nine capabilities with no consumer**, incl. the relationships engine that would populate this. | MEDIUM | 2026-07-29 | **FIXED 2026-07-29 (copy)** — all six now state plainly that the feature is unfinished. Deliberately not half-built: the dispatcher needs the opt-in schema + cost model of issue #132 (Craig's call) and would otherwise repeat issue #130's unbounded per-message AI spend. The other eight unused ai-engine subpaths are superseded duplicates — `voice/cloner` lost to `voice/style-cloner`, which is what `voice-clone.ts` actually uses, so the audit's "Voice is real" verdict stands. **NOT changed, needs Craig:** the landing page claims commitments are "tracked automatically" — same problem, but marketing copy is Boss Rule #9. |
| 139 | **The IP warm-up ramp was never enforced — the single highest-consequence deliverability gap found in this whole audit.** `services/mta/src/delivery/warmup.ts` implements the complete week-by-week per-ISP ramp from the deliverability runbook (Gmail 50/day week 1, doubling weekly, UTC-midnight rollover), and its ONLY importer was its own test file. The delivery worker never consulted it. A brand-new sending IP would have gone straight to unlimited volume on day one — the most reliable way in existence to get an IP blocklisted and a domain's mail rejected outright. Nothing else covered it: `DeliveryOptimizer`'s per-ISP throttles are a steady-state *rate* limit (connections/hour), not a *volume* ramp. Note CLAUDE.md issue #82 listed "warmup orchestrator w/ bounce gates" among the deliverability foundations that are "REAL" — the orchestrator in `services/reputation` is real and does drive the schedule/pause machinery, but nothing connected either module to an actual send. | HIGH | 2026-07-29 | **FIXED 2026-07-29** — new `services/mta/src/delivery/warmup-gate.ts` supplies the missing persistence + enforcement half: per-(identity, UTC day, ISP) counters in the Redis the queue already uses, reserved with atomic INCR-then-compare so concurrent workers can refuse slightly early but never jointly exceed a cap. Wired into BOTH worker send paths (relay and direct MX) beside the existing suppression check. Over-cap recipients are **deferred, not dropped** — quota returns at UTC midnight so the message stays deliverable. Attempts consume quota rather than successes, since a rejected message still spends a reputation signal at that ISP. Without Redis it degrades to in-process counters rather than failing open (bounded-but-weaker beats unbounded; same pattern as the AI-spend circuit breaker). On unless `MTA_WARMUP_ENABLED=false`, because the dangerous default is the permissive one. 13 tests. |
| 140 | **`API_URL` was unvalidated and defaulted to `http://localhost:3001`, and it is what goes into outbound mail.** `routes/messages.ts` builds the advertised `List-Unsubscribe` header and every click-tracking link from it. `lib/env.ts`'s fail-fast production validation did not cover it, and it is absent from `docs/infra/.env.production.template` (which has only the *client-side* `NEXT_PUBLIC_API_URL`) — strong evidence the box never had it set. A real send would therefore have advertised an unreachable one-click unsubscribe — a hard Gmail/Yahoo bulk-sender requirement — and rewritten every link in the message to point at localhost, broken for every recipient. No live harm to date only because nothing sends today. | HIGH | 2026-07-29 | **FIXED 2026-07-29** — `API_URL` is now required in production and must be an https, non-localhost URL; added to the env template with the reasoning inline. 4 new tests. **DEPLOY PREREQUISITE:** `/opt/alecrae/.env` must gain `API_URL=https://api.alecrae.com` before the next API restart or the API will refuse to boot. That refusal is deliberate — a loud failure at boot beats silently shipping dead links to every recipient. |
| 141 | **The virus scanner never existed, and the UI reported its invented verdicts as fact.** `performVirusScan()` in `attachment-intelligence.ts` rolled `Math.random()`: ~90% "No threats detected", 5% a scan error, and 5% a specific, real-sounding malware name — `"Trojan.GenericKD.46542893 detected by heuristic scan"` — attributed to a customer's file that nothing had opened. Both verdicts were **persisted** to `attachment_analysis` and rendered on the shipped `/attachments` page as a Clean/Infected pill with the detail text beneath it. Telling a user a file is clean when nothing scanned it is the more dangerous half. Same class as issue #84, which fixed the security-centre equivalent and left this one; issue #75(e) noted it in passing as "stubs per #29" without registering that it ships a safety verdict. Separately the `isSafe` → green **"Safe"** pill was derived from a file-extension and >25MB size check, so an ordinary `.docx` carrying a malicious macro displayed "Safe". | HIGH | 2026-07-29 | **FIXED 2026-07-29** — `/scan` and `/batch-scan` return 501 with an explicit "not available, nothing has been scanned, no result recorded", placed **after** the ownership check so a 404 still means what it meant. The row keeps its truthful `pending`; the `attachment_virus_scan_status` enum has no "unavailable" value and adding one is a migration (Boss Rule #7). UI states the gap plainly and the Rescan button is removed rather than left to fail; the threat pill now names the check it actually performed and the duplicate `isSafe` pill is gone. Wiring a real scanner needs a new dependency — Craig's call per Boss Rule #12. |
| 142 | **"Test this rule" returned a random number.** `POST /v1/ai-categorization/smart-rules/:id/test` returned `Math.floor(Math.random() * 50) + 1` matches against a hardcoded `sampleSize: 200`. The entire purpose of that endpoint is to tell a user whether the rule they just wrote is any good, and the answer had no relationship to their mail. Unlike #141 this was **fixable for real** — the conditions are stored in `smart_label_rules.conditions` and the emails are in the database. | MEDIUM | 2026-07-29 | **FIXED 2026-07-29** — real evaluation over the account's 200 most recent emails, returning a real `sampleSize` plus example matches so the count can be checked by eye. Patterns are case-insensitive substrings, matching what the rule builder's UI describes; treating them as regexes would let one stray `(` throw on every message in the sample. Attachment presence is one `selectDistinct` for the whole sample, not a query per email. `minImportance` is reported in a new `unevaluatedConditions` array because emails have no importance column — the difference between a number that is incomplete and one that is wrong. |
| 143 | **The 7-file end-to-end API test suite has never run anywhere.** `apps/api/package.json`'s `test` script is `vitest run --exclude 'tests/e2e/**'`, and **no CI workflow invokes `test:e2e`** (grepped every file in `.github/workflows`). The suite covers billing, domains, messages, suppressions, templates, webhooks and health by `fetch()`ing a live API, so it needs a booted server plus Postgres and Redis — infrastructure no job provides. Same class as issue #120: tests that exist, are counted as coverage, and execute nowhere. CLAUDE.md issue #30 records "~7 e2e files for 681 endpoints" as *thin* coverage; it does not record that the number of them actually running is zero. | MEDIUM | 2026-07-29 | OPEN — not a quick fix: needs a CI job that boots Postgres + Redis + the API (services containers) before running `test:e2e`, plus seed/auth fixtures. Deliberately not half-built. Until then the honest figure for executing e2e coverage is **0 endpoints**, not 7 files. |
| 144 | **DKIM signing failed OPEN — a domain with no key, or any signing error, sent the message unsigned anyway.** `services/mta/src/worker.ts` logged a warning and carried on. Three reasons this is not survivable: Gmail and Yahoo require DKIM from bulk senders outright; alecrae.com publishes **DMARC p=quarantine**, so an unsigned message has only SPF to fall back on — and SPF breaks on exactly the paths DKIM survives (mailing lists, `.forward`, any forwarder); and unsigned mail costs the reputation of **every domain sharing the IP**, not just the message that went out. Unsigned was also the *likely* path rather than an edge case: `dkimPrivateKey` is written only by `services/dns/src/auto-config.ts`, which per the 2026-07-22 journey audit does not run for real customer domains today. The dev seed compounds it — `packages/db/src/seed.ts` sets `dkimVerified: true` beside a truncated placeholder key that cannot sign. | HIGH | 2026-07-29 | **FIXED 2026-07-29** — unsigned messages are **held, not bounced**: `deliveryResults` deferred with a clear reason and the email returned to `queued`, so they go out the moment a key exists (a bounce would destroy a message that is still perfectly deliverable). `MTA_REQUIRE_DKIM=false` is the escape hatch, and **only that exact value disables it** — `"0"`, `"no"` and `"off"` all leave enforcement ON so a typo fails safe. Read per call rather than captured at module load, so an operator can change it mid-incident without a redeploy. 4 tests pin the default posture, which is the part a future edit could quietly flip back. Seed annotated so a deferred send against seed data isn't misread as a bug. |
| 145 | **The click-tracking endpoint was an open redirect on our own domain.** `GET /t/:emailId/click?url=…` checked only that the protocol was `http(s)` — **directly beneath a comment reading "Validate URL to prevent open redirect"**, which is not what it did. `https://api.alecrae.com/t/anything/click?url=https://phishing.example` returned a 302 to anywhere, with no check that the email id was real or that the URL had ever appeared in that message. Open redirects on reputable domains are actively harvested to launder phishing links past URL-reputation filters, and the cost lands on the host: Safe Browsing/SmartScreen flag the domain and blocklists follow. For a product whose deliverability *is* its domain reputation, this was a self-inflicted blocklisting waiting to be found by someone else. Found by auditing the tracking endpoints for spam-adjacent abuse rather than by any test. | HIGH | 2026-07-29 | **FIXED 2026-07-29** — new `apps/api/src/lib/tracking-link.ts`: every tracked link carries an HMAC over **(emailId, url)** and the redirect refuses anything that doesn't verify, so a URL we never sent cannot be redirected to. Binding the email id as well as the URL stops one leaked link becoming a universal redirector for other messages. Keyed on `JWT_SECRET` with a purpose string mixed in, so a tracking signature can never be replayed as an auth token; verified in constant time, before parsing or recording anything, returning false rather than throwing on malformed input (the endpoint is unauthenticated and reachable by anyone). **No compatibility shim for unsigned links** — nothing has ever sent, so none exist in the wild, and accepting one "for compatibility" would leave the hole open. 10 tests including the realistic attack: lift a signature from a real link, swap the destination. |
| 146 | **Seven services are built but run nowhere, and two documented hostnames for them do not exist.** Mapped every `services/*` package against its importers: `analytics`, `collab`, `imap`, `inbound`, `jmap`, `sentinel` and `support` have **zero importing files**. All seven are standalone entrypoints rather than libraries, so zero importers is expected — but per the deployment section only `alecrae-api` and `alecrae-web` actually run (plus `alecrae-mta`, stopped). `docs/infra/.env.production.template` nonetheless ships `COLLAB_WS_URL=wss://collab.alecrae.com` and `COLLAB_HTTP_URL=https://collab.alecrae.com`, a host absent from the DNS table and never resolved. No live breakage today: the only consumers of `collab-client.ts` are `CollaborativeDraftView`/`…Dynamic`, both still orphaned per issue #69, so nothing attempts the connection. Relevant because the Build Status headline counts all of this as shipped surface. | LOW (no live impact) | 2026-07-29 | OPEN — documentation/scope honesty rather than a code defect. `imap` is already a documented non-goal (#58) and `inbound` is mail-plan Phase 2; the other five need an explicit keep-or-delete decision. Not deleted unilaterally: several are real, complete implementations and deletion is a scope call, not an audit fix. |
| 147 | **The public tracking endpoints had no rate limit of any kind, and each hit made us call the customer's own webhook endpoint.** `/t/*` is mounted in `server.ts` with no middleware — correct as far as auth goes, since mail clients have no session — but every recorded event costs a DB insert **and** an `enqueueWebhookDelivery`. So anyone holding a single tracking URL (the recipient, or anyone they forwarded the message to) could drive **unlimited outbound HTTP calls at that customer's webhook receiver** and fill the `events` table, from an endpoint requiring no credentials. We would have been the amplifier. `recordEvent` does cheaply reject unknown ids before writing, so the flood needs a valid id — which every recipient has. **Also a false doc claim:** the Security Requirements list asserted "Rate limiting on every public endpoint (already done — 6 tiers)". | MEDIUM | 2026-07-29 | **FIXED 2026-07-29** — `lib/tracking-throttle.ts`, throttling **per (email id, event type)** rather than per IP. That axis choice is the load-bearing decision: Gmail/Yahoo/Apple fetch pixels through image proxies where a few IPs act for enormous numbers of unrelated recipients, so any per-IP limit tight enough to matter would silently drop open tracking for every recipient behind them — an outage of exactly the engagement data the send-time predictor and warm-up depend on. A proxy fetching for 10,000 recipients touches 10,000 ids; a flood against one message trips immediately. **Throttled, not rejected:** over-limit requests still get their pixel and still follow their redirect, and a repeat unsubscribe still answers 200 (the address genuinely is suppressed by then — telling a mail client's one-click unsubscribe that it failed would be both wrong and a compliance risk). Redis-backed with bounded in-process fallback, failing open on storage error. 7 tests. Doc claim corrected rather than deleted — the exception is deliberate and now states why. |
| 148 | **Every customer domain publishes a DMARC record whose reports can never be delivered.** `services/dns/src/auto-config.ts` generates `_dmarc.customer.com` with `rua=mailto:dmarc@alecrae.com` — pointing reports at a **different organizational domain** from the one being reported on. RFC 7489 §7.1 requires the receiving domain to opt in with a `_report._dmarc` record before a reporter will send across that boundary, and Google, Microsoft and Yahoo all enforce it. No such record exists in the alecrae.com zone (grepped the whole repo and the zone doc: zero occurrences of `_report`). A conforming reporter therefore **sends nothing at all** — silently, with no error on either side. Distinct from the `rua` mailbox problem already noted in issue #82 (dmarc-reports@alecrae.com can't receive because inbound isn't running): even with inbound fully deployed, the reports would never be sent in the first place. Ban-relevant because aggregate reports are the primary signal telling us who is spoofing a customer's domain and whether our mail passes SPF/DKIM alignment in the wild — the feedback loop that surfaces a deliverability problem *before* receivers start rejecting. | HIGH | 2026-07-29 | **CODE + DOCS DONE 2026-07-29; the DNS record itself needs Craig (Boss Rule #4).** `getDnsConfig()` now derives and exposes the required record via `deriveDmarcReportAuthorization()`, so the `rua` and its authorization can never drift apart; `checkDmarcReportAuthorization()` in the liveness checker probes a concrete name under the wildcard (resolving the literal `*` label would not test what a reporter actually looks up) and reports the exact record to publish. **Wildcard `*._report._dmarc.alecrae.com TXT "v=DMARC1"` deliberately, not per-customer:** the per-domain form needs a DNS write in our zone on every onboarding — a step that gets forgotten and whose absence is invisible. Recorded in `docs/infra/dns-zone-alecrae.md` §5a with the verification `dig`. 7 tests. |
| 149 | **The MTA would never have seen a single job — mail bring-up was blocked by a config nobody had reconciled.** `docs/infra/mta-box-setup.md` has stated the requirement since it was written: the API on Jarvis enqueues sends, the MTA on the mail box (`149.28.119.158`) consumes them, so **both must point `REDIS_URL` at the same Redis** — and warned in the same breath that a Redis local to one box "means jobs are never seen". Production is precisely that: Redis on Jarvis, bound to `127.0.0.1` only. Start the MTA today and **every send silently disappears** — the API enqueues into its own Redis and returns success, the MTA watches a queue that never fills, and there is no bounce, no error and no log line anywhere. The doc hedged ("either a shared/hosted Redis (Upstash) or one box's Redis over the tailnet"), and the hedge is what let it sit unresolved. Found by following Craig's question about whether Redis was Vercel. | HIGH | 2026-07-29 | **DECIDED + DOCUMENTED 2026-07-29 (Craig: bind Redis to the tailnet).** Chosen over moving the MTA to Jarvis (surrenders the dedicated sending IP that already has port 25 unblocked, a correct PTR and SPF authorising it) and over hosted Redis (new vendor, cost, per-op latency for something a bind address solves). New runbook `docs/infra/redis-tailnet-setup.md`: bind address, mandatory `requirepass` (**a tailnet is a network boundary, not an authentication one** — every device on it, including any future laptop, reaches anything listening there), firewall on the tailscale interface only, and a **Step 6 that proves both boxes see the *same* queue** rather than merely that each can reach *a* Redis. Both hedged paragraphs replaced with the decision. `REDIS_URL` added to the env template with the must-match warning. **On-box execution is Craig's** (Boss Rule #4). |
| 150 | **`bun run preflight` — the go-live environment check — has never been runnable, and is recorded as "✅ DONE".** `scripts/preflight.ts` imports `zod` and `@alecrae/db`, and **neither resolves from the repo root** (root deps are only `meilisearch` and `next`; zod is not hoisted). It died on module resolution before reading a single variable. `GO_LIVE_GAPS.md` line 55 lists it as "G4 … ✅ DONE — scripts/preflight.ts". Two further defects inside it: **(a)** its Redis section `required()` both `UPSTASH_REDIS_URL` and `UPSTASH_REDIS_TOKEN` and pinged an Upstash REST endpoint — but **nothing in the codebase reads `UPSTASH_REDIS_TOKEN`**, and our Redis is self-hosted with no REST endpoint, so preflight could only ever have passed against an architecture we don't run; **(b)** an optional check that *failed* rendered as a bare ❌ identical to a blocking one while the summary counted it as passing, so the icons and the exit code contradicted each other with no way to tell which line caused it. Same class as issue #143 (the e2e suite that executes nowhere) and #120 (tests that couldn't run): a control that exists, is marked complete, and does nothing. | MEDIUM | 2026-07-29 | **FIXED 2026-07-29** — moved to `apps/api/scripts/preflight.ts`, since bun resolves `node_modules` by walking up from the *file* and `apps/api` already has both deps; adding them to the root for one script would have been the worse fix. Upstash vars are optional now and the REST ping runs only when present, with the self-hosted path stating plainly that connectivity is unverified and pointing at the runbook step that does verify it. Optional failures render distinctly. Absent-but-optional vars no longer report "present and well-formed", which stated the opposite of the truth. New `checkSharedQueue()` names the #149 failure mode when `REDIS_URL` is localhost — **reported, not fatal**, because a single-box deployment is legitimate and, unlike a bad `API_URL`, this doesn't corrupt mail, it stops it. |
| 151 | **Three of the four send-queue producers run none of the API-side pre-send gates.** Asked structurally — *who enqueues onto the outbound queue?* — rather than by re-reading known files. Four producers: `routes/messages.ts`, `lib/agent-send.ts`, `routes/agent.ts`, `routes/unsubscribe.ts`. **Only messages.ts runs the stack.** The other three skip the outbound spam gate (#125), send-volume anomaly (#117), the compliance engine (CAN-SPAM/GDPR/CASL) and quota (#102). This is the same shape as issue #126 (the connected-account fast path) but on the path that goes through **our own MTA and our own IP**, so the reputation exposure is ours rather than the customer's provider. Practically it means **AI-generated mail is sent with no content scoring and against no quota** — the agent drafts a reply from an incoming message and it goes out unscored. **The floor is not zero:** the MTA worker applies suppression, warm-up, DKIM enforcement, VERP return-path and per-ISP throttling to *everything* queued, verified 2026-07-29, so the highest-consequence controls still hold. | HIGH | 2026-07-30 | **FIXED 2026-08-04** — new `apps/api/src/lib/pre-send-gate.ts` holds the stack (quota → suppression → compliance → spam → anomaly → headers), with response bodies unchanged so no API contract moved; the existing send-path suites passed untouched, which is the proof the extraction was faithful. Ordering is asserted in tests, not just outcomes — a refusal must never cost a warm-up slot, a quota count or a Claude call. **`agent-send.ts`** now runs the full gate *before* its `emails` insert (a refused draft leaves no row, no delivery_results, no job) and records against quota + the anomaly baseline, which it never did. **Two of the other three producers turned out to be worse than this row said, and neither was gated — both were removed instead.** `routes/agent.ts`'s `queueDraft` hook: the comment above it claimed queueing was "intentionally omitted" while the hook sat directly beneath, and `InboxAgent.run()` calls it for **every** drafted reply whenever `dryRun` is false, consulting `requiresApproval` nowhere — `dryRun` defaults to false, so every AI-drafted reply went onto the real send queue **with no user approval anywhere in the path**. Its job could not have sent anyway (`accountId` set to the *recipient's address*, `from` the literal `"agent-draft"`, `rawMessage` the bare body rather than RFC-5322); nothing escaped only because the MTA is stopped. Removed rather than gated — the approval path it duplicated already exists and is correct. `routes/unsubscribe.ts`'s mailto branch enqueued a job with **no `email` key at all** while the worker reads `job.data.email`, so every one threw on first property access and retried to the DLQ — and because the runner treats "callback did not throw" as success, history and UI both reported the unsubscribe as **done**, leaving a user believing they were off a list they are still on. Now fails honestly; not repaired, because `from` was caller-supplied and never checked against a domain the account owns (a spoofing vector the moment the shape is corrected) and the send needs an accountId + verified domain the helper never receives — see issue #152. |
| 152 | **`POST /v1/messages` returned 422 for every non-transactional email — the core send path refused ordinary mail.** `contentType` was `isTransactional ? "transactional" : "marketing"`, so every plain message was classified as a commercial bulk campaign. Run against the real `ComplianceEngine` that is **ten critical violations** (no physical postal address, no unsubscribe link, no List-Unsubscribe header, no recorded GDPR or CASL consent) and the handler refuses on `violations.length > 0`. A plain reply from your own verified domain was rejected as a non-compliant marketing campaign. **It survived because `messages.test.ts:96` mocks `ComplianceEngine.checkAll()` to return zero violations** — the real engine had never run in a test, so a completely broken send path sat under a green suite. Found by extracting the gate for #151, not by any test. Same class as #120's lesson: a green suite said nothing, only running the real thing did. | CRITICAL | 2026-08-04 | **FIXED 2026-08-04** — `classifyContent()` identifies marketing **positively** (an explicit `marketing`/`campaign`/`newsletter`/`bulk`/`promotional` tag, or a template — which is how campaigns are actually assembled in this API) instead of by elimination. Everything else is `correspondence`, which takes the engine's exempt branch alongside `transactional`; neither is commercial bulk mail under CAN-SPAM/GDPR/CASL, so the marketing-only requirements do not apply to a one-to-one message. The new tests **deliberately do not mock the engine**, and one pins the opposite direction — a genuine marketing send carrying no unsubscribe mechanism is still refused — so this cannot decay into a blanket compliance bypass. **Worth Craig's eye as policy, not code:** the tag/template signals are what now separate "campaign" from "correspondence". A new bulk mechanism must add its signal here or it will be treated as correspondence. |
| 153 | **Received spam was filed into the user's inbox.** `services/inbound/src/storage/postgres-store.ts` wrote `tags: [mailboxId]` and never set `folder` — the pre-#88 convention. Issue #88 added the real `folder` column, made `GET /v1/messages` filter on it, and defaulted it to `"inbox"` at the schema level. So the filter pipeline detected spam correctly, tagged it, and then filed it exactly where clean mail goes: the outcome was identical to having no inbound filter at all. Directly on the business-email path — the first thing a new mailbox would have done is deliver spam to the inbox. | MEDIUM | 2026-08-04 | **FIXED 2026-08-04** — `folder` written from the same value as the tag, so the two cannot disagree. `"spam"` added to the `ListMessagesQuery` folder enum: without it the fix would trade one bug for another, filing quarantined mail correctly and then making it unreachable through the API. `"rejected"` is mapped rather than folded into `"spam"` — unreachable today (both ingress paths reject before `store()`), but a message we told the sender we refused must never surface in a mailbox listing. Also fixed in passing: `services/inbound`'s `start` script ran `bun run dist/index.js` while `build` was an `echo` producing no dist (the issue #120 defect class, in the one service Craig still has to deploy), and `business-email-domain-onboarding.md`'s warning that `services/dns` hard-codes `.dev` hostnames — stale and actively misleading, there are zero occurrences of `alecrae.dev` in `services/dns/src` and the values now live in `config.ts` with production `.com` defaults. |
| 30 | Thin API test coverage| 137 | **Honest degradation signals the UI ignored — placeholder text presented as AI output.** Swept the class the Translation bug belonged to (backend correctly reports "this was not AI", frontend never reads it). `/v1/ai/write/compose` returns `degraded: true` with a canned body when Claude is unavailable — text that literally reads *"[AI-composed content would appear here … when Claude API is configured.]"*. The inbox called `setQuickReplyDraft(res.data.body)` with no check, so **that string landed in the user's reply box as an AI draft they could send to a real recipient**. Same for AI summarize. `degraded` was added by issue #99 precisely so callers could distinguish, and no caller read it. **Also: issue #98's fix was incomplete** — recorded as replacing the fabricated `confidenceScore: 0.3` path with a real error, it fixed only the compose-in-voice endpoint it named; `/v1/voice/draft` and `/adjust` still returned 200 with a placeholder body and a fabricated `confidence: 0.3`. | HIGH | 2026-07-29 | **FIXED 2026-07-29** — `degraded` declared on the result types (so omitting it is a type error, not a silent gap) and both inbox handlers now refuse rather than drafting nonsense; voice fallbacks now `confidence: 0` + `degraded: true`, matching the precedent #99 set across every ai-writing fallback. Checked and left alone: `attendeesAvailable`/`analysisAvailable` are already consumed, and `/proofread` deliberately has no flag because its fallback is genuine heuristic scoring, not a placeholder. |
| 30 | Thin API test coverage| 136 | **Response-envelope and request-contract mismatches, found by a sweep rather than page-by-page.** Classified all 356 typed frontend fetch sites and all 691 backend endpoints as bare-vs-`{ data }` and diffed. Two real defects (six candidates were classifier artifacts — a long handler's error branch returning `{ data }` while its success path is bare — each checked individually rather than assumed): **(a)** the Integrations page reads `GET/POST /v1/webhooks` bare though both are enveloped, so `setWebhooks()` received the envelope and the list never rendered. `lib/api.ts` had it right; the page hand-rolls its own fetch and drifted. Notable because the 2026-07-22 audit called this section "fully real" — the dispatcher behind it does work, so the section reads trustworthy; the bug is in the page. **(b)** Auto-Responder preview sent `{ sampleEmailBody }`, a field the schema does not declare, while the schema REQUIRES senderEmail/subject/body, and expected a bare `{ reply }` where the endpoint returns an envelope — three mismatches at once, 422 every time. | MEDIUM | 2026-07-29 | **FIXED 2026-07-29** — both corrected. The preview form now collects sender and subject, since the rendered reply addresses the sender and quotes the subject back. Also renamed "AI Reply Preview" → "Reply Preview" and made the result label follow the endpoint's own `aiGenerated: false` flag: the reply is templated, not model-written, so the heading had been making a claim its own backend contradicts. |
| 30 | Thin API test coverage| 129 | **The E2EE feature could never have worked, and the page said it did.** `/v1/encryption/keys/generate` generated the keypair ON THE SERVER and stored ITS public key, while the browser independently generated its own pair and kept that private key in IndexedDB — the registered public key and the user's private key came from **two unrelated pairs**, so anything encrypted to the stored key could never have been decrypted by the user. A private key therefore also existed in server memory (defeating the zero-knowledge claim outright), wrapped with `passphrase.padEnd(32,"0").slice(0,32)` used directly as raw AES key material — no KDF, no salt. The passphrase was a request field while the comment beside it read "the passphrase never touches the server"; the client was already sending a random throwaway purely to satisfy the schema. Separately, the settings page claimed "Emails you send… are encrypted automatically" when **nothing encrypts anything** — there is no encryption step in the send path and no decryption on read. Supersedes and expands issue #77(d). | CRITICAL | 2026-07-29 | **FIXED 2026-07-29** — server stores only a CLIENT-supplied public key, generates nothing, accepts no passphrase; optional client-encrypted private-key blob stored opaquely for cross-device recovery. Client registers the public half of the pair it actually holds. UI claim removed and replaced with an explicit "your email is not encrypted yet"; the endpoint returns `messageEncryptionActive: false` + a notice, so no future UI can imply otherwise without contradicting the API it calls. **Still OPEN:** the encrypt-on-send / decrypt-on-read pipeline itself — deliberately NOT half-built, since it needs key discovery, MIME encryption, and decisions about search and every server-side AI feature once bodies are opaque. Scoped project, not an audit fix. |
| 77 | **Backend findings from wiring tranche 5**# ALECRAE — THE BIBLE

> **This document is the single source of truth for AlecRae.**
> **Read it FIRST. Reference it ALWAYS. Violate it NEVER.**

---

## ⚡ THE PRIME DIRECTIVE

**AlecRae kills Gmail. AlecRae kills Outlook. AlecRae kills Superhuman.**

Email has not been reinvented since 2004. We are the reinvention. There is no second place. We dominate or we die. Every line of code, every component, every decision, every commit must serve this mission.

**The standard:** 80-90% ahead of every competitor at all times. Not 10%. Not 30%. Eighty to ninety percent.

If a competitor closes the gap, we accelerate. If new technology threatens our lead, we absorb it or destroy the need for it. We are not in a race — we are lapping the field.

---

## 📖 THE BIBLE RULE

**Before ANY new build, ANY refactor, ANY significant change — READ THIS FILE FIRST.**

This file is read at the start of every session. It is referenced before every architectural decision. It is updated at the end of every session. No work happens outside the framework defined here.

**No scatter-gun. No drift. No "just this once."** Every action ties back to this document.

### 🕒 THE TIMESTAMP RULE

**Every documentation file carries a full date AND time stamp, kept current.**

- Any doc file (`CLAUDE.md`, `docs/**`, `README`s, ADRs, runbooks) that is touched **must** have its `Last updated:` line refreshed in the same change, in the format `YYYY-MM-DD HH:MM UTC` (date AND time, not date alone).
- Docs are kept current on a **daily cadence** — if you work in a doc, it gets today's stamp. Stale stamps mean stale docs.
- The stamp is applied **on every edit** (when content changes), so the timestamp always reflects a real change — not a meaningless background bump.
- **Enforced in CI:** `bun run docs:check` fails if any doc is missing a valid stamp (the `Docs` workflow gates every push + PR). Run `bun run docs:fix` to stamp/normalize. **Docs stay clean and green at all times.**

---

## 👑 THE BOSS RULE — CRAIG MUST AUTHORIZE

The following actions require **explicit authorization from Craig (the boss/owner) BEFORE execution**:

1. **Major architectural changes** — swapping frameworks, changing core stack, altering data model
2. **New dependencies that aren't already in the approved stack** — we don't add bloat
3. **Pricing changes** — any modification to plans, tiers, or billing logic
4. **Domain or DNS changes** — anything touching alecrae.com or its subdomains
5. **Production deployments** — first-time deploy and any rollback
6. **Stripe configuration** — webhook URLs, price IDs, plan structures
7. **Schema migrations on production database** — irreversible changes need sign-off
8. **External API integrations** — adding new third-party services
9. **Brand/marketing changes** — copy on landing page, logos, taglines
10. **Anything that touches money, users' data, or public-facing communication**

**The rule:** When in doubt, ask Craig. Cost of asking = 30 seconds. Cost of acting wrong = days of damage.

**The exception:** Craig has pre-authorized continuous building of features within the existing build plan and stack. Routine code, bug fixes, refactors within the approved architecture, and committing/pushing to the development branch do NOT require additional authorization.

---

## 🎯 THE MISSION

Build the fastest, smartest, most beautiful, most aggressive email client ever made. One subscription. Every account. Every device. Every language. AI in every layer. No ads. No data mining. No bloat. No compromise.

**The customer sees:** Magic. Speed. Beauty. Their email actually works.
**The competition sees:** A force they cannot match without rebuilding from scratch.
**Craig sees:** Recurring revenue with 85%+ margins on a moat that compounds over time.


---

## 🔥 THE AGGRESSIVE STACK

Every tool here was chosen because it is the **best in its class right now**. If something better emerges, we replace it without sentiment. Loyalty is to the mission, not the tools.

### Backend & Runtime
| Layer | Choice | Why |
|---|---|---|
| **Runtime** | Bun | 52K req/s, 10-20x faster installs, native TS, replaces npm/yarn/pnpm |
| **API Framework** | Hono | 4x faster than Express, runs everywhere, RegExpRouter is the fastest JS router |
| **Type Safety** | TypeScript strict mode | No `any`, no `@ts-ignore`, no exceptions |
| **API Layer** | tRPC + REST + OpenAPI | Type-safe end-to-end, no codegen, no drift |
| **ORM** | Drizzle | 7.4KB bundle, SQL-like TS, optimal for serverless cold starts |
| **Validation** | Zod | Schema validation at every boundary |

### Frontend & UI
| Layer | Choice | Why |
|---|---|---|
| **Framework** | Next.js 15 (App Router) | RSC, streaming, deployment to Cloudflare Pages |
| **Language** | TypeScript strict | Same rules as backend |
| **Components** | Custom design system + Radix primitives | Accessible, themeable, ZERO HTML in app code |
| **Styling** | Tailwind CSS | Utility-first, atomic, zero unused CSS shipped |
| **State** | Signals + TanStack Query | Reactive, server-state aware, no Redux bloat |
| **Editor** | Tiptap (compose) | Best-in-class rich text |
| **Animation** | Motion (Framer Motion) | Spring physics, layout animations |
| **Bundler** | Turbopack | Rust-based, 10x faster than Webpack |
| **Linter/Formatter** | ⚠️ **ESLint + Prettier (actual)** — Biome (aspirational) | Verified 2026-07-28: there is no Biome config anywhere in the repo, no `@biomejs` dependency, and every workspace's `lint` script runs `eslint`/`next lint`. The root `package.json` devDependencies are `eslint`, `typescript-eslint` and `prettier`, and `format` runs Prettier. This row said "Biome — no ESLint, no Prettier", and the Forbidden List still implies it. Either migrate to Biome or drop the claim — but the doc must not keep asserting a tool the repo does not use. |

### AI Layer
| Layer | Choice | Why |
|---|---|---|
| **Primary LLM** | Claude (Anthropic) | Best reasoning, best at following instructions, fastest improvement curve |
| **Models** | Haiku 4.5 (default), Sonnet 4.6 (Pro), Opus 4.6 (Enterprise) | Tier features by model power |
| **Transcription** | Whisper API (OpenAI) | Best multi-language ASR |
| **Local Inference** | Transformers.js / WebLLM | Free, private, runs on user GPU |
| **Translation** | Claude API | Beats Google Translate on context |
| **Embeddings** | Voyage AI (when added) | Best semantic search quality |

### Data
| Layer | Choice | Why |
|---|---|---|
| **Primary DB** | Neon Serverless Postgres | Scale-to-zero, branches like Git, edge replicas |
| **Cache/Queue** | Upstash Redis | Serverless, CF Workers compatible, REST API |
| **Search** | Meilisearch | Sub-50ms full-text, typo tolerance, zero config |
| **Object Storage** | Vapron Object Storage | S3-compatible, integrated with platform |
| **Local Cache** | IndexedDB | Browser-native, offline-first, infinite size |
| **Analytics DB** | ClickHouse | Time-series at scale (when needed) |

### Infrastructure
| Layer | Choice | Why |
|---|---|---|
| **Hosting** | Vapron | Permanent platform — AI gateway, email, object storage, deploy |
| **DNS** | Cloudflare (alecrae.com zone) | Mail records must be DNS-only — see multi-platform-mail-plan.md |
| **CDN** | Vapron | Built-in with platform |
| **Container Registry** | Vapron Object Storage | S3-compatible, integrated |
| **GPU Compute** | Modal.com | A100/H100 on-demand for heavy AI |
| **Long-Lived Processes** | Jarvis box (66.42.121.161) | API, web, MTA — systemd-managed behind Coolify/Traefik |
| **CI/CD** | GitHub Actions | Already wired |
| **Monitoring** | OpenTelemetry + Grafana LGTM stack | Vendor-neutral observability |

### Auth & Security
| Layer | Choice | Why |
|---|---|---|
| **Primary Auth** | Passkeys / WebAuthn (FIDO2) | 98% login success vs 13.8% for passwords |
| **OAuth** | Direct integrations (Google, Microsoft) | Email account connection |
| **JWT** | jose library | Standards-compliant, fast |
| **Encryption** | Web Crypto API (RSA-OAEP-4096 + AES-256-GCM) | Native browser, FIPS-equivalent |
| **TLS** | TLS 1.3 minimum | No exceptions |

### Payments
| Layer | Choice | Why |
|---|---|---|
| **Billing** | Stripe | Industry standard, best DX, lowest churn tools |
| **Plans** | Free / Personal $9 / Pro $19 / Team $12pp / Enterprise | Fixed — Craig must authorize changes |

### Mobile & Desktop
| Layer | Choice | Why |
|---|---|---|
| **Desktop App** | Electron (initially), Tauri (v2) | Ship fast, optimize later |
| **Mobile App** | React Native + Expo | Single codebase, native performance |
| **PWA** | Built into Next.js | Day-one install on any device |


---

## ⚔️ THE AGGRESSIVE ARCHITECTURE

### Three-Tier Compute Model
```
CLIENT GPU (WebGPU) ──→ EDGE (Cloudflare Workers) ──→ CLOUD (Modal GPUs)
       $0/token              sub-50ms                    Full H100 power
       sub-10ms              lightweight inference        heavy AI / training
       grammar/triage        compose/translate            voice profile train
```

The platform decides where each request runs based on cost, latency, and capability. **The user never sees the tier. They just see speed.**

### Local-First Architecture
- All emails cached in IndexedDB on first sync
- UI reads from local cache (sub-50ms)
- Background workers sync changes to/from server
- Offline support out of the box
- Optimistic UI updates with rollback on failure

### Edge-First Deployment
- Every API route deployable to Cloudflare Workers
- Sub-50ms response times globally
- No regional bottlenecks
- Stateful workloads (MTA, WebSocket) on Fly.io microVMs

### AI-Native Architecture
**AI is woven into every layer, not bolted on:**
- AI in routing (predictive prefetch)
- AI in data fetching (smart cache invalidation)
- AI in UI (adaptive density, smart suggestions)
- AI in error recovery (self-healing)
- AI in search (natural language)
- AI in compose (voice profile + grammar agent)
- AI in triage (priority inbox + commitments)
- AI in security (threat detection)

### Component Architecture
- **ZERO HTML in app code.** Everything is a component.
- Every component has a Zod schema for AI composition
- Every component is themeable, accessible, keyboard-navigable
- Server Components by default, Client Components only when needed
- Storybook for visual testing (when added)

---

## 🛡️ THE QUALITY BAR

### Performance Budgets — CI FAILS IF VIOLATED
| Metric | Budget |
|---|---|
| First Contentful Paint | < 1.0s |
| Largest Contentful Paint | < 1.5s |
| Time to Interactive | < 2.0s |
| Inbox load (cached) | < 100ms |
| Inbox load (cold) | < 1.5s |
| Search response | < 50ms (local), < 200ms (server) |
| API response (edge) | < 50ms p99 |
| API response (cloud) | < 200ms p99 |
| AI response (client) | < 200ms |
| AI response (edge) | < 500ms |
| AI response (cloud) | < 2s |
| Initial JS bundle | < 100KB |
| Email send time-to-delivered | < 2s |

### Code Standards — NO EXCEPTIONS
- TypeScript strict mode (`strict: true`, `noUncheckedIndexedAccess: true`)
- No `any`. No `@ts-ignore`. No `as unknown as X`. Use `unknown` and narrow.
- Every function has explicit return types
- Every prop has explicit types
- Every API boundary has Zod validation
- Every error case has typed handling (Result types preferred over try/catch for business logic)
- Conventional commits: `feat:`, `fix:`, `perf:`, `refactor:`, `test:`, `docs:`, `ci:`, `chore:`
- All public APIs have OpenAPI specs
- All endpoints have integration tests
- All components have visual snapshots
- Biome formats and lints — no ESLint, no Prettier

### Component Rules
- ZERO raw HTML elements outside of `packages/ui` primitives
- Every component must be accessible (ARIA, keyboard nav, screen reader friendly)
- Every component must support themes (light/dark/system + accent colors)
- No inline styles — Tailwind classes or CSS modules only
- No CSS-in-JS runtime (build-time only)
- Server Components by default

### AI Integration Rules
- All AI calls have fallback behavior if AI is unavailable
- AI decisions are logged and auditable
- Confidence scores accompany all classifications
- User data used for AI must be anonymizable
- Model selection is automatic based on task complexity
- All AI interactions are traced via OpenTelemetry
- Destructive AI actions require human-in-the-loop approval

### Accessibility — CHARLIE BROWN TO 007
**AlecRae must work for novices AND experts equally well:**
- WCAG 2.2 AA minimum (target AAA where possible)
- Full keyboard navigation (every action has a shortcut)
- Screen reader optimization (real ARIA, real focus management)
- Voice control (dictation engine + command palette)
- High contrast mode
- Reduced motion mode
- Adjustable density (compact/comfortable/spacious)
- Adjustable font sizes (small/medium/large)
- Color blind safe palettes
- Minimum touch target 44x44px on mobile

### Security Requirements
- No secrets in code — env vars or secrets manager only
- All inter-service communication over TLS 1.3
- Rate limiting on every public endpoint — 6 tiers, **with one deliberate exception**: `/t/*` (open pixel, click redirect, one-click unsubscribe) carries no IP rate limit, because mail providers fetch those through image proxies where a handful of IPs act for enormous numbers of unrelated recipients. It is throttled **per email id** instead (`lib/tracking-throttle.ts`, issue #147) — the axis the abuse actually lives on. Verified 2026-07-29; the unqualified "every public endpoint" claim was false before that.
- Input validation at every boundary (Zod)
- CSP headers on all web responses
- HSTS preloading
- Regular dependency audits (Renovate + Dependabot)
- E2E encryption for users who enable it
- Zero-knowledge architecture for encrypted content
- No third-party trackers, no analytics that send PII off-server


---

## ❌ THE FORBIDDEN LIST

**NEVER do these things. Ever. Without exception:**

1. **Never write raw HTML in app code.** Components only.
2. **Never use `any` type.** Use `unknown` and narrow.
3. **Never use `@ts-ignore`.** Fix the type.
4. **Never commit secrets.** Env vars only.
5. **Never skip tests for "speed."** Untested code does not exist.
6. **Never use external JavaScript trackers.** No Google Analytics, no Hotjar, no Mixpanel-as-default.
7. **Never sell user data.** Period. This is the moat.
8. **Never show ads in the email client.** We're not Gmail.
9. **Never break the local cache contract.** Reads from cache must always return.
10. **Never deploy to production without Craig's authorization.**
11. **Never modify Stripe configuration without Craig's authorization.**
12. **Never add a dependency that isn't in the approved stack without Craig's authorization.**
13. **Never delete user data without explicit user action AND a 30-day soft-delete window.**
14. **Never ship a feature that isn't accessible.** If a screen reader can't use it, it's broken.
15. **Never use `localStorage` for sensitive data.** IndexedDB with encryption only.
16. **Never trust user input.** Validate everything with Zod.
17. **Never block on a single AI provider.** Always have a fallback path.
18. **Never let an error bubble unhandled to the user.** Wrap, log, recover, retry.
19. **Never silently fail.** Errors are visible to monitoring.
20. **Never ship a feature without a CLAUDE.md update.** This file is the source of truth.
21. **Never approve a PR you didn't read end-to-end.**
22. **Never use the word "Vienna" or "Emailed" in user-facing copy.** It's AlecRae.
23. **Never refer to competitors by name in marketing.** Show, don't tell.
24. **Never make up user metrics for marketing.** Real numbers or no numbers.
25. **Never let speed be an excuse for sloppiness.** Move fast WITHOUT breaking things.

---

## 📋 PRE-FLIGHT CHECKLIST (BEFORE EVERY BUILD)

Before writing a single line of new code:

1. ✅ Read the relevant section of CLAUDE.md
2. ✅ Confirm the task is in the build plan (TIER 1-4)
3. ✅ Confirm the task doesn't require Craig's authorization
4. ✅ Confirm the existing patterns to follow (check similar files)
5. ✅ Confirm the dependencies are already in the approved stack
6. ✅ Confirm the performance budget for this feature
7. ✅ Confirm the accessibility requirements
8. ✅ Identify which tests need to be added
9. ✅ Identify which routes/APIs need to be wired
10. ✅ Plan the commit message in advance

---

## 🧪 POST-BUILD CHECKLIST (BEFORE COMMITTING)

After writing the code:

1. ✅ Tests pass locally (`bun run test`)
2. ✅ Type check passes (`bun run typecheck`)
3. ✅ Lint passes (`bun run lint`)
4. ✅ Build passes (`bun run build`)
5. ✅ No `any`, `@ts-ignore`, `console.log` left over
6. ✅ All new endpoints registered in server.ts
7. ✅ All new routes have rate limiting + auth
8. ✅ All new schemas exported from packages/db/src/index.ts
9. ✅ CLAUDE.md updated with new feature in build status
10. ✅ Conventional commit message ready
11. ✅ Performance budget verified
12. ✅ Accessibility verified (keyboard nav works)

---

## 🚨 EMERGENCY PROTOCOLS

### Production Outage
1. **Check status page** (when set up): status.alecrae.com
2. **Roll back** to last known good commit
3. **Notify Craig** immediately
4. **Post-mortem** within 24 hours, written and committed to `docs/postmortems/`
5. **Add a test** that prevents the same failure

### Security Incident
1. **Immediately revoke** any compromised credentials
2. **Notify Craig** within 15 minutes of discovery
3. **Rotate ALL secrets** even tangentially related
4. **Audit log review** for the affected period
5. **Notify affected users** within 72 hours (GDPR requirement)
6. **Public disclosure** if appropriate, within 30 days
7. **Post-mortem** + prevention plan

### Data Loss
1. **Stop writes immediately** to prevent further loss
2. **Restore from most recent backup** (Neon point-in-time recovery)
3. **Notify Craig** + affected users immediately
4. **Verify integrity** of restored data before resuming writes
5. **Post-mortem** + prevention plan

### Cost Overrun
1. **If AI cost spikes 10x normal:** Auto-throttle to free tier limits
2. **If infrastructure cost spikes:** Alert Craig immediately
3. **Set budget alerts** on every paid service
4. **Review monthly** — anything growing unexpectedly gets investigated


---

## 💰 PRICING & REVENUE (LOCKED — CRAIG ONLY)

### Plans
| Plan | Price | Includes |
|---|---|---|
| **Free** | $0/mo | 1 account, basic AI (5 composes/day), 30-day search, no E2EE |
| **Personal** | $9/mo | 3 accounts, full AI, unlimited search, E2EE, snooze, schedule send |
| **Pro** | $19/mo | Unlimited accounts, priority AI (Sonnet), team features, API access, analytics |
| **Team** | $12/user/mo | Shared inboxes, admin console, audit logs, SSO, priority support |
| **Enterprise** | Custom | On-prem option, compliance, dedicated support, SLA, Opus AI |

**These prices are LOCKED. Changes require Craig's authorization.**

### Business Email Plans (native mailboxes on your own domain) — APPROVED 2026-06-11 by Craig
Separate product line from the consumer tiers above. Multi-currency from launch
(localized price points, NOT FX conversion; Stripe Tax for GST/VAT). Positioned
in the gap between Google Workspace Standard (floor) and MS 365 Premium+Copilot
(ceiling), bundling all AI included.

| Per user/mo | **Business** | **Business Plus** |
|---|---|---|
| NZD (incl. GST) | $25 | $45 |
| AUD (incl. GST) | $22 | $39 |
| GBP (+VAT) | £15 | £28 |
| EUR (+VAT) | €16 | €30 |
| USD | $16 | $30 |
| CAD (+tax) | $22 | $42 |

Business = mailboxes on your domain, full AI, shared inboxes, admin, SSO,
migration. Plus = priority AI (Sonnet/Opus), advanced compliance/eDiscovery,
larger storage, unlimited accounts, dedicated support. Billing wiring pending
(currency-aware: price per tier per currency).

### Add-On Revenue
- Custom domain email hosting: $4/user/mo
- Priority AI processing: $5/mo
- Email analytics premium: $7/mo
- API access (usage-based): $0.01/call
- White-label licensing: $2K-$10K/mo

### Revenue Targets
| Stage | Users | MRR | Team |
|---|---|---|---|
| Beta | 500 free / 50 paid | ~$700/mo | Craig + AI |
| PMF | 2K free / 500 paid | ~$6K/mo | Craig + AI |
| Growth | 10K free / 2K paid | ~$25K/mo | Craig + 1 dev |
| Scale | 50K free / 10K paid | ~$130K/mo | 5 |
| Series A | 200K free / 40K paid | ~$500K/mo | 15 |
| Exit | 1M+ free / 200K paid | ~$2.5M/mo | 40 |

---

## 🌐 DOMAIN & INFRASTRUCTURE

### Domains (live state verified 2026-07-13)
- **alecrae.com** — Landing site (Jarvis box via Cloudflare proxy) ✅ 200
- **mail.alecrae.com** — Email web app (Jarvis box) ✅ 200 — ⚠ must become DNS-only (grey cloud) for SMTP (mail plan Phase 0)
- **api.alecrae.com** — API server (Jarvis box) ✅ 200
- **mx1/mx2.alecrae.com** — Inbound MX — ❌ NO A RECORDS YET (mail plan Phase 0)
- **smtp.alecrae.com** — Authenticated submission — ❌ not created yet (optional, later)
- **status.alecrae.com** — ❌ 503, no app deployed (issue #71)
- **admin.alecrae.com** — never resolved; admin console lives inside the web app at `/admin` (apps/admin was deleted)
- **docs.alecrae.com** — Developer docs (when set up)

### Hosting Stack (actual, since 2026-07)
- **Compute:** Jarvis box `66.42.121.161` — systemd (`alecrae-api` :4100, `alecrae-web` :4200) behind Coolify/Traefik (owns 80/443)
- **Database:** local PostgreSQL 16 on the box (136-table baseline migrated)
- **Cache/Queue:** local Redis on the box (BullMQ queues) — **installed 2026-07-14**; before that redis-server was NEVER installed on Jarvis (this doc claimed it was — the API spammed `ECONNREFUSED 127.0.0.1:6379` and every queue-backed feature was degraded). Binds 127.0.0.1 only — **which is why the MTA cannot see the send queue; see issue #149.** Craig has decided to bind it to the tailnet so both boxes share one instance (`docs/infra/redis-tailnet-setup.md`); update this line once that is executed on the box. (The stack table's "Upstash Redis" row is the serverless/edge aspiration, not what production runs — and nothing has ever read the `UPSTASH_REDIS_REST_*` vars the env template used to list as required.)
- **Object Storage:** Vapron Object Storage (S3-compatible)
- **DNS:** Cloudflare (alecrae.com zone)
- **Mail:** self-hosted MTA + inbound (code-ready, NOT running — `docs/infra/multi-platform-mail-plan.md`)
- **Monitoring:** OpenTelemetry → Grafana (aspirational — nothing alerting today; issue #72)

---

## 🚀 DEPLOYMENT GATES

**Production deployment requires ALL of these to be green:**

1. ✅ All tests pass (`bun run test`)
2. ✅ Type check passes
3. ✅ Lint passes
4. ✅ Build artifacts generated successfully
5. ✅ E2E tests pass against staging
6. ✅ Performance budgets met (Lighthouse CI)
7. ✅ Accessibility audit passes
8. ✅ Security scan clean (Dependabot, secret scanning)
9. ✅ Database migrations tested on staging
10. ✅ Rollback plan documented
11. ✅ **Craig has authorized the deployment**
12. ✅ Status page updated
13. ✅ On-call engineer available for next 2 hours

**Staging deployments:** Auto-deploy from main branch.
**Production deployments:** Manual trigger after ALL gates pass + Craig authorization.

---

## 🎨 BRAND & VOICE

### The AlecRae Brand
- **Name:** AlecRae (always capitalized as "AlecRae", never "ALECRAE" or "alecrae")
- **Tagline:** "Email, Evolved."
- **Tone:** Confident, sharp, no corporate fluff. Speak like a human who knows what they're doing.
- **Colors:** TBD (Craig to approve)
- **Logo:** TBD (Craig to approve)

### Copy Rules
- Never use the word "Vienna" or "Emailed" in user-facing text — those were codenames
- Never refer to competitors by name in marketing copy
- Never use marketing buzzwords ("synergy", "leverage", "best-in-class")
- Never claim features we don't have
- Never make up user numbers
- Always be specific ("3x faster than Gmail" with proof, not "blazing fast")

### Marketing Strategy
- **Phase 1 (Build in Public):** Weekly X/Twitter updates, "Gmail is 22 years old" narrative
- **Phase 2 (Private Beta):** 500 power users, weekly feedback calls
- **Phase 3 (Public Launch):** Product Hunt #1, Hacker News, tech press
- **Phase 4 (Growth):** SEO, content, referrals, enterprise sales


---

## 📦 BUILD STATUS

All planned feature tiers are **code-complete**: Tiers 1–8 (36 core + 20 expansion + 9 platform + 6 intelligence + 6 deep-AI), Tier S/A/B (industry firsts + UX + power features), and 7 bonus features — **84 features, 97 API route files, 681 endpoints, 136 DB tables, ~64K lines of TypeScript**. The per-tier checklists are archived verbatim in `docs/archive/claude-bible-archive-2026-07-13.md`.

**The honest caveat (2026-07-13 full audit):** code-complete ≠ reachable product.

- **46% of endpoints / 60% of route files have web UI** — 39 route files are fully unwired. Live numbers: `docs/audits/route-coverage.md` (regenerate with `bun run audit:routes`, CI-gated for new routes). Prioritized wiring roadmap: `DEVOPS_TRACKER.md` §3.
- ~10 backend endpoints are still stubs (Known Issue #29 below).
- Tier C compliance items (SOC 2, DPA workflow, bug bounty, public roadmap) are not started — legal/process tasks, not code.
- Email sending/receiving is code-ready but **not operational** — see `docs/infra/multi-platform-mail-plan.md` (the plan for all platforms sending/receiving through alecrae.com).

### 📚 GROUND-TRUTH SOURCES (which doc is authoritative for what)

| Question | Authoritative source |
|---|---|
| Live infra state, box, outages | `DEVOPS_TRACKER.md` |
| Backend↔UI wiring coverage (heuristic — string-match only, not a functionality check) | `docs/audits/route-coverage.md` (generated — never hand-edit) |
| **Does a feature actually work, end to end, right now** | `docs/audits/2026-07-22-full-journey-audit.md` — 49 dashboard journeys, each independently traced frontend→API→DB/AI, verdict + evidence + fix estimate per journey, with a build-plan checklist. **Trust this over any "FIXED" claim below for the 49 journeys it covers** — it supersedes prior status claims and won't be lost like the 2026-07-19 audit was; re-run periodically rather than assumed to still be accurate as code changes. |
| Mail architecture + multi-platform plan | `docs/infra/multi-platform-mail-plan.md` |
| Incident history | `docs/postmortems/` |
| Historical build detail + fixed issues | `docs/archive/claude-bible-archive-2026-07-13.md` + git history |
## 🔧 KNOWN ISSUES — OPEN

> Issues #1–#57 and #63–#65 are resolved; their full history (root causes + fixes) is archived in `docs/archive/claude-bible-archive-2026-07-13.md`. Numbering continues from there. Keep this table OPEN-items-only; when an issue is fixed, move its row (with the fix note) to the archive.

| # | Issue | Severity | Found | Status |
|---|-------|----------|-------|--------|
| 8 | ~~`emailStatusEnum` missing "draft" value — using "queued" as workaround~~ **This was wrong.** `emailStatusEnum` has had `"draft"` as its first value all along (`packages/db/src/schema/emails.ts:21`). The stale note is likely why Drafts was built against `status: "queued"` — i.e. a wrong doc caused a real bug. | LOW | 2026-04-09 | **CLOSED 2026-07-28** — claim disproven; real drafts persistence shipped the same day (POST/PUT `/v1/messages/drafts`, status `"draft"`, folder `"drafts"`). |
| 29 | **~10 backend endpoints still stubs** (re-verified 2026-07-13): files presigned-upload (mock URL — uploads don't persist), voice-message storage (placeholder URL, lost on restart), documents ai-assist, search-intelligence suggestions/trending/related (3), video-meetings summarize, ai-categorization test/retrain (2), attachment-intelligence OCR, contact-enrichment (domain-only), contacts-extended insights, delegation inbox. documents export + warmup + meeting-token encryption are FIXED (see archive). | HIGH | 2026-06-11 | files + voice-message storage **FULLY FIXED 2026-07-21** — 2026-07-20 had only stopped the bleeding (honest 501 instead of a fake URL, see prior note in this cell, kept below for history). Craig then supplied working Vapron platform API docs + a live key, which exposed the ACTUAL root cause: `lib/vapron.ts`'s entire transport was guessed against an unpublished/wrong shape (`api.vapron.ai/api/trpc/...`) — fixed same day as issue #83's real fix, see that row. With a correct client, `POST /v1/files/upload` now calls `vapron.storage.getUploadUrl()` for a real presigned PUT URL, inserts the `files` row only after Vapron actually issues one, and returns an honest 502/503 (never a fabricated success) if that call fails — client PUTs bytes directly to storage, nothing proxied through our API. `POST /v1/voice-messages/record` + `/:id/reply` do the same, PUT the recorded bytes to storage server-side (since the audio already arrived via multipart formData), best-effort transcribe via Whisper if `OPENAI_API_KEY` is set, and persist the `voice_messages` row only after the upload genuinely succeeds; a `/:id/reply` also verifies the parent message is owned by the caller first (404 otherwise). Both routes cap upload size (100MB files, 50MB voice) before ever calling storage. Real regression tests: `apps/api/tests/files-upload.test.ts` (4 tests, passing), `apps/api/tests/voice-message-upload.test.ts` (6 tests — cannot execute in this sandbox due to a pre-existing, unrelated vitest↔`@alecrae/ai-engine` dist-resolution gap also hit by several pre-existing test files on clean `main`; typechecks clean, logic mirrors the passing files-upload suite exactly). **Two things still need live verification once deployed** (this sandbox's outbound network calls with the live key are blocked by the permission classifier): (a) the bucket names `alecrae-files` / `alecrae-voice-messages` need to actually exist in Vapron (or be auto-created — unconfirmed which); (b) the public playback `audioUrl`/file URL is derived by stripping the query string off the presigned PUT URL (standard S3-compatible convention, Vapron Object Storage is documented S3-compatible) — reasonable but unverified against a live upload. Original 2026-07-20 note, kept for history: *investigated first — no working object-storage integration existed anywhere (only listBuckets/createBucket, unreachable prod key); returned honest `501` instead of silently discarding uploads.* **Remaining OPEN:** the other ~8 stubs in this list (documents ai-assist, search-intelligence x3, video-meetings summarize, ai-categorization x2, attachment-intelligence OCR, contact-enrichment, contacts-extended insights, delegation inbox) are untouched by this pass. **UPDATE 2026-07-29:** two are now closed — `ai-categorization` `smart-rules/:id/test` really evaluates the rule against real mail (issue #142), and the attachment virus scan no longer fabricates a verdict (issue #141). Both were listed here as ordinary stubs; the virus scan in particular was **not** a visibly-missing feature but an invented safety verdict, which this row's "stub" framing understated. |
| 120 | **`@alecrae/ai-engine` was unresolvable by anything except Bun, and it silently disabled the core send path's entire test suite.** The package advertises 40+ subpath exports whose `import` condition points at `./dist/*.js`, but its `build` script was `echo '...'` — dist never existed. Only Bun (which takes the `bun` condition, pointing at source) could resolve it, so production ran fine while vitest, Node and every bundler failed outright. Consequence: 25 tests across messages/quota/suppression/voice-message/import-store/persistent-stores **never executed** — that is the core send path, with zero real test coverage, while the post-build checklist claimed a green suite. This was recorded in issue #29 as an unrelated "vitest↔ai-engine dist-resolution gap"; it was neither unrelated nor environmental. Root cause of the mis-emit: `tsconfig` had no `rootDir`, and the inherited repo-root `paths` mapping pointed at sibling packages' *sources*, dragging them into the program and rebasing `outDir` to the monorepo root. Also found: `main`/`types`/`"."` all referenced a `src/index.ts` that does not exist (no caller used it), and `services/mta` had the identical `dist`-that-is-never-built defect. | CRITICAL | 2026-07-28 | **FIXED 2026-07-28** — real `tsc` build + `rootDir` + neutralised `paths`; dead entry points removed from both manifests. apps/api went 180 passing/31 failing → 226/0. **Standing lesson: a green typecheck said nothing here — only running the suite did.** |
| 121 | **`lib/storage-quota.ts` had zero production callers.** `checkStorageQuota`/`incrementStorageUsage`/`decrementStorageUsage` were fully implemented and unit-tested, but only the weekly reconciler was ever wired into `server.ts`. So `POST /v1/files/upload` minted presigned upload URLs with no plan limit checked at all (unbounded object-storage spend — a direct cost exposure for a commercial launch), and deletes never returned the space. `reconcileStorageUsage` separately summed only the `attachments` table, so every weekly run reset accounts to attachments-only and wiped the accounting for directly-uploaded files. | HIGH | 2026-07-28 | **FIXED 2026-07-28** — quota checked before any URL is issued (413 with real usage/limit), usage incremented on insert and decremented on delete, `maxSize` exposed to the UI from the same source as the 413, reconciler now sums both tables. Tests: `files-storage-quota.test.ts`. |
| 122 | **`PlanGate` ignored its own `feature` prop** (`feature: _feature`) and gated on a hand-passed `required`, so `FEATURE_PLANS` — the map the server's `requirePlan(...)` mirrors — was consulted by nothing and all 24 call sites could drift from the server independently. One had: AI Triage passed `"personal"` against the map's and the server's `"pro"`, admitting Personal-tier users to a page whose every request then 403'd. Also found `sso` gated on a feature name absent from the map entirely. | HIGH | 2026-07-28 | **FIXED 2026-07-28** — tier derived from `FEATURE_PLANS`; the override prop removed outright so drift is unexpressible; `feature` typed as the map's key union so an unknown name is a compile error. Audited all 24 call sites — `ai_categorization` was the only tier mismatch. **Still open (deliberately, needs a product call):** the server gates `/v1/sso/config` on admin role with **no** `requirePlan`, so a Free-tier admin can configure SSO via the API — revenue-policy gap, not a security hole. |
| 123 | **Vapron client tests asserted the transport that issue #83 replaced.** After the 2026-07-21 REST rewrite, `vapron.test.ts` still asserted the old tRPC URL/`{json}` envelope for email and AI, and `ai.test.ts`/`transactional-email.test.ts` still mocked tRPC-shaped responses — 6 failing tests left on main, meaning the platform's email/AI/storage integration had **no passing proof of either transport**. | HIGH | 2026-07-28 | **FIXED 2026-07-28** — tests rewritten to pin the documented REST contract (email/AI/storage) and, separately, the tRPC surface still used for DNS/bucket admin, plus error-body shapes, schema rejection and key-leak-free network errors. New assertion: an unparseable Vapron gateway response must surface as a provider failure, never as an empty successful completion. |
| 124 | **Header injection via `subject` and recipient display names — the send path could be used to add hidden recipients to mail we DKIM-sign.** `buildRawMessage()` writes `subject` and display names straight into the `Subject:`/`From:`/`To:`/`Cc:`/`Reply-To:` lines. `SendMessageSchema` accepted `z.string().max(998)` for subject and a bare `z.string()` for names, so `"Hi\r\nBcc: victim@example.com"` injected a real `Bcc:` header — hidden recipients on a message signed as us and relayed from our own IP (or pushed through the customer's Gmail on the connected-account path). `validateCustomHeaders()` was built for exactly this threat but only ever guarded the `headers` object; buildRawMessage's own comment claims custom headers are "guaranteed safe by the validator" while subject/display names are written raw a few lines above it. This is a textbook route to a blocklisting, and it would have looked like we were the spammer. | CRITICAL | 2026-07-28 | **FIXED 2026-07-28** — rejected at the schema boundary (`apps/api/src/types.ts`, so every send path and future caller inherits it) plus a defensive strip in `buildRawMessage`. 10 tests, two of which assert ordinary subjects/display names still pass — an over-broad character class here would block every legitimate send. **REOPENED AND RE-FIXED 2026-07-30:** the fix covered `messages.ts`'s builder only. `lib/agent-send.ts` has a SECOND RFC-5322 builder and wrote every header value straight through — found by asking who enqueues onto the send queue rather than by re-reading the fixed file. Live exposure was `subject`, which the model generates from an incoming email's content, so a newline is reachable by **prompt injection** rather than needing API access — a wider door than the original. `inReplyTo`/`references` interpolate our own internal ids and were checked and ruled out. Sanitiser extracted to `lib/header-safety.ts` and shared by both builders, since a second private copy is exactly how this stayed vulnerable. 11 tests, half on the false-positive direction. |
| 125 | **Nothing inspected outbound message content.** Every pre-send check protected the recipient or the customer (suppression, CAN-SPAM/GDPR/CASL, warm-up, quota, attachment scanning); none read the message. `services/ai-engine`'s `SpamClassifier` was fully built and tested with **zero production callers** — nothing outside its own test file imported it. An authenticated but compromised account could push phishing/419 content through the API and we would DKIM-sign it and relay it from our sending IP. | HIGH | 2026-07-28 | **FIXED 2026-07-28** — `lib/outbound-spam-gate.ts` wires the classifier into both send paths; hard 422 above a tunable block threshold, logged-for-review above a lower one. Scored on the **content layer only**, deliberately: the Bayesian layer returns a flat 0.5 untrained (no trained model ships) and the header layer scores a Received chain an unsent message does not have — including either would put a constant false-positive floor under every legitimate send. Reasons are filtered to the scoring layer so a refusal cannot cite "authentication failures" for an unsent message. Fails open on classifier error. Tests: `outbound-spam-gate.test.ts` (10), covering both false-negative and false-positive directions. |
| 126 | **The connected-account (Gmail/Outlook) fast path returns before every abuse gate** — quota, suppression, compliance, header validation, warm-up. Deliberate for warm-up/throttling (the provider owns the IP), but not for the rest: a compromised account blasting phishing through its connected Gmail risks **Google suspending our OAuth client**, which would break Gmail for every customer at once — our exposure there is the app registration, not an IP. | HIGH | 2026-07-28 | **PARTIALLY FIXED 2026-07-28** — spam gate, send-volume anomaly, and bounce/complaint suppression now apply on this path (`suppression_lists.domain_id` is NOT NULL so there is no account-level row; joined through the caller's own domains). Header-injection safety needed nothing here — it lives in the schema (#124), so this path inherits it. **Still OPEN, deliberately, both needing a product/billing decision rather than a silent change:** unsubscribe suppression (list-scoped consent — blocking a personal 1:1 reply would be over-reach) and per-account quota on this path (a billing behaviour change — Boss Rule #10). |
| 117 | **No send-volume anomaly detection** — only a flat per-account rate limit sized for legitimate heavy use, with nothing comparing current volume to an account's own historical baseline. An account that normally sends 20/hour could suddenly send thousands, each individually under the ceiling. This is exactly why issue #105's abuse ran nine days unseen. (Failed-login/credential-stuffing detection, the other half of this issue, remains open.) | HIGH | 2026-07-20 | **SEND-VOLUME HALF FIXED 2026-07-28** — `lib/send-anomaly.ts`, wired into both send paths. Threshold is `max(floor, baseline × multiplier)` over the account's mean hourly volume for the trailing week; the multiplier catches the blast, the **floor is what stops it punishing normal growth** (a new account has a baseline of 0, so without a floor any send looks infinitely anomalous and a legitimate first campaign would be blocked). Both env-tunable so an operator can tighten mid-incident without a deploy. Redis primary with a **Postgres fallback** — a Redis outage is precisely when an abuser most wants this blind — failing open only if both are unreachable. 7 tests covering both directions. **Still OPEN:** per-account failed-login tracking + credential-stuffing detection, and alerting on any of it (blocked on issue #72). |
| 127 | **THE OPEN RELAY (#105) WAS NEVER ACTUALLY CLOSED.** Issue #105 is recorded as "FIXED same day", but the remediation stopped the service and added TLS + per-IP throttling — **neither of which is a relay control**. The hole itself was untouched and still in the code: `handleRcptTo` (`services/mta/src/smtp/server.ts`) accepted ANY recipient address and answered `250 OK`, with no check that the domain was one we host. The only thing standing between AlecRae and a repeat of the 9-day abuse was that `alecrae-mta` is switched off — **restarting the service would have restored an open relay**, which is the single fastest route to a permanent blocklisting. Found by reading the RCPT handler while auditing spam protection at Craig's request. | CRITICAL | 2026-07-28 | **FIXED 2026-07-28** — `SmtpServerConfig.localDomains`; RCPT TO for a domain we don't host is refused `550 5.7.1`. **Fails CLOSED** (empty/omitted accepts nothing — a deployment that forgets to configure it must reject mail, never relay it), subdomains NOT implicitly accepted (hosting `example.com` must not make us the relay for `anything.example.com`), loaded from active verified `domains` rows **before the listener binds** so there is no accept-anything window, refreshed every 5 min via `updateLocalDomains()` since customer domains are onboarded while the server runs, and degrading to `MTA_LOCAL_DOMAINS` alone if the DB is unreachable. Tests: `relay-control.test.ts` (8), driving a real SMTP conversation over a socket — the path an anonymous remote actually reaches. **This does NOT make the service safe to restart:** the inbound handler in `services/mta/src/index.ts` still logs an accepted message and drops it, so received mail goes nowhere. Relay abuse is closed; inbound delivery is unfinished. The two were bundled under #105 and are separate problems. |
| 82a | **Return-Path/MAIL FROM not rewritten — async bounces never reached suppression.** The worker passed the message's own `From:` as the SMTP envelope sender, so DSNs (the bounce a receiving server returns minutes/hours after accepting) went to the **customer's** inbox and never reached our bounce processor. Suppression therefore only ever learned about *synchronous* rejections, so we would keep sending to addresses already reported dead — the clearest spammer signature there is. Same built-but-never-wired pattern as the rest of this audit: `services/dns` already **generates and verifies** the `bounce.<domain>` CNAME this depends on, and even exposes `returnPathDomain`. Nothing used it. | HIGH | 2026-07-14 | **FIXED 2026-07-28** — `services/mta/src/bounce/return-path.ts`; MAIL FROM is now `bounces+<emailId>@bounce.<sender-domain>` on both relay and direct-MX paths (the `From:` header is untouched — only the envelope changes). **Per-customer bounce domain, not one shared `bounce.alecrae.com`**, and that choice is load-bearing: `bounce.customer.com` shares an organizational domain with `customer.com`, so SPF aligns under DMARC's relaxed policy *on top of* existing DKIM alignment; a single shared domain would have broken SPF alignment for every customer. Falls back to `From:` (with a warning that bounces won't be captured) when no bounce domain resolves — an envelope sender is mandatory. Inbound `dsn-suppression.ts` upgraded from its documented "most recent message to that address" heuristic (wrong for a recipient mailed from several of an account's domains) to exact attribution via the VERP email id, keeping the heuristic only for pre-VERP mail. 14 tests. |
| 128 | **The outbound MTA shipped an SMTP receiver it was never meant to have — the root cause behind #105/#127.** `services/mta` is the OUTBOUND service (BullMQ consumer, mail-plan Phase 1); the port-25 listener is `services/inbound` (Phase 2), which already has the complete pipeline including its own relay control ("Relay not permitted for domain"), MIME parsing, DKIM verification, routing and Postgres storage. The outbound service nonetheless bound port 25 **by default**, with no relay control and a `message` handler that logged an envelope and discarded it. It duplicated an existing service, incompletely, and that duplicate is what was abused for 9 days. `docs/infra/mta-box-setup.md` instructed operators to deploy exactly this. | HIGH | 2026-07-29 | **FIXED 2026-07-29** — receiver is now opt-in via `MTA_ENABLE_SMTP_RECEIVER`, **default off**: the outbound service opens no listening socket at all. Removing the attack surface beats guarding it; #127's relay control stays as defence in depth for anyone who enables it, and enabling it logs a warning saying why they shouldn't. The message handler now states plainly that it is discarding the message and points at `services/inbound` — a "in production this would route to…" comment on code that silently drops mail is how this went unnoticed. Runbook corrected. |
| 130 | **Unbounded AI spend on the mail-ingestion path.** `storeReceivedEmail()` runs once per MESSAGE — every message of an initial Gmail/Outlook sync, and every message of an MBOX/EML import — and fired a Claude call per message with **no AI-quota check and no backfill guard**. The per-account quota (issue #102) is enforced by middleware on HTTP routes; this is an internal path no middleware sees, so triage spent against a plan's allowance without checking it *or recording it* (the counter gating every other AI feature never moved). A customer connecting a 50,000-message mailbox meant 50,000 unbounded Claude calls, invisible until the bill. Found while scoping the Phase-3 dispatcher — and it is precisely why that dispatcher must not simply fan out more per-message AI calls. | HIGH | 2026-07-29 | **FIXED 2026-07-29** — `ReceivedEmailInput.backfill` marks historical mail and skips per-email AI work, threaded from the real discriminators (Gmail's full-list path only runs with no `syncState`; Outlook's on a missing deltaLink; `parsedToReceived` sets it unconditionally since an archive file is historical by definition). Incremental sync — mail actually arriving — still triages. Quota checked before the call and incremented after, failing open when unenforceable (a Redis outage must not stop triage; the backfill guard still bounds spend in that state). 7 tests covering both directions. |
| 131 | **Five more routers mounted with `app.route()` and NO `authMiddleware` — every request 401'd, for every caller including owners.** `requireScope()` reads `c.get("auth")`, which only exists if authMiddleware ran. `/v1/contact-groups`, `/v1/signatures`, `/v1/auto-responder`, `/v1/push`, and `/v1/changelog`'s admin writes had none. Identical to issue #87, repeated five times. **Contradicts standing claims:** the 2026-07-22 journey audit lists Signatures as "real CRUD" and says Auto-Responder's "config genuinely saves" (neither could), and CLAUDE.md records push as wired in tranche 5 while issue #77 documents behaviour of `/v1/push` endpoints that in fact 401'd for everyone. Only the contact-groups instance had ever been spotted. **Every affected router's unit tests pass** — they mock `requireScope`, so no per-route test can see a wiring defect between server.ts and the router. | HIGH | 2026-07-29 | **FIXED 2026-07-29** — middleware added for all five; changelog scoped by method (`app.on([POST,PUT,DELETE], …)`) because its GETs are legitimately public. New `route-auth-coverage.test.ts` is a **structural** test over server.ts source: any future router mounted without auth fails the build unless added to an `INTENTIONALLY_PUBLIC` list with a stated reason. Its rule accepts auth prefixes sitting *beneath* a mount, so mixed routers like `/v1/billing` (per-endpoint auth + Stripe-signed public webhook) aren't false positives. |
| 132 | **The journey audit's headline estimate is wrong: "fix this once, well, and eight of these features become real in the same afternoon" is not achievable as written.** That estimate assumed the eleven "runs automatically on new mail" features expose callable handlers. They do not — `productivity-analytics.ts`, `attachment-intelligence.ts`, `scheduling-intelligence.ts`, `knowledge-graph.ts` and `sentiment-timeline.ts` have **55 route handlers and zero exported functions** between them; every one is logic embedded in a route body. There is also **no per-account feature-enablement store anywhere**, so "which automations does this account want" cannot currently be answered. Real shape of the work: (a) extract each feature's logic from its route body into a callable function, with tests, one refactor per feature; (b) a new opt-in table + UI — **a schema migration, which Boss Rule #7 reserves for Craig**, and a product decision about defaults and plan-gating; (c) the dispatcher itself, which per issue #130 must be built around cost (per-account opt-in, quota-aware, backfill-excluded) rather than fanning out per-message AI calls. | MEDIUM | 2026-07-29 | OPEN — **needs Craig**: the opt-in schema + policy is a product/billing decision, not an audit fix. Deliberately not built unilaterally. The cost guard it depends on (#130) is done. |
| 133 | **The Sent page's list request was rejected outright, and its "Opened" badge could never be true.** `ListMessagesQuery`'s status enum had drifted from the DB's `email_status`: it accepted `"sending"` (not a DB value — matched nothing) while **rejecting `"sent"`**, the status the send path writes and the one the page filters on, so `GET /v1/messages?status=sent` **422'd on every load**. Separately the badge read `tags.includes("opened")`, a tag nothing has ever written — the tracking pixel records an `events` row of type `email.opened` instead — so a genuinely-opened message showed "Not opened" forever. The 2026-07-22 audit rated this journey "partial — real list/send". | HIGH | 2026-07-29 | **FIXED 2026-07-29** — enum now mirrors the DB exactly, with a test walking every value the database can hold plus one asserting a non-DB value is refused. `GET /v1/messages` returns a real `openedAt` from the events (one extra bounded query per page, not a join on the hot list query, and **not** a denormalised tag that could drift from the events that are the source of truth); wrapped so open data can never take down the inbox. **Method note recorded in the audit:** that pass traced response *shapes* but never checked whether the *request* was accepted, which is why a 422 on a page's primary query went unnoticed. |
| 134 | **`lib/api-features.ts` held ten calls to nonexistent endpoints that `.catch()`d into hardcoded, plausible-looking data** — a security score of 85/"B+" with 3 phishing blocked, an inbox-hygiene score of 72 with 47 unread, and more. Anything importing them would have shown a user invented numbers as their own data, with no error and no way to tell. Issue #73(i) recorded this for `securityApi.score()` as a single instance; it was one of ten in the same file. Found by a systematic sweep: every `/v1` path the frontend references (254) diffed against every concrete backend route (753 — a naive grep finds only 183, because most registrations span lines). | MEDIUM (dead code today, landmine) | 2026-07-29 | **FIXED 2026-07-29** — all seven affected API surfaces were **dead** (live pages import dedicated modules that shadowed them; verified per symbol and by typecheck). Deleted rather than repaired: there is no caller to repair for, and leaving fabricating helpers around is how they get imported later. Swept every other `lib/api*.ts` for the pattern — zero. |
| 135 | **AI quota was charged for reads that make no AI call, across ~20 routers.** `requireAiQuota` is mounted on path WILDCARDS (`/v1/ai/categorize/*`, `/v1/sentiment/*`, `/v1/agent/*`, `/v1/programs/*`, `/v1/voice-clone/*`, `/v1/meetings/rooms/*`, …) which sweep in every endpoint beneath them, including plain DB reads. On `/v1/ai/categorize` alone, **10 of 12 endpoints charged quota while only 2 call Claude** — listing your smart rules or opening the stats tab drew down the monthly allowance. Enough of those and a paying customer is locked out of AI they never used. Same class as the `/v1/translate` instance fixed earlier in this audit; found here by enumerating every endpoint on the router rather than by hitting the symptom. | HIGH | 2026-07-29 | **FIXED 2026-07-29** — middleware skips GET/HEAD, after verifying that **no GET endpoint in `apps/api/src/routes` makes an AI call** (every AI-calling route is a POST). Keyed on method, not a path list, so a read endpoint added under an existing wildcard later inherits correct behaviour rather than the bug — this fixes all ~20 routers at once instead of re-auditing each by hand. A test also pins that an exhausted quota still serves GETs: locking a user out of data they already produced would be worse than the over-charging. Quota now mounted on exactly the two endpoints that spend a Claude call. **Three dead mounts corrected in passing:** `/smart-labels` (router registers `/smart-rules` — never matched, so smart-rule writes got the read-tier limiter), `/batch` (real path is the doubled `/v1/ai/categorize/categorize/batch`, which is what the web app calls), and `/feedback` (no AI call, no longer charged). |
| 154 | **The mailto unsubscribe has never worked, and reported success anyway.** Split out of #151. `routes/unsubscribe.ts` enqueued `{ from, to, cc, bcc, subject, text, kind }` directly, but the MTA worker reads `job.data.email` — an `EmailJobData` wrapping a `QueuedEmail` with `rawMessage`/`accountId`/`domain`/`messageId`. There was no `email` key at all, so every job threw on first property access and retried to the DLQ. Nothing was ever delivered — and because `sendUnsubscribeMailto` treats "the callback did not throw" as success, the `unsubscribe_history` row and the UI both reported the unsubscribe as **done**. A user believes they are off a list they are still on, which is worse than a visible failure (same class as #141's fabricated scan verdict). Two problems sit behind the shape: `from` was caller-supplied and never checked against a domain the account owns — a spoofing vector the moment the job shape is corrected — and the send needs an `accountId` + verified domain to clear the pre-send gate, neither of which `executeOption()` currently receives. | MEDIUM | 2026-08-04 | **PARTIALLY FIXED 2026-08-04** — the branch now fails honestly ("not available yet — nothing was sent") instead of claiming a success that never happened, and `unsubscribe.ts` is no longer a queue producer at all. Deliberately not half-built beyond that: a real fix threads account + verified-domain context into `executeOption`, builds an RFC-5322 message, validates `from` against owned domains, and runs `runPreSendGate`. The one-click POST and browser-agent unsubscribe paths are unaffected and still work. |
| 155 | **The migration journal was out of order — the next production `db:migrate` would have silently skipped migrations 0008, 0009 and 0010.** Drizzle's migrator applies a migration only when the DB's MAX recorded `created_at` is *less than* the migration's `when`, and tracks nothing per-migration. Hand-written `0006` carried `when: 1784563200000`, **larger** than generated `0007`–`0010` (all `…4875…`/`…5221…`/`…5223…`). Production migrated "through 0007", so its max stored value is 0006's. The next deploy — imminent for business email, and `box-deploy.sh` runs `db:migrate` every time — would apply only 0011 and permanently skip: **0008** the `emails_account_message_id_idx` dedup unique index (silently reverting #107's TOCTOU fix); **0009** the `provider_message_id` column (so every Gmail/Outlook sync insert fails on an undefined column — the #79 empty-mailbox outage returns); **0010** `stripe_webhook_events` (every Stripe webhook throws). `check-schema-drift.sh` diffs schema against the snapshot and never checks journal monotonicity, so CI stayed green and nothing made a sound. | CRITICAL | 2026-08-05 | **FIXED 2026-08-05** — 0008/0009/0010 `when` values bumped just above 0006's (`…5633/5634/5635…`), preserving their relative order. 0007 grandfathered (its SQL — `ALTER TYPE … ADD VALUE` without `IF NOT EXISTS` — is not idempotent, and every DB that applied 0006 applied 0007 in the same run, so none is stranded). New `packages/db/tests/migration-journal.test.ts` fails the build on any future non-monotonic entry — the guard the schema-drift check never had. 2 tests. |
| 156 | **Remote code execution in the API process, reachable by anyone who can register.** `POST /v1/scripts/:id/test` ran user-authored code via `new Function(...)` (`services/ai-engine/src/scripts/snippet-runner.ts`). Its "sandbox" shadowed `Function`/`eval`/`process`/`fetch` as `undefined` parameters — which stops nothing, because `(()=>{}).constructor("return process.env")()` reaches the real Function constructor through any function literal's prototype. `scripts:*` is owner/admin-only, but `POST /v1/auth/register` makes **every** new user an owner, so: register → create script → test it → the response hands back `JWT_SECRET` (cross-tenant account takeover + forges #145's tracking HMACs), `DATABASE_URL`, `ANTHROPIC_API_KEY`, `STRIPE_*`. Second, independent failure: a synchronous `while(true){}` blocks the single Bun event loop — one request downs the API for every tenant, because a `Promise.race` timeout can't interrupt synchronous code. The irony: `services/ai-engine/src/programs/runtime.ts` is a real QuickJS-WASM sandbox used by `/v1/programs`; scripts wired the unsafe twin. Issue #76(i) recorded the programs/scripts overlap as a *conceptual* duplication and never registered that one is an in-process escape. | CRITICAL | 2026-08-05 | **FIXED 2026-08-05** — the endpoint returns 501 (after the ownership check, so a 404 still means what it meant), the unsafe executor is no longer imported by `apps/api`, and the runner's own header is rewritten from a false security-model description to a "⚠️ NOT A SANDBOX — do not wire `runSnippet` to any route" warning pointing at the QuickJS runtime as the correct home. `route-auth-coverage`-style structural test (`scripts-execution-disabled.test.ts`) fails the build if any `apps/api/src` file imports or calls `runSnippet` again. A real fix ports the endpoint onto the QuickJS-WASM runtime — a deliberate build, not a patch on an escape-prone boundary. **Adjacent, NOT fixed, worth Craig's eye:** `register` granting `owner` to every signup is the privilege half of this; it deserves its own review. |
| 157 | **The pre-send compliance gate hardcoded `hasUnsubscribeLink: false` and `hasPhysicalAddress: false`, so every marketing/templated send was unsatisfiable — squarely on the business-email path.** `#152` fixed the *classification* (ordinary mail is no longer treated as marketing), but the marketing branch itself was still impossible to pass: `pre-send-gate.ts` passed those two fields as literal `false` regardless of message content, so CAN-SPAM raised missing-unsubscribe + missing-address on every genuine campaign even when the body contained a perfect unsubscribe link and postal address. A platform sending a templated newsletter got a 422 listing violations no configuration or content could clear. (The GDPR/CASL consent violation behind it is real — no consent-recording feature exists yet — and correctly still refuses; that is a separate, honest gap, not this bug.) | HIGH | 2026-08-05 | **FIXED 2026-08-05** — new `detectUnsubscribeLink()` / `detectPhysicalAddress()` read the actual `text`/`html` body (URL, mailto, and anchor-text forms of unsubscribe; PO-box and street-address patterns), erring toward detection so a sender who genuinely included the mechanism passes. `hasUnsubscribeHeader` set true because `buildRawMessage` emits the RFC 8058 header on every message it assembles. Tests pin both directions and assert a compliant marketing body clears the *content* rules while the consent violation still refuses — so this cannot decay into a blanket bypass. 3 new tests (14 total in the suite, still run against the REAL ComplianceEngine). |
| 158 | **The outbound virus scanner reported `clean: true` for every file it never scanned, and it is live in the send path.** `services/security/src/virus-scanner.ts` returned `clean: true, detections: 0` on all four non-scan paths (no API key, file >32MB, API error, poll timeout), and `messages.ts` gates sends on it. `VIRUSTOTAL_API_KEY` is set nowhere (not in the env template, `lib/env.ts`, or preflight), so in production the scanner scans nothing and passes everything while looking like a real control — the fabricated-clean-verdict class of #141. The test suite *pinned the fail-open as correct*, the #152 mock-blindness shape again. **Two corrections to the record:** #141's note that a real scanner "needs a new dependency" is wrong (this is a complete VirusTotal client over plain `fetch`), and #146 should not list `services/security` as unwired — it has a live production importer. | HIGH | 2026-08-05 | **FIXED 2026-08-05** — the four non-scan paths now return `clean: false` with their truthful `status` (`skipped`/`error`/`pending`); a clean verdict requires an actual scan (`status === "clean"`). `isSafe()` still fails **open** (blocks only on detected threats — failing closed would refuse every attachment with no key configured), but its contract is now documented as "block-or-not, NOT a clean verdict", and the send path is unchanged. Tests rewritten to assert unscanned ≠ clean while the send still proceeds. **Still Craig's call (Boss Rules #8/#10):** `uploadAndPoll` sends customer attachments to VirusTotal, whose free tier shares samples — a confidentiality/GDPR decision, and whether to configure the key at all. |
| 159 | **Warm-up adaptive safety is inert across the board — the ramp advances blind and two of three send paths ignore the hard pause.** Three related gaps found by importer-mapping `services/reputation`: (a) `warmup/monitor.ts`'s `runHealthCheckCycle()` (its own comment says "call periodically") has **zero callers** — it is the only writer of `bounceRate24h`/`complaintRate24h`/`totalBounced`, so the >0.1% complaint and >10% bounce auto-pause gates can never fire and `maybeAdvanceAutoStep` gates on a bounce rate permanently pinned at 0: **the ramp always steps up on schedule regardless of a domain bouncing 40%.** This contradicts #82/#138's "the pause machinery is real and drives the schedule." (b) The reputation hard-pause (`warmupSessions.status = "paused"`) is read only on `routes/messages.ts`'s path; `lib/agent-send.ts` and the MTA's own `warmup-gate.ts` don't consult it, so a hard-paused domain keeps sending via the other two producers. (c) The API-side counter is a stale-read lost-update race (check and increment several awaits apart, no atomicity — unlike the MTA gate's documented INCR-then-compare) and counts *messages* not *recipients*, so one `POST` with 500 addresses consumes 1 of a day-1 cap of ~20. | HIGH | 2026-08-05 | **FIXED 2026-08-05 (all three).** **(a)** `runHealthCheckCycle()` now runs hourly from `server.ts`, the cadence its own doc comment specifies and the one its 24h windows assume — so `adjustSchedule` actually writes bounce/complaint rates and the auto-pause gates can fire. It reads what the DB already has (delivery results + FBL rows), so it works today and gets sharper when #82 c/d credentials land; it is not blocked on them. **(b)** The warm-up + hard-pause check moved out of `routes/messages.ts` into `runPreSendGate`, so **every** producer inherits it — the #151 shape, one path later. Placed **last** in the gate deliberately: it is the only check with a side effect (it enrols a domain), so a message refused by any earlier control must never reach it; a test pins that a quota refusal creates no session, and another pins the 429 body verbatim so no API contract moved. `agent-send.ts` now also **records** against the ramp, since a check with no matching record would let agent volume go unseen. **(c)** `recordSend(domainId, count)` takes RECIPIENTS, not messages (both call sites pass the real recipient count — ISP caps are per-recipient, and one API call can carry hundreds), and the increment moved into SQL (`sentToday + n`) so concurrent sends compose instead of clobbering each other's read-then-write. 6 tests, incl. one asserting the payload is a SQL expression and not a pre-computed number — the property a tidy-looking "simplification" would undo. |
| 160 | **DKIM private keys are stored in plaintext under two comments claiming they are encrypted.** `packages/db/src/schema/domains.ts` (`// Encrypted at rest`) and `services/dns/src/auto-config.ts` both claim encryption; `auto-config.ts` inserts raw PEM and `services/mta/src/worker.ts` reads and signs with it directly. Production Postgres is local with no TDE and no app-level crypto on this column. A backup or disk leak hands out signing keys for **every customer domain** — an attacker can then send DKIM-valid, DMARC-passing mail as any customer. OAuth tokens got AES-256-GCM in #80; the strictly more dangerous secret in the same database did not. | HIGH | 2026-08-05 | **FIXED 2026-08-05** — new `packages/crypto/src/secret-box.ts` is the **single** implementation of at-rest secret encryption; `services/dns` seals the key on write (both the create and the rotate path) and `services/mta` opens it in the signer. It lives in `@alecrae/crypto` rather than `apps/api` because neither service can import from the API — and `apps/api/src/lib/token-crypto.ts` (#80's OAuth tokens) is now a thin re-export of it rather than a second copy, since a duplicated crypto boundary is exactly how #124's header fix ended up applied to one of two builders. **Failure direction is the load-bearing choice:** the worker uses `openSecretSafe`, which returns null rather than throwing, so a key that cannot be decrypted is treated as a key we do not have — routing into #144's **hold, don't send unsigned** branch instead of failing the job into the DLQ. Legacy plaintext rows pass through unchanged and self-heal on next write, so **no migration** (Boss Rule #7). Both misleading "Encrypted at rest" comments replaced with what the code does plus a write-only-through-sealSecret instruction. 11 tests covering the compatibility contract and both failure directions. **Rotation caveat, documented in the module:** rotating `JWT_SECRET` makes stored keys unopenable — affected domains then hold mail until keys are regenerated and DNS republished (nothing leaks; mail stops). Same class as #115c's OAuth warning. |
| 161 | **The "Dependency Audit" CI job has never audited anything, and four SOC 2 documents attest that it does.** `.github/workflows/security.yml` ran `npx audit-ci --config audit-ci.json \|\| true`. audit-ci resolves its package manager from `package-lock.json`/`yarn.lock`/`pnpm-lock.yaml` — this is a Bun workspace with `bun.lock` and none of those exist, so **every run died on "Cannot establish package-manager type"** and `\|\| true` turned the error into a pass. The OSV-Scanner step beside it also ended in `\|\| true` with its output discarded, which is indistinguishable from a scanner that never ran. The step's own comment ("Bun does not have a built-in audit command yet") was stale — `bun audit` works, and running it revealed **155 advisories: 3 critical, 72 high, 68 moderate, 12 low**, none of which anything had ever reported. Worse than a missing control, because `docs/compliance/soc2/controls-matrix.md` recorded it **Implemented** with a "moderate severity threshold", and three more SOC 2 policy documents repeated the claim — an attestation to auditors about a control that audited nothing. Same class as #150 (preflight that could not run) and #143 (e2e suite that executes nowhere). | HIGH | 2026-08-05 | **FIXED 2026-08-05** — new `scripts/check-dependency-audit.ts` runs `bun audit` against the real lockfile and **fails the build on any unreviewed CRITICAL**. The three current criticals (`tar`, `shell-quote`, `vitest`) are allowlisted **individually with a written reason each**, verified against `bun audit`'s own dependency paths: all three are build/dev-chain only (expo, electron-builder, pulumi, drizzle-kit, react-native, the test runner) and no `apps/api` or `services/mta` request path reaches them; the vitest advisory additionally requires a UI server neither CI nor production starts. Removing a package from that list is how it becomes blocking again, so an accepted risk must be re-stated deliberately rather than inherited. **High/moderate/low are reported, not blocking, and that is deliberate:** there are ~140, nearly all transitive dev tooling, and a permanently-red gate is one nobody reads — it would bury the criticals this exists to surface. Counts print on every run so the number cannot quietly grow. OSV-Scanner output now goes to the job summary instead of being discarded. `audit-ci.json` deleted. **All four SOC 2 documents corrected**, with the controls matrix stating plainly that any prior evidence period should be treated as unscanned. |
| 162 | **Quoted-printable attachments were corrupted on every MBOX/EML import, and three package manifests pointed `start` at a `dist` their build never produces.** (a) `packages/email-parser/src/parser.ts`'s `decodeTransferEncoding` ran the QP branch through `new TextEncoder().encode(decodeQuotedPrintable(body))` — a double encode. `decodeQuotedPrintable` returns one code unit per byte, so re-encoding it as UTF-8 turns **every byte >= 0x80 into two**: a 12-byte PDF fragment came out 15 bytes and structurally broken, silently, with no error. Verified by running both paths side by side. This is the attachment half of issue #113(a) — the correct helper, `quotedPrintableToBytes`, was written for that very fix and left unused 30 lines below the broken call. The 7bit/8bit branch had the same defect. (b) `apps/api`, `services/collab` and `services/jmap` paired an `echo` build script with `start: bun run dist/...`, so `bun run start` fails outright, and `services/sentinel`'s `main` pointed at a `dist` nothing emits — the #153 defect class, still present in the API itself. | MEDIUM | 2026-08-05 | **FIXED 2026-08-05** — (a) both branches now produce faithful bytes (`quotedPrintableToBytes` for QP, latin1 for 7bit/8bit, since those carry raw octets and TextEncoder would corrupt 8bit bodies the same way). Regression test asserts the exact 12 bytes of a QP PDF header including `=FF`, a byte no UTF-8 encoding can produce — proven to distinguish the old path, which yielded 15. (b) All four manifests now run from source under Bun, matching what production's systemd units actually execute (`bun run .../src/index.ts`) and the pattern #153 established for `services/inbound`. |
| 163 | **Batch categorisation invented a category from the character sum of the email id and stored it as Claude output.** `POST /v1/ai-categorization/categorize/batch`, for any id it had no record of, computed `hash % PRIMARY_CATEGORIES.length` and persisted the result with `aiModel: "haiku"` and `confidence: 0.5` — a category with no relationship to any message, or to any model, attributed to one. Those rows were then averaged into the categorisation-accuracy figure the stats tab reports, so the accuracy metric was partly measuring a hash of id strings. Same fabricated-output class as #141 (invented virus verdicts) and #137 (placeholder text presented as AI). It also quietly swallowed a cross-tenant probe: another account's email id came back with a confident-looking category instead of being ignored. | MEDIUM | 2026-08-05 | **FIXED 2026-08-05** — unknown ids are **skipped and returned in a new `skipped` array** rather than invented, so a caller that asked for 50 and got 40 knows which ten and why (silently dropping them would trade one dishonesty for another). Nothing is persisted for an email the account does not own. |
| 55 | MTA + inbound services not running; mail DNS incomplete | HIGH | 2026-06-19 | SUPERSEDED — full current state + phased fix in `docs/infra/multi-platform-mail-plan.md` |
| 58 | `services/imap` TCP listener not bootable | HIGH | 2026-07-01 | DECIDED 2026-07-01 (Craig): OAuth-only for launch — not blocking |
| 59 | `.env.production`/`.env.test` old contents still in git history (untracked 2026-07-01 but not scrubbed) — rotate any values that were ever real | HIGH | 2026-07-01 | OPEN (rotation unconfirmed) |
| 61 | Inline markdown parsing unimplemented in `packages/email-parser/src/document-model.ts:575` (bold/italic/link/code) | LOW | 2026-07-01 | OPEN |
| 62 | MTA `/metrics` returns placeholder 404 instead of OTel export (`services/mta/src/health.ts:207`) | LOW | 2026-07-01 | OPEN |
| 66 | **main failed typecheck for 9 days** (2026-07-04 → 07-13) — 3 errors introduced by audit commit 0947895: plan-gate.tsx `PlanBadge` missing business/business_plus tiers, dns auto-config.ts unchecked regex captures, agent.ts missing `sql` import + phantom `draft.accountId`. | HIGH | 2026-07-13 | FIXED 2026-07-13 (all three) — **follow-up FIXED 2026-07-20:** verified `ci.yml` ran on every push to main but **main had zero GitHub branch protection** (`gh api repos/.../branches/main/protection` → 404) — CI going red was purely informational, nothing ever blocked a bad commit from landing, which is exactly how main stayed broken for 9 days. Enabled branch protection on main (Craig-authorized 2026-07-20): PR required to merge + 4 required status checks (Lint, GateTest Quality Gate, Schema/Migration Drift, Route Coverage) must pass, `strict: true` (branch must be up to date). `enforce_admins` deliberately left `false` (Craig's call) so admins retain an emergency direct-push escape hatch. **Workflow change going forward: direct `git push origin main` no longer works for non-admins — normal flow is now branch → push → PR → CI green → merge.** |
| 67 | Sidebar "Delegation" item links to `/shared-inboxes` (label/page mismatch); delegation route only 33% wired | MEDIUM | 2026-07-13 | FIXED 2026-07-13 — dedicated `/delegation` page (all 12 endpoints incl. shared drafts), sidebar corrected. Bonus root-cause: old `delegationsApi` called `/v1/delegations` (plural) while the mount is singular — every call 404'd; fixed in api.ts. |
| 77 | **Backend findings from wiring tranche 5** (report-only unless noted): (a) SSO config required issuer `alecrae-sso` token → admin sessions 403'd — **FIXED same day** (authMiddleware+requireAdmin on /v1/sso/config); (b) encryption:read/write missing from scopes → all encryption 403'd — **FIXED same day**; (c) push `/test` is a stub (marks queued, never delivers; no VAPID key exists server-side → browser subscribe degrades gracefully); (d) encryption `/keys/generate` generates keypairs SERVER-side with a weak no-KDF passphrase-to-AES-key, breaking the zero-knowledge claim — UI does correct client-side keygen + IndexedDB and ignores the server's wrapped copy, but the endpoint should accept a client-supplied public key instead; (e) SSO `/config` PUT requires re-pasting the full certificate on any edit (no partial update); (f) push `/subscribe` re-parents a subscription by endpoint without userId scoping | MEDIUM | 2026-07-14 | (a)(b) FIXED; rest OPEN (backend-hardening pass) |
| 76 | **Backend findings from wiring tranche 4** (report-only unless noted): (a) `grammar:read` scope missing from session tokens broke spellcheck AND pre-existing grammar-check — **FIXED same day**; (b) messages list exposes no threadId, so thread-mute UI falls back to message.id — mutes won't span a thread's other messages until the list returns a real thread/conversation id; (c) TWO competing label systems: `bulk/label` writes a `label:<id>` string into emails.tags while `labels/:id/apply` writes the email_labels join table — nothing reconciles them; (d) bulk read/unread write tags but inbox derives read-state from status, so a reload may not reflect bulk read; (e) signatures had TWO stores (old localStorage widget in settings + new DB API) — **reconciled same day** (settings now links to the DB-backed /settings/signatures); (f) signatures context auto-switch is schema-only (stored, never consumed); (g) programs `/:id/test` dry-runs inflate real runCount/errorCount; (h) recall has no "list recalls" endpoint (only reachable right after a send); (i) programs (B1) overlaps the existing /scripts page conceptually — both are programmable email, worth reconciling which is canonical | MEDIUM | 2026-07-14 | (a)(e) FIXED. **(b) FIXED 2026-07-29** — the list exposed no threading at all, so the inbox used the **message** id as the thread id (`mutedIds.has(email.id)` against a set of thread ids) *and* sent message ids to the thread-mute endpoint: muting a thread muted exactly one message and every reply arrived unmuted. New `lib/thread-key.ts` derives a stable key from the RFC 5322 headers already persisted — `references[0] ?? inReplyTo ?? own messageId` — so no migration is needed (Boss Rule #7). Every message in a chain resolves to the same value **including the root**, which is the case a naive version gets wrong; angle brackets are stripped because clients disagree about them and a mixed chain would otherwise split partway through. Exposed on list and detail from one helper so they cannot diverge; muting de-duplicates, since three messages from one thread are one mute. 9 tests. **(d) was ALREADY FIXED and wrongly still listed open** — issue #88's consolidation moved bulk read/unread onto the real `is_read` column; verified 2026-07-29. **(g) FIXED 2026-07-29** — `/:id/test` returned `dryRun: true` while inserting a `program_runs` row and incrementing the program's real `runCount`/`errorCount`, so testing twenty times reported twenty executions and a deliberately-broken test script drove `errorCount` up indistinguishably from real failures: the statistics a user consults to judge a program were made of their own experiments. A dry run now leaves no trace — **not** written-and-tagged, because `program_runs` has no dry-run column and adding one is a migration, and an untagged test row would move the same lie one level down into the run history. Nothing is lost; the full result is already in the response. **(c) FIXED 2026-07-29 — and it was bigger than "competing".** BOTH systems were **write-only**: `bulk/label` wrote `label:<id>` into `emails.tags`, `labels/:id/apply` wrote `email_labels` rows, and **no endpoint anywhere returned an email's labels** (`emailId` appears in labels.ts only in apply/remove; `GET /v1/messages` returned none; the UI type has a `labels` field nothing populates or renders). A user could create a label, apply it from either surface, and never see it again — so unifying alone would have merged two sinks into one sink. `email_labels` wins: real relational model, FK cascade on label deletion, already backs the labels CRUD. A magic string in a tags array has none of that — deleting a label would have orphaned `label:<id>` strings on every message forever with nothing able to resolve them to a name. `GET /v1/messages` now returns labels with name and colour via one bounded per-page query, wrapped so label data cannot take down the inbox. **Cross-tenant write found in passing:** `labels/:id/apply` checked the *label* belonged to the caller but never the *emails*, so anyone could attach their label to a guessed message id in another account — both endpoints now resolve ids through the caller's own account and report what was actually labelled rather than echoing the request. **Still OPEN:** (f) signatures context auto-switch stored but never consumed, (h) no "list recalls" endpoint, (i) programs/scripts overlap. |
| 75 | **Backend findings from wiring tranche 3** (report-only unless noted): (a) productivity `/leaderboard` aggregated across ALL accounts, leaking tenant IDs + volumes — **FIXED same day** (scoped to caller's account; a real cross-member leaderboard needs a per-user tracking dimension that doesn't exist); (b) sentiment `/contacts` + `/timeline` cursor pagination unstable (cursors on id while ordering by healthScore/createdAt); (c) sentiment `/trends` passes date_trunc first arg as a bound param (may error depending on driver); (d) scheduling `/propose` meetingType is dead logic (both ternary branches return one_on_one), `/conflicts` filters start-only by createdAt not selectedTime; (e) attachment analyze/scan use Math.random() virus results + regex-only PII (stubs per #29); (f) MANY route-file header comments document wrong mount paths (attachment-intelligence, productivity-analytics, scheduling-intelligence, sentiment-timeline all lie — real mounts verified in server.ts); (g) old hygiene page was calling an entirely fictional `/v1/email-hygiene/*` surface | MEDIUM | 2026-07-14 | (a) FIXED. **(b) FIXED 2026-07-29** — `/timeline` ordered by `createdAt` and `/contacts` by `healthScore`/`totalInteractions`/`updatedAt`, both paginating on `id`: pages skipped and repeated rows while the response stayed well-formed, so nothing surfaced as an error. Both moved onto `lib/keyset-cursor.ts` (built for #74g), which is why each is a few lines rather than a reimplementation. The tiebreaker matters more here than anywhere — health scores are integers 0-100 and interaction counts are small, so dozens of contacts share a value and a sort-value-only cursor would drop or duplicate all of them at the boundary. **(c) FIXED 2026-07-29 — and the original framing was too gentle.** It was filed as "may error depending on driver"; it is not driver-dependent. `date_trunc(${truncFn}, …)` bound `truncFn` as a **parameter** and the expression appeared three times (SELECT, GROUP BY, ORDER BY), so each got its own placeholder and Postgres saw three textually different expressions — the query is invalid and **the endpoint could never have returned a row**. Now built once and reused so all three render identically by construction; `sql.raw` is used for the trunc unit and only there, since it derives from a validated three-value enum and can never carry caller input. **(d) FIXED 2026-07-29 — both halves were worse than recorded.** `/conflicts` filtered `createdAt >= startDate`, i.e. when the *proposal* was created rather than when the *meeting* is, so "what clashes next week" missed any meeting scheduled for next week but agreed a month ago — precisely the one most likely to clash. And `endDate`, computed from `range` on the line above, **was never referenced**: the range parameter did nothing, every future accepted proposal came back regardless of the window. Now filtered on meeting time, by **overlap not containment** (a meeting starting before the window and running into it still occupies time in it), in JS rather than SQL because `selected_time` is a **TEXT** column — a SQL range comparison would be lexicographic and correct only if every stored value shared one ISO format. On the propose side, `detectSchedulingIntent("")` was called with an **empty string** and its result discarded into `_intent`, while `meetingType` came from a ternary with two identical branches — so the classifier never saw text and every proposal was a one-to-one. It now classifies on the referenced email's real subject and body, overrides to `group` on participant count (a signal the text cannot contradict), and uses the email's real subject instead of "Meeting with N participant(s)". Also removed `meetingType as "one_on_one"`, an assertion that would have written an invalid value into a Postgres enum the moment the classifier returned anything else. 7 tests. **Still OPEN:** (e) attachment `Math.random()` virus results — *superseded by issue #141, now fixed*, (f) lying route-header mount comments, (g) old hygiene page. **Four sites now share the keyset helper**: if a list orders by anything but its primary key, the cursor must carry that column plus a tiebreaker. |
| 74 | **Backend findings from wiring tranche 2** (report-only unless noted): (a) SQL injection in knowledge-graph `/graph` centerEntityId — **FIXED same day** (parameterized inArray); (b) cross-tenant dedup leaks in ai-intelligence priority-score + sentiment — **FIXED same day** (accountId filter); (c) ai-intelligence writing-coach + predictive-actions return Math.random() scores and PERSIST them; (d) context-intelligence confidence scores hard-coded not from extractor, promisor/promisee naive participants[0/1], batch-extract 25 serial Claude calls in one request; (e) notification-intelligence evaluate is NOT a dry run (mutates focus-session deferred count), senderVip/labels conditions accepted but never evaluated; (f) search-intelligence suggestions/trending/related still stubs (#29), nothing wrote search_history until **fixed same day** (ai-search records keyword+semantic); (g) knowledge-graph broken cursor pagination (orders by mentionCount, cursors by id), writeRateLimit applied to reads; (h) multiple route-file header comments still lie about mount paths | MEDIUM | 2026-07-14 | (a)(b) + search-history FIXED. **(g) cursor half FIXED 2026-07-29** — `/entities` ordered by `mentionCount`/`lastSeenAt` and `/relationships` by `strength`, while both filtered `id < cursor`. Those columns have no relationship, so every page excluded an essentially random subset of the ordering: rows skipped, rows repeated, and the response still well-formed so nothing surfaced as an error — the list just lied as the user scrolled. Replaced with real keyset pagination in new `lib/keyset-cursor.ts`: the cursor carries the sort value **plus an id tiebreaker**, and the ORDER BY tiebreaks too. The tiebreaker is load-bearing, not decoration — `mentionCount` and `strength` are heavily tied, and without it every row on the boundary value is dropped or duplicated. A malformed or legacy bare-id cursor decodes to null and serves page one rather than resuming from a position it cannot compute; cursors are base64url so they can never again be confused with a bare id, which is exactly what the bug was. Extracted to a shared module because this was the third site to need it. 8 tests. **Still OPEN in (g):** `writeRateLimit` applied to reads. Rest OPEN. |
| 73 | **Backend findings from wiring tranche 1** (report-only, from building the UIs): (a) video-meetings summarize stub PERSISTS placeholder summaries + fake confidence to DB; (b) `POST /rooms/:id/schedule` returns synthetic eventId, stores nothing; (c) meet join links point at `meet.alecrae.com` — no service serves that host; (d) room slug uniqueness is global across tenants (slug-existence oracle); (e) shared-drafts approve has no reviewer authorization + no reject/send transitions; (f) delegation `expiresAt` never enforced; (g) `GET /v1/delegation/inbox` not account-scoped; (h) sender-reputation `threatHistory` counts ALL account threats, not the queried sender's; (i) `securityApi.score()` in api-features.ts calls nonexistent `/score` endpoint and silently returns hardcoded fake data; (j) analytics goals never auto-sync from snapshots; (k) route-file header comments lie about mount paths (analytics-dashboard, delegation) — **FIXED 2026-07-29 across all SIX affected files** (also attachment-intelligence, productivity-analytics, scheduling-intelligence, ai-categorization). Not cosmetic: the delegation header is where issue #67's outage came from — the web client called the plural `/v1/delegations` it documented and every request 404'd. Now enforced by `tests/route-header-paths.test.ts`, which diffs each header against `server.ts`'s `app.route()` calls, so a moved mount fails the build instead of silently orphaning its documentation | MEDIUM | 2026-07-13 | Scope fix (messages:write + contacts:* baseline) shipped same day. **(e)(f) FIXED 2026-07-29 — both were authorization controls that existed on paper and enforced nothing.** (f) `expiresAt` was accepted, stored and echoed back, and compared against the clock **nowhere**: granting mail access "until Friday" granted it permanently, and the person who set the expiry had every reason to believe otherwise — silent over-permission, where nothing looks wrong so nobody looks. The delegate-inbox query now requires no expiry or a future one; **NULL deliberately stays open-ended**, since reading it as expired would revoke every dateless delegation ever granted, a worse outage than the bug. (e) shared-draft approve consulted the `reviewers` array nowhere, so anyone with `messages:write` could approve — **including the author approving their own draft**. Enforced where reviewers were named, which is the one unambiguous case; an empty list expresses no policy and inventing one would break drafts submitted without assignees (that is a product decision, not an authz detail). 9 tests, covering the null-expiry direction that would turn the fix into an outage if inverted. **(a) FIXED 2026-07-29** — it did not summarise anything: it WROTE a canned paragraph and two invented action items into `aiSummary`/`aiActionItems`, so the recording carried them permanently as if extracted from that meeting, then returned `confidence: 0.85` for content no model produced. Generic action items are worse than none — a user can act on them believing they came from the transcript. Cannot be made real here: `transcriptKey` is only ever read back, nothing writes it, and the storage client has no download method. Now 501, writing nothing, after the ownership check. **Still OPEN:** (b) synthetic eventId, (c) `meet.alecrae.com` unserved, (d) global slug uniqueness, (g) cross-account delegation inbox, (h) sender-reputation threat counts, (j) analytics goals never auto-sync, (k) lying header comments. |
| 68 | **Mail-blocking DNS gaps** — mx1/mx2/`_spf`/smtp records missing, bounce CNAME mistargeted, PTR on wrong hostname | HIGH | 2026-07-13 | FIXED 2026-07-13 — Craig executed the full Cloudflare changeset + Vultr PTR change (`smtp.alecrae.com`); all records + FCrDNS verified live. Mail plan Phase 0 COMPLETE; next is Phase 1 (MTA bring-up on 158). |
| 69 | 14 orphaned web components never imported by any page: CollaborativeDraftView, SpatialInboxView, MeetingTranscriptPanel, EmailQueryConsole, VoiceCloneManager, SignatureManager, TaskProviderSelector, EmailScriptManager, NewsletterSummaryPreview, FocusModeEmailCard, DragToSnooze, LocalAIStatusIndicator, + SwipeableEmailRow (mobile-only), others per audit | MEDIUM | 2026-07-13 | OPEN — wire or delete per DEVOPS_TRACKER §3 roadmap |
| 70 | No `email.received` webhook event — external platforms can't programmatically react to inbound mail (needed for the multi-platform mail story) | MEDIUM | 2026-07-13 | FIXED 2026-07-20 — `storeReceivedEmail()` (`lib/received-email-store.ts`, fires for every Gmail/Outlook sync + MBOX/EML import) now writes an `email.received` row to `events` and enqueues delivery via the existing `webhook-dispatcher.ts` (same HMAC-signed, retried-with-backoff mechanism `email.opened`/`email.clicked` already use). Required a migration (`0007_numerous_valkyrie.sql`, adds `email.received` to the `email_event_type` Postgres enum). **Bonus fix found in the same code path:** `WebhookEventType` in `types.ts` (the public "create a webhook" request schema) used bare names (`"opened"`, `"clicked"`) while `events.type` — what `webhook-dispatcher.ts` actually filters against — uses dotted names (`"email.opened"`). Any webhook that restricted itself to specific event types (rather than leaving the filter empty) could never match anything; nobody had hit it yet since the product predates real customers. Fixed to dotted names matching the DB enum everywhere (`types.ts` + the `/settings/developer` webhook UI). See issue #103 for a related, larger gap found while tracing this. |
| 71 | `status.alecrae.com` returns 503 — no status app deployed on Jarvis, no Traefik route | LOW | 2026-07-10 | OPEN |
| 72 | No staging environment + no alerting (outage ran 3 days unnoticed while fleet-check logged 503s) | HIGH | 2026-07-10 | OPEN — see `docs/postmortems/2026-07-07-alecrae-503-outage.md` + DEVOPS_TRACKER §1.7; staging design needs its own session |
| 78 | **No deploy-drift detection** — the box served a July-4 build for 10 days (unpushed on-box hotfix → `--ff-only` refused to pull) and nothing alerted. The drift itself was fixed 2026-07-14 (`git reset --hard origin/main` + redeploy). | HIGH | 2026-07-14 | **FIXED 2026-07-20, verified live** — `/health` + `/v1/health` report the running process's commit SHA (`apps/api/src/lib/deploy-info.ts`); `scripts/check-deploy-drift.sh` compares box HEAD to `origin/main` on a 15-min systemd timer and writes a status file the API reads back, flipping `/health` to `degraded` on drift. Setup: `docs/infra/deploy-drift-check.md`. Installed on Jarvis (Craig-authorized): first ran the full deploy ritual since the box was **31 commits behind, predating this session** (git pull, bun install, db:migrate incl. migration 0007, web rebuild, restart, health-checked), then the systemd timer. Found + fixed two real bugs during the live install: status file written `600 root:root` was unreadable by the `alecrae-api` service (different user) — `deployDrift` silently read `null`; and a shared fixed `/tmp` path for fetch-error capture broke across users. Both fixed (`write_status()` now `chmod 644`s, fetch-error capture uses `mktemp`). Confirmed working: `curl https://api.alecrae.com/v1/health` shows a populated `deployDrift`. **Also found:** only `root` has GitHub SSH access on Jarvis (the `alecrae` user `alecrae-api`/`alecrae-web` run as has none) — documented, timer runs as root. **Separately found (new, issue #104):** `/opt/alecrae/.env` line 21 (`RESEND_API_KEY`) has a stray space after `=`, which breaks POSIX-shell `source`/`.` — the exact fallback `box-deploy.sh`'s own docs recommend for setting `DATABASE_URL` manually. Bun's own dotenv loader tolerates it (services boot fine), so this was invisible until someone tried to source the file by hand. |
| 79 | Gmail/Outlook initial sync after OAuth connect is fire-and-forget — errors only go to console, account stays `active`, user sees a silently empty inbox. Surface sync status/last error on the connected account (UI + `lastSyncAt`/`lastError`). | MEDIUM | 2026-07-14 | FIXED 2026-07-19 — was actually worse than described: `sync/engine.ts` stored every message under `connected_accounts.id` instead of the workspace `accounts.id`, so every insert failed the NOT NULL FK on `emails.account_id` and **zero messages ever persisted**, silently, for every connected account ever. Fixed the accountId bug, added a `last_error` column + `lib/mailbox-sync-worker.ts` (`syncAndPersist` persists cursor/refreshed-tokens/errors; a 5-min interval sweep now re-syncs active accounts — nothing did before), and fixed the connected-account send path to refresh an expiring OAuth token instead of using a stale one (sends previously died ~1h after connect). |
| 80 | OAuth tokens in `connected_accounts` stored unencrypted (access + refresh). Encrypt at rest before real users connect mailboxes. | MEDIUM | 2026-07-14 | FIXED 2026-07-19 — AES-256-GCM via new `lib/token-crypto.ts` (key from JWT_SECRET, same pattern `meeting-link.ts` already used); IMAP/SMTP passwords covered too. Legacy plaintext rows transparently decrypt and self-heal to encrypted on next write — no data migration needed. |
| 81 | `OWNER_EMAILS` env not set on the box — founder allowlist runs on the hardcoded default (`ccantynz@gmail.com`) only. Mitigated 2026-07-14: `/me` + `switch-workspace` now reconcile the founder's plan for the ACTIVE workspace, and PlanGate honors the server-derived `isFounder` flag. | LOW | 2026-07-14 | NOTED |
| 83 | **`VAPRON_API_KEY` on the box uses the wrong key scheme** — box holds a `vpk_…` key but Vapron's deployed API only authenticates `Bearer btf_sk_…` (apiKeyAuthMiddleware ignores other prefixes → 401 UNAUTHORIZED, verified live against `dns.myZones.list`). This silently breaks EVERY Vapron integration in prod: transactional email, AI gateway, object storage, and the new DNS auto-config. Fix: Craig mints a `btf_sk_` API key in Vapron for the platform account that owns the DNS zones → replace `VAPRON_API_KEY` in `/opt/alecrae/.env` → `systemctl restart alecrae-api`. | HIGH | 2026-07-14 | **ROOT CAUSE WAS BIGGER THAN THE KEY PREFIX — fixed 2026-07-21.** Craig supplied working Vapron platform API docs + a live key (still `vpk_`-prefixed) directly authenticating against `POST https://vapron.ai/api/platform/email/send` with a plain `Bearer <key>` header and a plain-JSON request/response body — a completely different transport than what `lib/vapron.ts` had ever implemented (`https://api.vapron.ai/api/trpc/<procedure>` with a tRPC/superjson envelope, guessed against unpublished docs — see the file's original header comment, which said as much). That means email/AI/storage were never going to work regardless of key prefix; the two problems (wrong transport, and the box's wrong key scheme) were separate and this session only fixes the first. Rewrote `lib/vapron.ts` to use the real REST surface (new `restRequest()` helper, `getPlatformBaseUrl()`) for `email.send`, `ai.complete`, and a new `storage.getUploadUrl()` (which then fixed issue #29's presigned-upload stubs same-day). `dns.*` (zone/record management) and `storage.listBuckets/createBucket` are UNTOUCHED — no documentation for those was provided, so they stay on the original unverified tRPC transport rather than guessing again; if DNS auto-config is still broken, it needs its own investigation, separately from this fix. **Could not live-verify with the supplied key**: this sandbox's outbound `curl`/network calls carrying a live production secret are blocked by the permission classifier by design (network call + real credential = exactly what it exists to catch) — confirmed this is a hard stop, not something to route around. Live verification is now either (a) Craig running the same curl himself, or (b) deploying to the box, where `VAPRON_API_KEY` already lives as an env var and the corrected transport gets exercised for real on next deploy. Treat email/AI/storage as "code-correct against supplied docs, not yet proven against the live API" until one of those happens. |
| 82 | **Deliverability pre-send gate (2026-07-14 audit)** — foundations are REAL (DKIM signer, per-ISP throttles, warmup orchestrator w/ bounce gates, suppression enforced at send+worker, ARF/FBL ingestion w/ auto-throttle, List-Unsubscribe one-click, retry/backoff, live DNS all green incl. FCrDNS + DMARC p=quarantine), but 5 gaps MUST close before the first real production send: (a) Return-Path/MAIL FROM not rewritten to bounce domain (breaks SPF alignment on bounces + FBL routing); (b) no outbound content/spam self-check; (c) Google Postmaster Tools coded but no credentials; (d) Microsoft SNDS coded but no token; (e) bounce DSN ingestion not wired into suppression (retroactive only). Also: DMARC rua points at dmarc-reports@alecrae.com which can't RECEIVE yet (inbound not running) — reports currently vanish. ~15-26h total. | HIGH | 2026-07-14 | **(a)(b)(e) FIXED, re-verified live 2026-07-29.** (a) `buildReturnPath()` produces a VERP `bounces+<emailId>@bounce.<sender-domain>` envelope sender — per-customer bounce domain so SPF aligns under DMARC relaxed policy alongside DKIM — and is called in `worker.ts`'s send path. (b) `lib/outbound-spam-gate.ts` runs the previously caller-less `SpamClassifier` over outbound content before send (issue #125). (e) DSN suppression is wired at both ends: `processInboundDsn()` from `services/inbound/src/index.ts` for async bounces, plus hard-bounce auto-suppress inside the worker — though the async half only runs once `services/inbound` is actually deployed. **(c)(d) still OPEN and are Craig-only** (Google Postmaster Tools + Microsoft SNDS credentials). **This row's own premise was partly wrong:** it listed "warmup orchestrator w/ bounce gates" among the foundations that are REAL — the ramp existed but was enforced nowhere in the send path. See issue #139, fixed 2026-07-29. |
| 84 | **security-intelligence.ts fabricated phishing/threat and sender-reputation data with `Math.random()` on a shipped, user-facing UI** (security-center page) — invented threat types, severities, confidence scores, and SPF/DKIM/DMARC pass/fail results presented as real analysis. Not on issue #29's stub list; worse than a stub since it's confidently wrong rather than visibly missing. | HIGH | 2026-07-19 | FIXED 2026-07-19 — `/scan`, `/scan/batch` now return an honest "not available" (501/200 with `status: "unavailable"`) instead of persisting a fake verdict; `/sender-reputation/:email` keeps the genuinely real parts (block-policy status, this account's own threat history) and returns `reputationScore: null, checks: null, analysisAvailable: false` instead of invented numbers. Web UI (`security-intelligence-panels.tsx`) updated to render the honest-unavailable state. |
| 85 | **Workspace invitations were a complete dead end** — no invite email ever sent, the web UI discarded the invite token, and the accept endpoint was accidentally caught by the `/v1/organizations/*` authMiddleware wildcard (401 for a logged-out invitee, even though "the token IS the auth" per its own comment). Separately, accepting for a brand-new email created a `users` row with `passwordHash: null` and issued no session — the invitee had no way to ever log in even if they reached the endpoint. | HIGH | 2026-07-19 | FIXED 2026-07-19 — exempted accept + new `GET /invitations/:token/lookup` from the auth wildcard; accept now requires a password when creating a new identity (hashed the same way `/register` does) and returns a real token pair so the browser lands the invitee straight in the new workspace; Team page now surfaces a copyable accept link (`/invite/[token]`, new page) since no email sends yet. |
| 86 | **No server-side plan-tier enforcement anywhere in apps/api** — `apps/web/lib/plan.ts`'s FEATURE_PLANS gate (Pro-tier AI features: agent, semantic search, knowledge graph, context/commitments, sentiment timeline, voice clone, programs, etc.) was UI-only. Any Free-tier session could call these directly with no ceiling on Claude/AI spend. | HIGH | 2026-07-19 | FIXED 2026-07-19 — added `middleware/plan-gate.ts` (`requirePlan`) and wired it into every Pro-gated route mount in server.ts. DB `plan_tier` enum only has free/starter/professional/enterprise (team/business/business_plus have no billing yet — see pricing table), so those enforce at "pro" for now rather than silently blocking paying customers; documented in plan-gate.ts. `personal`-tier gating (grammar etc.) deliberately left untouched — that's a product decision on grammar tiering to make explicitly, not a side effect of closing this gap. |
| 87 | **`/v1/ai-intelligence` (priority scoring, smart replies, sentiment, writing coach, predictions) had no authMiddleware wired at all** — every request hit `requireScope()` with no auth context and 401'd unconditionally, for every caller including owners. Found while adding issue #86's plan gate. | HIGH | 2026-07-19 | FIXED 2026-07-19 — added the same authMiddleware + rate-limit + Pro plan-gate wiring every other route domain has. |
| 88 | **Archive/delete/star didn't persist** — overwrote the whole `tags` array and repurposed the send-pipeline `status` enum as "dropped" for both archive and delete; `GET /v1/messages` never filtered on either, so archived/deleted mail reappeared on reload, marked unread, labels/star wiped. A third, disconnected folder concept also existed (`bulk/move` wrote into `metadata.folder`, read by nothing). | HIGH | 2026-07-19 | FIXED 2026-07-19 — added real `is_read`/`is_starred`/`folder` columns (migration 0006, with a read-state backfill); `GET /v1/messages` defaults to `folder=inbox`; `PATCH /:id` is now a validated schema instead of an unchecked pass-through (closes the "arbitrary status string" gap too); bulk-actions.ts consolidated onto the same columns; Gmail/Outlook sync's already-computed isRead/isStarred (previously parsed then discarded) now persist; inbox mark-read-on-open/keyboard/undo now call the API instead of only updating local state. |
| 89 | **Snoozed page called a nonexistent `GET /v1/snooze`** (404 every time); snooze/unsnooze/undo-send all overwrote the whole `metadata` JSON column instead of merging, clobbering AI-triage fields; nothing ever resurfaced a snoozed email. `registerUndoable()` (undo-send) was fully implemented with zero callers anywhere — dead code, `POST /v1/send/undo/:id` always 410'd. | HIGH | 2026-07-19 | FIXED 2026-07-19 — added `GET /v1/snooze` + a 2-minute resurface sweep (plain `setInterval`, matching the existing grace-expiry/storage-reconcile pattern); snooze now sets `folder="snoozed"` (reuses issue #88's column) and merges metadata; own-domain sends now hold 10s before MTA pickup with `registerUndoable` wired to cancel the queued job — compose shows a "Sending in Ns… Undo" banner. |
| 90 | **Domains page showed hard-coded, wrong DNS records** for manual setup (wrong SPF host, wrong DKIM record type/selector, wrong DMARC policy, no MX/bounce at all) — a customer following it could never pass verification. `translate.ts`'s cached-translation lookup on `POST /:id/translate` had no `accountId` filter (cross-tenant read risk), unlike the scoped GET a few hundred lines below it in the same file. Send-time predictor was real but permanently stuck on its generic fallback — `tracking.ts`'s open/click handlers never called the engagement-recording endpoint. | MEDIUM-HIGH | 2026-07-19 | FIXED 2026-07-19 — domains page now renders `GET /v1/domains/:id/dns` (the real, already-correct server-generated records); translate.ts scoped by accountId; extracted `recordEngagementEvent()` from send-time.ts and wired it into `tracking.ts`'s pixel/click handlers so engagement data actually accumulates. |
| 91 | **`sentiment-timeline.ts` and 4 other Tier-7 routers (`context-intelligence.ts`, `knowledge-graph.ts`, `productivity-analytics.ts`, `scheduling-intelligence.ts`) read `c.get("accountId" as never)`** — a context key nothing in the codebase ever sets (the real key is `"auth"`). `accountId` was `undefined` on every request across all 5 routers (58 call sites) — every DB query scoped by `account_id=undefined`, which Postgres typically rejects outright. These endpoints (including the flagship "AI catches every promise" commitments feature) were most likely erroring on every call. The `as never` cast silenced the compile-time type error that would otherwise have caught this. | HIGH | 2026-07-19 | FIXED 2026-07-19 — replaced with `c.get("auth").accountId` everywhere; verified no other route file uses the same cast pattern. |
| 92 | **`sentiment-timeline.ts`'s "sentiment" had zero model calls** — score/tone computed by counting 14 hardcoded keywords as substring matches, "emotional tone" was a modulo index into a canned array with no relationship to content. Sold behind the Pro ($19/mo) tier as AI-derived relationship health. | HIGH | 2026-07-19 | FIXED 2026-07-19 — rebuilt `/analyze` + `/batch-analyze` on `analyzeEmailSentiment()` from `services/ai-engine`'s sentiment-analyzer.ts (the same real Claude Haiku classifier `ai-intelligence.ts` already uses correctly); returns an honest 503 when `ANTHROPIC_API_KEY` is unset instead of fabricating a result. Topic-keyword matching kept as-is (an honest heuristic, not a fabricated AI claim). |
| 93 | **AI-created rules never ran.** `ai-rules.ts`'s natural-language rule builder generates real Claude output and saves to `email_rules` — nothing ever read that table back and applied it to incoming mail. A user could create a dozen rules and every one sat inert. | HIGH | 2026-07-19 | FIXED 2026-07-19 — new `lib/rule-engine.ts` (`matchesRule` + `runRulesForEmail`), wired into `received-email-store.ts` for every Gmail/Outlook sync and MBOX/EML import. 7 of 11 action types implemented (move/archive/star/mark_read/delete/snooze/categorize); `label` deliberately skipped (two competing label systems already exist — see #76c — and this engine picking one would deepen that split); `mark_important`/`forward`/`auto_reply` skipped (need capabilities not yet added). Skipped actions log a warning. `smart_label_rules` (the other, differently-shaped rules table used by ai-categorization.ts) is a separate system, not covered by this pass. |
| 94 | New mail was invisible to semantic search — `enqueueEmail()`'s own doc comment said "called by the email ingest pipeline when a new email arrives" but nothing ever called it. | MEDIUM | 2026-07-19 | FIXED 2026-07-19 — wired into `received-email-store.ts` and both send paths in `messages.ts`. |
| 95 | **`ai-intelligence.ts` writing-coach + predictive-actions were `Math.random()`**, persisted to the DB and shown to Pro-tier users; the predictive-actions feedback loop compared real user behavior against a random prediction, so its "accuracy" metric was mathematically meaningless. | HIGH | 2026-07-19 | FIXED 2026-07-19 — both rebuilt on `lib/ai.ts`'s `aiComplete()` (Claude-primary/Vapron-fallback client, previously underused per the AI audit) with real prompts anchored to the actual draft/email content; honest 503 on AI-unavailable instead of fabricating. |
| 96 | **Compose grammar check was a 1500ms debounce + Claude round trip (2-4s), and "Apply" never touched the editor** — it only dismissed the suggestion chip; same for the `AISuggestionsPanel` side panel inside `ComposeEditor`. Meanwhile `apps/web/lib/local-ai.ts` + `webgpu-inference.ts` — a fully-built local WebGPU grammar engine matching the "instant, $0/token" architecture this file's own Aggressive Architecture section promises — sat wired to nothing but an orphaned status widget. | HIGH | 2026-07-19 | FIXED 2026-07-19 — compose now tries `localGrammarCheck()` first (instant on supported hardware, 400ms debounce instead of 1500ms), falls back to the real `grammarApi.correct()` Hono endpoint. Moved from structured-issue-chips to a full-corrected-text model so "Apply" is a real, working body replace. Found in the process: `local-ai.ts`'s own cloud fallback calls `/api/ai/complete`, a Next.js route that doesn't exist — so it always throws for non-WebGPU users; compose's own fallback covers this, but the route itself is still a gap (not built this pass). Also fixed `grammarApi.correct()`'s TypeScript return type, which didn't match the server's actual response shape. Not yet live-verified in a browser (typecheck + production `next build` both pass) — do a click-through smoke test before relying on it. |
| 98 | `voice/page.tsx` had no `PlanGate` despite `voice_clone: "pro"` in FEATURE_PLANS — free-tier users got the full Sonnet-backed voice cloning feature client-side. The compose-in-voice endpoint's degraded path also returned 200 with a placeholder draft and a fabricated `confidenceScore: 0.3` instead of a real error. | MEDIUM | 2026-07-19 | FIXED 2026-07-19 — gated the clone-profiles section; endpoint now returns 503/502 with a real error instead of fabricating. |
| 99 | **`ai-writing.ts`'s fallback paths (compose/rewrite/expand/translate/subject-lines/autocomplete) emitted plausible-looking confidence scores (0.58-0.91) on canned placeholder or genuinely irrelevant content** when Claude was unavailable — subject-lines even faked a descending-confidence gradient across "Option 1/2/3" placeholders to look like a ranked model output. | MEDIUM | 2026-07-19 | FIXED 2026-07-19 — added a `degraded: true` flag and zeroed confidence on every fallback path; proofread's heuristic fallback (real sentence-length-based scoring, not fabricated) keeps its scores but gets the same flag so callers can distinguish the two methods. |
| 100 | **`agent:*`, `collaborate:*`, and `dictation:*` scopes were missing from BOTH `base` and the owner/admin list** — the AI Inbox Agent page, team chat/shared-inboxes, and dictation 403'd for every session including owners. Auditing every `requireScope()` prefix against what's actually granted also found `templates:*`, `translate:read`, `programs:*`, and `calendar:*` missing from `base` (each gates a real page; only owner/admin could reach them, every member 403'd), and `analytics:read` — which gates the *read* side of the Tier-7 "personal AI intelligence" routers (commitments, knowledge graph, sentiment timeline, productivity analytics, scheduling intelligence) — was owner/admin/viewer-only, so a member still couldn't read their own AI insights even after #91 fixed those routers' accountId bug. | HIGH | 2026-07-19 | FIXED 2026-07-19 — all moved into `scopesForRole()`'s `base` list. |
| 101 | `dictation.ts` (real Whisper transcription + Claude structuring, honest degradation) had zero UI consumer anywhere in apps/web — the Competitive Mandate's "reply by voice, Dragon is dead" claim had no product surface. | HIGH | 2026-07-19 | FIXED 2026-07-19 — new `components/voice-dictation-button.tsx` wired into compose; records via MediaRecorder, transcribes, structures into a real email via Claude, merges into the existing draft. Depended on #100's scope fix to be reachable at all. |
| 102 | **No AI usage quotas existed anywhere** despite the pricing table's "Free: 5 composes/day" cap — a single session had unbounded Claude/Whisper spend. | HIGH | 2026-07-19 | FIXED 2026-07-19 — added `aiCallsPerMonth` per plan tier + `lib/ai-quota.ts`/`middleware/ai-quota.ts` (Redis month-bucketed counter mirroring the existing email quota.ts, fails open on Redis outage). Wired into all 60 `requirePlan("pro")` mounts plus baseline-scoped AI routes (grammar, dictation, translate, ai-rules, explain, compose-assist, ai-writing). Deliberately NOT applied to core search (free-tier baseline per FEATURE_PLANS) or real-time spellcheck (would exhaust a free account's budget in minutes of normal typing). |
| 103 | **Three non-interoperable event-type naming conventions for the same concept, discovered while fixing #70.** (a) DB `email_event_type` enum: dotted `"email.opened"` — the real source of truth. (b) Public SDK (`packages/sdk/src/types.ts`) `WebhookEventType`: a THIRD convention, `"message.sent"`/`"message.opened"` etc — never matches a real payload's `type` field, so it actively misdocuments the SDK for anyone building on it (no runtime validation catches the mismatch). (c) **Bigger gap:** two entire features — `routes/integrations.ts` (Zapier/Make/n8n connectors, full CRUD + a hardcoded `AVAILABLE_EVENTS` catalog listing `email.received`/`email.replied`/`contact.created`/etc) and `routes/programs.ts`'s sandboxed-TS-snippet `email.received`/`email.sent` triggers — have **no dispatcher at all**. Grepped for callers: `runProgram` is only ever invoked from programs.ts's own `/:id/test` dry-run endpoint (matches issue #76g's "dry-runs inflate real runCount"); nothing calls into `integrations.ts` from anywhere. Both UIs let a user configure a Zapier/n8n connection or a program trigger that will never fire for real activity — CRUD-only theater, worse than a stub since it looks fully configured. | HIGH | 2026-07-20 | OPEN — (a)/(b) are naming-only fixes (low effort, do alongside a real pass); (c) is a real feature build: needs a dispatcher wired into the same `storeReceivedEmail()`/send-path hook points issue #70 just used, PLUS a safety review for programs.ts specifically since it means running user-authored TS against every real inbound email rather than a manual sandboxed test run. Own scoped session, not a quick fix. |
| 104 | `/opt/alecrae/.env` on Jarvis has a stray space after `=` on the `RESEND_API_KEY` line (`RESEND_API_KEY= re_...`), found while installing issue #78's drift-check timer. Bun's dotenv loader tolerates it silently (all services boot fine), but POSIX-shell `source`/`.` parses `KEY= value` as "set KEY empty, then run value as a command" — breaks exactly the manual `set -a; . /opt/alecrae/.env; set +a` fallback `docs/infra/box-deploy.md` itself recommends for setting `DATABASE_URL` by hand. | LOW | 2026-07-20 | OPEN — fix the one line in the box's `.env` (remove the space) | **FIXED 2026-08-06 on the box** — corrected during the production deploy, which needed exactly this: the deploy sources the env file to get DATABASE_URL for db:migrate, so the malformed line was no longer merely theoretical. Verified with a real `set -a; . /opt/alecrae/.env; set +a` round trip. |
| 105 | **The mail box was running an unauthenticated open SMTP relay on port 25 for 9 days**, actively abused for 419/advance-fee spam (90 accepted messages, confirmed via a relay-scanner probe in the logs). Discovered live, not via code review — CLAUDE.md's own "mail services not running anywhere yet" text was stale and wrong; `alecrae-mta` had been running since 2026-07-11. Root cause: `services/mta/src/index.ts`'s inbound SMTP handler is unfinished placeholder code (comment: "In production: route to inbound processing pipeline") — it accepts and logs anything, then does nothing further. No spam was actually delivered anywhere (confirmed via the real outbound BullMQ queue: 2 completed jobs total, both legitimate pre-incident test sends from Craig; IP checked clean against Spamhaus ZEN/SpamCop/Barracuda/SORBS). | CRITICAL | 2026-07-20 | **FIXED same day** — `alecrae-mta` stopped immediately (Craig-authorized emergency action). Follow-up hardening shipped same session: TLS wiring (issue #78-adjacent, see the Shipped log) and per-IP throttling (Fail2ban-equivalent, `services/mta/src/smtp/server.ts`) — see commits `299fc32` and `5208e42`. **Still OPEN before this service can restart:** the inbound placeholder handler itself needs a real decision — finish the routing/filtering pipeline properly, or keep port 25 closed until it's built. Also OPEN: `vapron-email-send.service`, a *separate*, platform-wide MTA also found running on the same box/IP (port 587, since 2026-07-14) — not yet checked for the same vulnerability class; deliberately not touched without Craig's explicit go-ahead since it's outside AlecRae's scope and serves other platform tenants. |
| 106 | **main had zero GitHub branch protection** despite `ci.yml` running on every push — CI going red was purely informational and never blocked a bad commit, exactly how main stayed broken for 9 days in issue #66. | HIGH | 2026-07-20 | FIXED same day (Craig-authorized) — PR + 4 required status checks now gate merges to main; `enforce_admins` deliberately left off per Craig for an emergency bypass. Documented in issue #66's row above. |
| 107 | **Comprehensive security/robustness audit (2026-07-20), 6 parallel agents against both the user-supplied checklist and an independently-generated one covering network exposure, multi-tenant boundaries, MIME parsing, AI-specific risk, data integrity, and observability.** 14 of the highest-severity, most bounded findings were fixed same-day (see the Shipped log for full detail and commit hashes): three cross-tenant IDOR/data-leak bugs (`chat.ts` had zero auth checks on any channel endpoint; `account.ts` let any user delete any other user's passkey; `gamification.ts`'s leaderboard leaked every tenant's data — same bug class as the already-fixed productivity leaderboard); the MTA's TLS manager was fully built but never wired in (mail transits in plaintext — no cert exists yet either, a separate ACME/DNS task); the MTA had no per-IP abuse throttling (the direct enabler of issue #105); the offline-compose banner promised auto-queueing that didn't exist (three separate breaks: never called `queueOutboxEmail`, the sync engine was never instantiated reachably from compose, and reconnect never flushed the outbox regardless); one-click unsubscribe had perfect RFC 8058 headers but never wrote to `suppressionLists` (a real CAN-SPAM/GDPR exposure disguised as compliance); the built-and-tested DSN bounce processor (`services/mta/src/bounce/processor.ts`) was never called from anywhere — async bounces never suppressed dead addresses; email dedup was a pure app-level race with no DB constraint, and Gmail-reported deletions were counted but never applied locally (no stored mapping from Gmail's internal message id to our row existed at all — added `emails.providerMessageId`); prompt-injection framing was missing on all 9 `ai-writing.ts` endpoints (fixed once in the shared `callClaude()` helper); zero PII redaction existed anywhere before content reached Claude/Whisper (added to the automatic AI-triage path); the AI quota fails open on Redis outage with nothing behind it (added an in-memory, Redis-independent circuit breaker); account deletion was an immediate hard delete, directly violating this file's own Forbidden List rule #13 (schema already had `scheduled_for_deletion` + `scheduledDeletionAt` sitting unused); Stripe webhooks had no idempotency despite Stripe's documented at-least-once delivery guarantee. All 14 fixes have real regression tests, typecheck clean, and are pushed to main. | CRITICAL (bundle) | 2026-07-20 | 14 fixed same day (see rows above/below + Shipped log for the itemized list with commit hashes). **The remaining findings are logged individually below (issues #108–#116) rather than re-summarized here** — this row exists as the audit's index/provenance record. |
| 108 | **SSRF gaps beyond the already-guarded link-preview path.** `apps/api/src/lib/ssrf-guard.ts` is a thorough guard (IP-range blocking incl. cloud metadata, redirect-chain re-validation) but is used in exactly one place. Three other server-side fetches of user-supplied URLs bypass it entirely: `lib/webhook-dispatcher.ts:323` (the REAL webhook delivery path, not just the `/test` endpoint), `routes/integrations.ts:256` (webhook test-send), and `routes/meetings.ts:496` (`POST /transcribe`'s `audioUrl` — worse than blind SSRF, since the fetched response gets transcribed and handed back to the caller, an exfiltration channel for internal service responses/cloud metadata). | HIGH | 2026-07-20 | FIXED 2026-07-20 — `safeFetch()` applied to all three call sites; extended it to support `method`/`body` (needed for POST webhook delivery), bodies deliberately never re-sent to a redirect target. Tests added. |
| 109 | **Systemic cross-tenant/authorization gaps beyond the 3 already fixed.** `routes/delegation.ts`'s `POST /` never validates `delegateUserId` belongs to the caller's workspace before granting mail-delegate permissions, and `GET /inbox` leaks delegation metadata cross-account (no `accountId` filter — message bodies aren't wired yet, so impact is metadata-only today). `routes/push-notifications.ts`'s `POST /subscribe` updates by `endpoint` alone with no owner check (low exploitability — requires knowing another user's secret push endpoint). | MEDIUM | 2026-07-20 | FIXED 2026-07-20 — delegation POST / validates via `getWorkspaceRole()`, GET /inbox scoped by `accountId`; push-subscribe now explicitly transfers `userId` on re-parent instead of silently updating in place. Tests added. |
| 110 | **Web session tokens stored in `localStorage`, including the 7-day rotating refresh token** (`apps/web/lib/auth-token.ts`) — any XSS on the web app is a full, long-lived account takeover, not just a short-session leak. The one cookie set (`alecrae_session`) is non-`httpOnly` and used only for Next.js page-routing gates, never trusted by the API (bearer-token auth is used throughout, which is CSRF-safe but doesn't get the XSS-mitigation benefit `httpOnly` would). Desktop (`apps/desktop`) has zero credential storage code at all — no `safeStorage`/OS-keychain integration despite Electron providing one, so it inherits the same weakness. Mobile (`apps/mobile`) is correctly implemented via `expo-secure-store`. | HIGH | 2026-07-20 | OPEN — architectural, larger lift than the other audit findings (would need an `httpOnly` cookie-based session redesign, or at minimum OS-keychain wiring for desktop) |
| 111 | **Upstream provider rate-limit (429) handling is entirely missing.** `lib/ai.ts`'s `callClaude` and `sync/engine.ts`'s Gmail/Outlook `fetch()` calls have zero 429-specific handling, no `Retry-After` header read, no backoff — a 429 is treated like any other failure and immediately retried on the next unconditional sweep (5-minute for sync). A burst of reconnects after an outage risks Google throttling the whole app's OAuth client. Related, lower-severity: DNS lookups in `services/mta`/`services/inbound` (`dns.resolveMx`/`resolve4` etc.) have no explicit timeout, relying entirely on OS/Node defaults — if the box's resolver becomes unreachable, delivery + inbound DKIM/SPF checks hang with no bound. Also: Redis is not centrally pooled — at least 9 separate call sites each construct their own `new Redis(...)` client independently. | HIGH (429 handling) / MEDIUM (DNS timeout, Redis pooling) | 2026-07-20 | **PROVIDER-SYNC HALF FIXED 2026-07-29** — new `lib/provider-backoff.ts` records a per-account "do not sync before" window on a 429, honouring `Retry-After` in **both** legal encodings (delta-seconds *and* HTTP-date) and clamped to `[60s, 1h]`: the floor stops a `Retry-After: 0` meaning "retry forever", the ceiling stops a bogus value silently disabling a customer's mail for days. Also detects Google's **403-with-`rateLimitExceeded`**, which would otherwise read as an auth failure and keep us hammering an exhausted quota. Worst instance found and fixed in passing: a non-ok Gmail *history* response fell straight through to a **full** sync, so a 429 immediately triggered a far more expensive request against the quota we had just been told we exhausted. Backed-off accounts are counted separately from `attempted` in the sweep so a pause never reads as a failure. State is in Redis rather than a new `connected_accounts` column — ephemeral by nature, and a migration is Boss Rule #7. 13 tests. **DNS HALF FIXED 2026-07-29** — `services/mta/src/dns/resolver.ts` gives one process-wide `dns.Resolver` with real `timeout`/`tries` bounds (5s × 2 by default, env-tunable, and a zero/negative/unparseable value falls back to the default rather than *removing* the bound). Swapped in at all four MTA call sites plus the inbound DKIM verifier, which reaches it through `@alecrae/mta/lib` rather than keeping a second copy that could drift. Deliberately **not** a `Promise.race`: racing resolves the caller early while the underlying query keeps running, so under a resolver outage it trades a stall for an unbounded pile of in-flight queries. The severity here is about *where* it lands — a worker blocked in `resolveMx` isn't failing, it's waiting, so the queue backs up behind it and (with no alerting, #72) the first symptom is a customer noticing mail stopped. 6 tests. **AI HALF FIXED 2026-07-29** — Claude 429/529 now carry distinct error codes instead of being indistinguishable from a real failure, so the Vapron fallback is logged rather than silent (it means the answer came from a different model). One inline retry when `Retry-After` fits inside the 2s cloud-AI budget; anything longer falls through to the fallback, because inside a user's request a long wait is a hang, not a retry. 5 tests. **REDIS POOLING FIXED 2026-07-29 — issue #111 is now FULLY CLOSED.** New `apps/api/src/lib/redis.ts` is the single source: 8 private `getRedis()` copies (six byte-identical) plus a ninth in `uptime.ts` collapse to one shared, readiness-gated client, taking a process from 13–15 connections to one. **The argument was never tidiness — the duplication had produced real defects:** `closeQuotaRedis()`/`closeAiQuotaRedis()` were exported, documented as "call during app shutdown", and called by **nothing**; four modules had no close function at all; and `uptime.ts` set its availability flag `true` once at module load and `false` in three places, never back, so the **first** transient error permanently downgraded the uptime ledger to in-process memory for the life of the process. One shared client is safe because ioredis multiplexes and **no caller uses the three modes that monopolise a connection** (SUBSCRIBE, blocking pops, interactive WATCH/MULTI) — verified by grep, not assumed. BullMQ deliberately excluded (Workers block; it requires `maxRetriesPerRequest: null`). Health probes deliberately keep throwaway connections — a probe on a pooled socket reports that socket's cached readiness rather than testing reachability. **No control changed its fail direction; that was the acceptance criterion.** Enforced in three layers because a convention would be as skippable as the last one: the registry closes new subsystems automatically, an ESLint `no-restricted-imports` rule makes the value import unwritable, and `redis-single-source.test.ts` catches anything past lint including a deliberate `eslint-disable` — **both layers verified to reject a real violation.** 18 tests; existing suites pass unmodified but for one line in each of two reset helpers. |
| 112 | **No live-state drift detection for running services/ports, only for git commits (issue #78 covers code version only).** No script or timer greps `systemctl list-units`/`ss -tlnp` to check "is a service running that shouldn't be" or "is an expected service NOT running" — precisely how issue #105's open relay went unnoticed for 9 days. Related: no firewall rules (ufw/iptables) or SSH hardening (fail2ban on :22, `PermitRootLogin`/`PasswordAuthentication` settings) are version-controlled anywhere in this repo — that state lives only on the live boxes, invisible to review (that sub-part remains OPEN). | HIGH | 2026-07-20 | Service/port half FIXED 2026-07-20 — `scripts/check-service-drift.sh` (host-configurable `EXPECTED_SERVICES`/`EXPECTED_PORTS`), wired into `/health`/`/v1/health/detailed`. Setup: `docs/infra/service-drift-check.md`. **Firewall/SSH-hardening-as-code still OPEN.** |
| 113 | **Email parsing/deliverability correctness gaps**, all found in the audit: (a) `packages/email-parser/src/parser.ts` (the import/EML/MBOX path) ignores declared charset entirely — always decodes as UTF-8 regardless of a `charset=iso-8859-1`/`shift-jis` header, silently mojibake-ing non-UTF-8 imports (the separate, better `services/inbound/src/parser/mime-parser.ts` used for live inbound mail handles this correctly); (b) no SMTPUTF8/punycode fallback anywhere — an internationalized address sent to a non-SMTPUTF8-supporting remote server (several of our own hardcoded ISP profiles say `supportsSmtpUtf8: false`) has no ASCII-safe fallback; (c) no recursion-depth limit on nested `multipart/*` parsing — a pathological message trades a soft per-message CPU/latency hit for itself, not an outage (the surrounding try/catch already isolates it); (d) ~~`services/mta/src/bounce/classifier.ts`'s richer bounce classifier is dead code~~; (e) ~~Outlook sync doesn't attempt delta-delete detection at all~~. | MEDIUM | 2026-07-20 | **(d) and (e) FIXED 2026-07-20.** (d): `classifyBounce()` now called on every bounce, mapped to the DB's `bounceType`/`bounceCategory` enums, tests added. (e): Outlook sync now handles delta-query `@removed` markers (previously would have parsed a deletion marker as a garbage message, not just missed it) and sets `providerMessageId` for correlation, same as Gmail's fix — tests added. **(a) FIXED 2026-07-29** — and it was three bugs, not one: base64 bodies ignored the Content-Type charset (every caller already passed the header; the parameter was named `_contentType`), RFC 2047 encoded words ignored the charset named *in the word itself* (parameter named `_charset`, so subject lines — the most visible field in the product — mis-decoded), and quoted-printable returned one code unit per byte, so a **UTF-8** QP body read as latin1 (`=C3=A9` → `Ã©`). That last one fires on ordinary European-language mail rather than only legacy archives, so the practical blast radius was much wider than "non-UTF-8 imports". Shared `decodeBytes()` falls back to UTF-8 on an unrecognised label — an import must never abort mid-mailbox over one message's charset — and leaves `fatal` off so invalid bytes become U+FFFD instead of discarding the message. 12 tests. **(b) FIXED 2026-07-29** — the two halves of an address needed opposite treatment, which is the shape of the fix: a **domain** always has an ASCII form, so punycode it (applied in `SmtpClient.resolveMx`, in the optimizer's `resolveMx` *before* the cache key so both spellings share one entry, and to the RCPT TO recipient) and it delivers to every server ever written with no extension negotiation; a **local part** has none, so the client checks the negotiated extensions after EHLO and refuses with a clear 550 rather than sending a RCPT TO it knows will be rejected — a rejection we caused still counts against sending reputation. `toAsciiDomain` returns its input rather than `""` on conversion failure, since substituting the empty string turns a bad address into a lookup against nothing. 13 tests. **(c) FIXED 2026-07-29** — depth capped at 20 and width at 500 parts per level in **both** parsers. Severity was understated by the original note: `services/inbound`'s parser runs on the port-25 receive path, so an anonymous remote sender chose our recursion depth, and each level re-splits the body it was handed, making the work superlinear in the message's own size. The width cap is separate and necessary — one level with tens of thousands of siblings is the flat version of the same attack and the depth limit does nothing about it. Over-depth parts are returned unparsed rather than dropped. 6 tests, the first two being ordinary mixed/alternative mail, since a bound that broke real structure would be worse than the problem. **Issue #113 is now fully closed.** |
| 114 | **The real HTML email rendering pipeline (`packages/email-parser/src/document-model.ts`, "SEDM") is fully built — URL allowlisting, block-model parsing — and completely unused.** The inbox currently strips all HTML to plain text before display (`apps/web/app/(dashboard)/inbox/page.tsx`), which is *why* XSS/tracking-pixel/CID-image/quote-nesting/CSS-isolation/dark-mode are all non-issues today — none of that HTML ever reaches the DOM. This is a forward-looking landmine, not a live risk: the moment someone wires `htmlToDocument` in (a natural next step given the code already exists for exactly that), every one of those becomes a real, live gap simultaneously with no groundwork laid — no sanitizer library in the repo, no iframe/shadow-DOM isolation, no image-blocking/proxy mechanism, no CID-to-blob-URL rewriting, no dark-mode color-inversion logic. | HIGH (forward-looking) | 2026-07-20 | OPEN — must be designed as one piece before HTML rendering ships, not patched on after |
| 115 | **Data integrity / disaster-recovery gaps.** (a) IndexedDB corruption is never detected — `apps/web/lib/offline-store.ts`/`indexeddb-cache.ts` only handle `onerror` on open failure, nothing checks structural integrity or triggers a rebuild; storage-quota monitoring and attachment cache eviction are both entirely absent (`indexeddb-cache.ts`'s `evictOlderThan()` exists but has zero call sites — two parallel, mostly-disconnected local-cache database implementations exist, `alecrae-mail` vs `alecrae_mail`, a separate cleanup worth doing on its own). (b) A DB restore has never actually been drilled — `docs/compliance/soc2/policy-templates/bcp-dr-plan.md` has literal `[INSERT DATE]`/`"Not yet conducted"` placeholders where a real test record should be. (c) ~~The incident-response runbook's own JWT_SECRET-rotation step doesn't warn that the same rotation permanently breaks decryption of every stored OAuth token~~. (d) E2EE key loss has no proactive backup/export flow at all — the only warning is reactive (shown after a key mismatch is already detected), not at the point of risk (enabling encryption for the first time). | HIGH | 2026-07-20 | **(c) FIXED 2026-07-20** — runbook now explicitly warns the containment step breaks OAuth token decryption + requires user reconnection, framed as an expected side effect to plan for, not a surprise. **(a)(b)(d) still OPEN.** |
| 116 | **Billing/compliance gaps beyond Stripe idempotency (fixed this session).** (a) ~~`createCheckoutSession` never checks for an existing active subscription before creating a new Checkout Session~~. (b) No multi-currency amount handling anywhere in `billing.ts` despite the six-currency business-plan pricing table already in this file — self-acknowledged as "billing wiring pending," not a silent bug, but real exposure the moment business-plan billing ships before this is built. (c) ~~Failed-payment dunning (`recordPaymentFailure`/grace window/downgrade) is fully real, but sends zero user notification at any stage~~. (d) No GDPR export or right-to-be-forgotten self-service exists at all — `routes/dpa.ts` is a B2B contract e-signature flow, not a data-subject tool; any Article 15/17 request today is entirely manual/out-of-band. | HIGH ((d) GDPR) / MEDIUM (a,b,c) | 2026-07-20 | **(a) FIXED 2026-07-20** — `createCheckoutSession` now checks the existing subscription's live Stripe status (not just our DB's possibly-stale copy) and blocks a new session if active/trialing/past_due, directing to the billing portal instead. Tests added. **(c) FIXED 2026-07-21** — added 3 nullable timestamp columns to `dunning_records` (migration 0011) to track per-cycle send state, and a new `lib/dunning-emails.ts` chokepoint (gated behind `VAPRON_DUNNING_EMAILS`, same opt-in pattern as the existing welcome email — off until Craig flips it on, since this reaches a real customer's inbox about a real billing event). Wired into all three real transitions: first failure of a new cycle → "payment failed, N days to fix it" (retries within the same cycle do NOT re-send); grace-window expiry or a dunning-driven subscription cancel → "downgraded to Free" (a *voluntary* cancel, never having been past_due, stays silent — no false "your payment failed" email); recovery → "payment succeeded" (+ which plan was restored, if any). 13 new tests across `dunning-emails.test.ts` (templates) and `billing-dunning-notifications.test.ts` (wiring — verifies each transition fires exactly once and silent cases stay silent), all passing; full monorepo typecheck clean. **(b)(d) still OPEN** — (d) GDPR export/erasure is the highest-priority remaining item in this whole findings set. |
| 118 | **`lib/ai.ts`'s `aiComplete()` — used by AI triage, `ai-rules.ts`, `ai-intelligence.ts`, and other callers — has the same prompt-injection framing gap fixed in `ai-writing.ts` this session (issue #107), but was deliberately left unfixed pending a survey.** | MEDIUM | 2026-07-20 | FIXED 2026-07-20 — surveyed all 4 callers (all single user-role messages, no multi-turn) and applied the same delimiter-wrapping fix once inside `aiComplete()` itself, so both providers (Claude + Vapron) inherit it. Test added. |
| 119 | **Migration caution: `emails_account_message_id_idx` (migration `0008_overjoyed_risque.sql`, part of issue #107's dedup fix) is a new unique constraint on `(account_id, message_id)`.** It was reasoned safe (idempotent sends dedupe upstream of the DB write; synthetic import IDs are always-fresh UUIDs) but was not verified against live production data. | LOW | 2026-07-20 | **FIXED/VERIFIED 2026-07-20** — ran a read-only `GROUP BY ... HAVING COUNT(*) > 1` query against the live production `emails` table on Jarvis (via `bun run --env-file`, no raw credentials handled): **0 duplicate `(account_id, message_id)` pairs found.** Migration 0008 is confirmed safe to apply on the next `db:migrate`. |
## 🧭 PRODUCT DECISIONS LOG

| Date | Decision | Rationale |
|---|---|---|
| 2026-07-01 | **Native IMAP: not required for launch.** Gmail/Outlook OAuth sync is the supported path; `services/imap`'s TCP listener stays broken (issue #58) and is documented honestly rather than claimed done. | OAuth covers the real-world usage pattern; fixing the IMAP handler/type-shape mismatch is ~1 week of work with no launch-blocking upside right now. |
| 2026-07-01 | **First users: general beta, no specific vertical (not lawyers/accountants-only).** All modules held to a general-beta quality bar rather than a compliance-heavy vertical bar. | No vertical has been chosen yet; revisit this once first users are actually onboarded — a legal/accounting focus would raise the bar on E2EE, audit logging, and retention features specifically. |
| 2026-07-03 | **Multi-workspace: one login can own/join several separate workspaces (accounts), each with its own billing, domains, mailboxes, and team — via a `workspace_members` table, not by widening `users.accountId` to a many-to-many everywhere.** | Every downstream resource (mailboxes, domains, emails, billing — 90+ route files) is already scoped purely by `auth.accountId` from the JWT; keeping that contract and only changing how the ACTIVE accountId is chosen (login → home workspace; switch-workspace → any workspace you're a member of) avoided touching those 90+ files. Alternative considered: give every `users` row a home account only and require a separate login per workspace (Slack-style) — rejected, worse UX for "one login, many workspaces." |
| 2026-07-13 | **Mail engine stays on the "158" box (149.28.119.158) as a dedicated mail box; Jarvis (161) keeps web/api.** All mail DNS records target 158. | 158 already has outbound port 25 unblocked by Vultr, PTR → mail.alecrae.com, and SPF authorizing it — weeks of deliverability groundwork kept, and a dedicated sending IP is isolated from other products on Jarvis. Alternative (consolidate on 161) rejected for now: would need a Vultr port-25 ticket, new PTR, SPF rewrite, and IP warmup from zero. Full plan: `docs/infra/multi-platform-mail-plan.md`. |

---

## 🗓️ NEXT ACTIONS — IN ORDER

### ⏸️ RESUME HERE — session paused 2026-08-06 07:20 UTC

**Branch `audit/production-readiness-2026-07-28`, working tree clean, all four gates green. Draft PR #92 is open against main to run the four required CI checks against the whole branch for the first time (checks only run on PRs, and none existed before — so 90 audit commits had never been CI-validated; all four required checks pass, only the pre-existing Default-Setup CodeQL conflict fails, which is already item 12 on Craig's list).**

**Production-readiness pass, 2026-08-05 — what this session found and did.** Ran five parallel deep audits (doc-vs-code verification, contract drift, never-audited surfaces, the live website, plus a ground-truth deploy check) and fixed everything bounded. Verified before trusting, per the standing lesson.

- **THE most urgent finding, #155 (CRITICAL, fixed): the migration journal was out of order.** Hand-written 0006's timestamp was larger than generated 0007–0010, so Drizzle's migrator — which only compares against the DB's max recorded timestamp — would have **silently skipped 0008/0009/0010 on the very next deploy** (which business-email bring-up makes imminent). That reverts the dedup index, drops `provider_message_id` (returning the #79 empty-mailbox outage), and breaks every Stripe webhook. No gate saw it: the schema-drift check never checked journal monotonicity. Fixed + guarded with a test.
- **#156 (CRITICAL, fixed): remote code execution in the API process.** `POST /v1/scripts/:id/test` ran user code via `new Function()` behind a fake sandbox any function literal escapes; combined with `register` making every signup an owner, any user could read `JWT_SECRET`/`DATABASE_URL`/API keys or hang the event loop for all tenants. Endpoint now 501s, unsafe executor disconnected and structurally guarded. (The `register`-grants-owner half is flagged for Craig.)
- **#157 (HIGH, fixed): the marketing send path was still unsatisfiable after #152.** The compliance gate passed `hasUnsubscribeLink`/`hasPhysicalAddress` as hardcoded `false`, so a campaign with a perfect unsubscribe link and postal address was still refused. Now derived from the real body.
- **#158 (HIGH, fixed): the outbound virus scanner reported `clean: true` for files it never scanned** (no key configured in prod), the #141 fabricated-verdict class, live in the send path. Unscanned is now honestly not-clean; the send still fails open by policy but no longer lies about it.
- **#159 (HIGH, fixed): warm-up adaptive safety was inert in three ways.** The bounce/complaint health cycle had zero callers (so the auto-pause could never fire and the ramp advanced on a bounce rate pinned at 0), the reputation hard-pause was consulted on only one of three send paths, and the counter both raced and counted messages instead of recipients. All three closed: hourly health cycle, warm-up moved into the shared gate so every producer inherits it, and recipient-accurate SQL-atomic counting.
- **#160 (HIGH, fixed): DKIM private keys were plaintext under comments claiming encryption.** Now sealed with AES-256-GCM through a single shared `secret-box` in `@alecrae/crypto` (which `token-crypto.ts` also delegates to, rather than keeping a second copy). Undecryptable keys route into #144's hold-don't-send-unsigned branch rather than throwing; legacy rows self-heal, so no migration.
- **#161 (HIGH, fixed): the "Dependency Audit" CI job had never audited anything** — `audit-ci` cannot run against a Bun lockfile, so it errored every time and `|| true` reported success, while **155 advisories including 3 critical** went unreported and four SOC 2 documents attested the control was implemented. Replaced with a real `bun audit` gate that blocks on unreviewed criticals; the three current ones are allowlisted individually with verified reasons; all four SOC 2 docs corrected.
- **#162 (MEDIUM, fixed): quoted-printable attachments were corrupted on every MBOX/EML import** — a double-encode turned every byte ≥ 0x80 into two, so a 12-byte PDF fragment arrived as 15 broken ones. The correct helper had been sitting unused 30 lines below since #113(a). Also fixed four manifests whose `start` pointed at a `dist` their build never produces (the #153 class, still present in the API itself).
- **#163 (MEDIUM, fixed): batch categorisation invented a category from the character sum of the email id** and stored it as Claude output, then averaged it into the reported accuracy figure. Unknown ids are now skipped and reported.
- **Smaller fixes shipped:** the `mta: ok` health probe was meaningless (queue-readable ≠ worker-consuming — the exact #149 silent-vanish mode; now reports degraded with 3 tests); the attachments page still claimed "virus scanned"; nine `(legal)` pages including the **CCPA-required California-notice and Do-Not-Sell** bounced logged-out visitors to `/login`; dead footer links to nonexistent status/changelog/docs; the undefined `accent`/`text-text-*` colour utilities (~109 uses rendering transparent/blue); `box-deploy.sh` would have **restarted the emergency-stopped MTA** as a deploy side effect (used `is-enabled` not `is-active`); the service-drift checker was blind to IPv6 `[::]` wildcard binds (a repeat open-relay on v6 would pass it forever); route-coverage.md regenerated (was 3+ weeks stale while marked authoritative).

**✅ MERGED AND DEPLOYED 2026-08-06.** The branch is merged to main (`f9d1528`, 100 commits) and **live on Jarvis** — production ran 2026-07-19 code (`51c763e`) for 17 days, so every fix from #120 through #163 was unreachable until this deploy. Verified live: `api.alecrae.com/health` reports `f9d1528d06f4`, the web app rebuilt at the same SHA, database 151 tables, `deployDrift` back to `drifted=false`. **`/v1/health` reports `degraded` and that is correct, not a fault:** the new MTA probe (this session) now says *"queue reachable but no worker is consuming it — queued sends will not be delivered"* instead of the meaningless `ok` it used to return while `alecrae-mta` sat stopped. Meilisearch is also down, which predates the deploy.

**The deploy proved issue #155 was real and about to fire.** Before migrating, the production `__drizzle_migrations` table's MAX `created_at` was **1784563200000 — exactly migration 0006's timestamp**. Under the old journal, 0008/0009/0010 all carried *lower* values and Drizzle would have skipped all three on this very run. After the fix: 8 → 12 migrations, and all three objects verified present in the live database (`emails_account_message_id_idx`, `emails.provider_message_id`, `stripe_webhook_events`). Had the deploy gone ahead unfixed, the dedup constraint would have vanished, Gmail/Outlook sync would have failed on a missing column, and every Stripe webhook would have thrown.

**Deploy prerequisites resolved on the box:** `API_URL=https://api.alecrae.com` added to `/opt/alecrae/.env` (issue #140 — the API now refuses to boot without it, by design; it booted clean, 8/9 checks) and the issue **#104** stray-space bug fixed, which had stopped being theoretical because the deploy itself sources that file for `DATABASE_URL`.

**Still not deployed anywhere:** `services/mta` (deliberately stopped since the #105 open-relay incident — restarting is Craig's call) and `services/inbound` (never run; receiving is most of what business email means).

**All work is committed and pushed. All four gates green: typecheck 36/36, lint 0 errors, test 48/48 tasks (apps/api 444), build 26/26.**

**Craig's stated goal, 2026-08-04:** he has many platforms (Zoobicon, BookARide, DavenRoe, Gluecron, Verom, Dominat8, …) that each need an organisation with **business email on its own domain**. That is now the driving priority, and it reframes the roadmap: the critical path is *send **and receive** on customer domains*, not the general audit backlog.

**What that path looks like, verified in code rather than from the docs.** The parts that already work: org/workspace per platform; add a domain and get the real DNS records (`GET /v1/domains/:id/dns`); provision a mailbox (`POST /v1/mailboxes`, gated on verified+active domain); send as any address on a verified domain (`messages.ts` validates ownership); and **`services/inbound` is a complete, bootable receive pipeline** — SMTP → MIME → SPF/DKIM filter → DB-backed routing (real mailbox lookup, aliases, plus-addressing, catch-all) → Postgres. That last point corrects a widespread misreading: the "unfinished placeholder inbound handler" warning in #105/#128 is about `services/mta`'s **duplicate** receiver, a different file. The real inbound service is not the blocker people thought.

**Shipped this session, all on that path:** the mailbox management UI (`/mailboxes`) — the actual missing piece, since provisioning previously required curl; `PATCH /v1/mailboxes/:id`; issue **#151** (pre-send gate unified across every producer); and two findings that came out of it, **#152** (the send path 422'd on *every* ordinary email — the single biggest blocker to business email, invisible because the test suite mocks the compliance engine) and **#153** (received spam was filed into the inbox).

**Next, in priority order for the business-email goal:**
1. **Deploy the branch** (needs the PR merged — see below). Everything Craig sees on the live site is 07-19 code; the whole audit campaign is invisible in production until a deploy. This is now the highest-leverage single action.
2. **#159 warm-up safety + #160 plaintext DKIM keys** — both HIGH, both on the send path, both need doing before real customer mail flows at volume.
3. **#154 (open):** the mailto unsubscribe path needs a real job shape, an `accountId` + verified domain, and a `from` validated against a domain the account owns. Fails honestly today; that is the floor, not the fix.
4. A **send/receive smoke test against a real domain** once Craig's box steps below are done — nothing in this repo can prove the loop end to end.
5. Then the remaining backlog: #143 (e2e suite runs nowhere), #110 (tokens in localStorage), #114 (HTML rendering), #115(a)(b)(d), #116(b), and the MEDIUM findings from the 2026-08-05 surface sweep (CI `|| true` on the security scans, `bun run start` broken on the API/collab/jmap/sentinel, the batch-categorisation hash-as-Claude-output, the analytics/collab/jmap/support service-level issues in #146's keep-or-delete set).

**Waiting on Craig — nothing in the codebase can progress these, and business email cannot work without 1–3:**
1. **The tailnet Redis setup, before the MTA is ever started** (issue #149, `docs/infra/redis-tailnet-setup.md`) — otherwise the API enqueues into one Redis, the MTA watches another, and **every send silently vanishes** with no bounce, error or log line.
2. **`API_URL=https://api.alecrae.com`** in `/opt/alecrae/.env` before the next API restart, or the API refuses to boot (deliberate — see issue #140).
3. **Deploy `services/inbound` on the mail box** (`149.28.119.158`, port 25 + ufw). It has never run anywhere, and receiving is most of what "business email" means. `docs/infra/business-email-domain-onboarding.md` is the runbook; its stale `.dev` warning was corrected 2026-08-04.
4. **Per-platform DNS** — MX → mx1/mx2.alecrae.com, SPF, DKIM, DMARC, bounce CNAME, one set per domain. Auto-config is still broken (#83), so this is manual; `GET /v1/domains/:id/dns` prints exactly what to paste.
5. One TXT record in the alecrae.com zone: `*._report._dmarc` → `v=DMARC1` (issue #148, `docs/infra/dns-zone-alecrae.md` §5a). Without it no DMARC reports are ever sent for any customer domain.
6. The real Vapron DNS API shape, if auto-config is wanted (issue #83 / the error Craig hit live). Deliberately not guessed at again.
7. Google Postmaster Tools + Microsoft SNDS credentials (issue #82 c/d).
8. Decisions: restarting the MTA (#105 — relay control #127 and receiver-off-by-default #128 have since landed, but it is Craig's call), **the #152 marketing-vs-correspondence classification as policy**, the automation dispatcher's opt-in schema (#132, Boss Rule #7), keep-or-delete on five undeployed services (#146), the `register`-grants-owner default (#156), whether to configure `VIRUSTOTAL_API_KEY` given the free tier shares samples (#158), and **merging PR #92** (main requires PR + green CI — all four required checks now pass on it).
9. **Landing-page and legal copy — the biggest trust/legal exposure found, all Boss Rule #9/#10 and untouched by this session (the 2026-08-05 live-site audit).** The site sells an email product that cannot currently send, receive, or connect an account, and makes ~12 claims that are false or inoperable in the running build. Worst offenders: **(a)** Personal ($9) sells "E2E encryption / zero-knowledge / we can't read your email" while the deployed code does the *opposite* (#129 — server holds an unrelated keypair, passphrase touches the server, nothing encrypts the mail); this is a paid security claim that is the reverse of the architecture. **(b)** "Commitments tracked automatically" (already flagged #138) and the whole "runs automatically on new mail" family (#132) — nothing dispatches. **(c)** "Universal Inbox — Gmail, Outlook, iCloud, Yahoo, any IMAP" names competitors (Forbidden Rule #23) and promises IMAP/iCloud/Yahoo that are documented non-goals (#58); even Gmail 403s for the public (item 6b). **(d)** Legal pages name **"AlecRae, Inc., San Francisco / 548 Market St"** (no evidence this US corporation exists; Craig is NZ-anchored), `/privacy` claims **AWS/Hetzner** infra (actual: Vultr + Cloudflare), and `/sla` commits to **99.99% uptime with 10–50% service credits** on infrastructure that had a 3-day unnoticed outage and has no alerting (#72) or status page (#71) — a formal contractual promise nothing supports. Registration is open despite "private beta" framing. None of this is a code fix; it needs Craig's copy/legal decisions before any real traffic.

**Method note worth keeping:** the findings that mattered most came from *structural* questions, not from re-reading files — "who mounts a router without auth", "who enqueues onto the send queue", "which headers disagree with server.ts". Recorded "FIXED" claims were wrong in both directions repeatedly (#105, #98, #73i were not fixed; #76d was fixed and still listed open), so verify before trusting a row.

**Sharper version of that lesson, 2026-08-04 — the mock is the blind spot.** Issue #152 was a CRITICAL, product-stopping bug (every ordinary send 422'd) sitting under a fully green suite, because `messages.test.ts` mocks `ComplianceEngine.checkAll()` to return zero violations. The same shape as #131 (per-route tests mock `requireScope`, so no route test can see a wiring gap) and #120 (a green typecheck said nothing; only running the suite did). **Where a test mocks a collaborator, it has stopped testing the thing that collaborator does — so when a component is load-bearing, run it for real at least once.** The three findings this session all came from *executing* something rather than reading it: running the real compliance engine, following the job shape from producer to worker, and reading what `InboxAgent.run()` actually calls versus what the comment above it claimed.

---

> Completed items 1–10 + 18–19 of the old list are in the archive. Current list (2026-07-13):

-1. **Full journey audit build plan (2026-07-22)** — `docs/audits/2026-07-22-full-journey-audit.md` is now the authoritative source for feature-by-feature status, with a phased build plan and checkboxes. Highest priority per that plan: Phase 0 (Craig's live Domains/Vapron-DNS blocker — needs real Vapron DNS API docs, same category as issue #83's email fix), then Phase 1 (7 journeys broken/crashing for real users today: Drafts, Files, Achievements, Translation, Security overview, Integrations, AI Triage plan-gate), then Phase 2 (the one outright false claim — Settings/Encryption's "encrypted automatically" doesn't actually encrypt anything). Phase 3 covers the single biggest-leverage fix found: 11 separate features across 4 nav sections all promise "runs automatically on new mail" and none of them fire, because nothing in the real ingestion pipeline ever dispatches to any of them — one real hook point fixes 8 of the 11.
0. **Security audit follow-ups (issues #108–#119, from the 2026-07-20 incident-response audit)** — 13 findings logged, not yet fixed. Highest priority: #108 (SSRF gaps in the real webhook-delivery + transcription paths — bounded, mechanical fix), #110 (web session tokens in `localStorage` incl. the 7-day refresh token — architectural), #112 (no live service/port-inventory drift detection — the actual root cause class behind the open-relay incident), #116(d) (GDPR export/erasure doesn't exist at all). Full list with severity in the Known Issues table.
0b. **`alecrae-mta` is currently STOPPED** (Craig-authorized emergency action, issue #105 — was an unauthenticated open relay for 9 days, actively abused). TLS + per-IP throttling are now wired in (issue #107), but the inbound SMTP handler itself is still unfinished placeholder code (`services/mta/src/index.ts` — accepts anything, does nothing further). **Do not restart this service until that pipeline is either finished properly or a decision is made to keep port 25 closed** — this directly supersedes/blocks item 3 below.
1. ~~Craig: Decision 1~~ — **DECIDED 2026-07-13: keep the "158" box (149.28.119.158) as the dedicated mail box** (see Product Decisions Log).
2. ~~Craig: Phase 0 DNS fixes~~ — **COMPLETE 2026-07-13, verified live incl. FCrDNS**: mx1/mx2/smtp A → 149.28.119.158 (grey), MX 10+20, `_spf` TXT, bounce CNAME → smtp, PTR → `smtp.alecrae.com`. NB: `mail.alecrae.com` stays PROXIED (webmail on Jarvis); the MTA identity is `smtp.alecrae.com` (`MTA_HOSTNAME` env).
3. **Phase 1: bring up `alecrae-mta`** on the mail box (`docs/infra/mta-box-setup.md`) — **BLOCKED, see item 0b above.** Once unblocked: test send → Gmail inbox with SPF/DKIM/DMARC pass, run the idempotency check.
4. **Phase 2: deploy the inbound service** (port 25 listener) + test receiving into a provisioned mailbox.
5. **Phase 3: onboard each platform domain** (mail.vapron.ai already verified; repeat the 5-record recipe per platform) + mint per-platform API keys with `messages:send`.
6. ~~Verify the CI typecheck gate actually blocks bad pushes to main~~ — **FIXED 2026-07-20** (Known Issue #66 follow-up): it didn't — main had no branch protection at all. Now enabled: PRs required + 4 required checks, admin bypass intentionally left open.
6b. **CONFIRMED 2026-07-21 (was ❓, now reproduced live):** Craig hit `Error 403: access_denied — alecrae.com has not completed the Google verification process` connecting Gmail. Root cause found: `docs/infra/morning-setup.md`'s own OAuth consent-screen steps said "skip Scopes, skip Test users" — so whichever setup pass created this OAuth client never added Craig (or anyone) as a test user, and the app sits in Testing publishing status (full Google verification is a slower, separate process not needed for a beta with a handful of known testers). In Testing status, ONLY accounts listed as test users can complete the flow — everyone else gets exactly this 403, even though nothing in the app itself is broken. **Doc fixed same day** (test-users step + scopes step no longer skipped, third redirect URI added to the instructions). **Craig still needs to do this by hand in Google Cloud Console** (Claude has no access to Craig's Google account): APIs & Services → OAuth consent screen → Test users → **+ Add Users** → add `ccantynz@gmail.com` (+ any other testers) → Save. While there, also verify the Scopes tab has `gmail.readonly` + `admin.directory.user.readonly`, and Credentials → the OAuth client has all three redirect URIs (`/v1/auth/callback/google`, `/v1/connect/callback/gmail`, `/v1/import/workspace/callback`) — the other two ❓s from this line are now also unverified-live but at minimum documented correctly.
6c. ~~Deploy-drift prevention (Known Issue #78)~~ — **FULLY SHIPPED + VERIFIED LIVE 2026-07-20** on Jarvis (Craig-authorized full deploy + timer install; see issue #78).
7. ~~Fix the 2 data-loss stubs~~ — **FIXED 2026-07-20** (Known Issue #29): both now return an honest `501` instead of silently losing the upload. **New follow-up:** pick a real object-storage backend (Vapron once #83's API key is fixed, or Cloudflare R2 directly — the latter needs Craig's OK on a new `@aws-sdk/client-s3` dependency) and actually wire uploads through it.
8. ~~Wire `email.received` webhook event~~ — **FIXED 2026-07-20** (Known Issue #70). **New follow-up (Known Issue #103):** the Integrations (Zapier/Make/n8n) and Programs features have zero dispatch behind their "email.received" trigger UI — CRUD-only, never fires. Own scoped session (needs a safety pass for programs.ts running arbitrary TS on real mail).
9. **Route-wiring roadmap** per `DEVOPS_TRACKER.md` §3 — **Craig-ordered campaign to close the 40% gap (2026-07-13). Tranche 1 SHIPPED 2026-07-13:** Delegation (12 endpoints, new page), Security Intelligence (12, security-center tabs), Analytics Dashboard (12, analytics tabs), AlecRae Meet (10, new /meet page), Contact Enrichment + CRM (10, contacts panels) — 5 domains, ~56 endpoints wired, plus scope + plural-path backend fixes. **Tranche 2 SHIPPED 2026-07-14:** AI Intelligence (13 endpoints, new /ai-intelligence page), Knowledge Graph (12, new /knowledge page with dependency-free SVG graph), Commitments/Context Intelligence (12, new /commitments page — makes the flagship "AI catches every promise" claim real), Search Intelligence (10, extended /search + wired search-history recording into ai-search.ts), Notification Intelligence (8, extended /notifications). ~55 endpoints. **Security fixes shipped same day:** SQL injection in knowledge-graph `/graph` (raw string-interpolated ARRAY → parameterized `inArray`), cross-tenant dedup leaks in ai-intelligence priority-score + sentiment (added accountId filter — issue #28 class). **Tranche 3 SHIPPED 2026-07-14:** Sentiment Timeline (/sentiment, 8 endpoints, SVG sparklines), Productivity Analytics (/productivity, 8), Attachment Intelligence (/attachments, 9), Scheduling Intelligence + Analytics (/scheduling, ~12 across both routes), Email Hygiene (hygiene page REWRITTEN — old page called a fictional `/v1/email-hygiene/*` API that doesn't exist; now wired to the real `/v1/hygiene/*`, 12 endpoints). **Security fix same day:** productivity leaderboard aggregated across ALL tenants (leaked account IDs + volumes) → scoped to caller's account. **Tranche 4 SHIPPED 2026-07-14:** Inbox power features (labels 6 + bulk-actions 8 + thread-mutes 3 — multi-select, bulk toolbar, labels manager, mute; surgical edits to inbox), Compose power features (compose-assist 2 [it's actually the B7 calendar-slot assistant, not writing help] + spellcheck 5 + recall 4), Signatures (new /settings/signatures, DB-backed — replaces the old localStorage-only widget the settings page rendered), Contact Groups (contacts page tab, 7), Programs (new /programs — Programmable Email B1, sandboxed TS snippets, 8). **Scope fix same day:** `grammar:read` added to baseline scopes — BOTH the new spellcheck AND the pre-existing compose grammar-check (issue #45, thought wired) were silently 403ing without it. Added `programs: "pro"` to FEATURE_PLANS. **Tranche 5 SHIPPED 2026-07-14:** SSO/SAML config (new /settings/sso, 4 endpoints), E2E Encryption (new /settings/encryption — client-side Web Crypto keygen, private key in IndexedDB per zero-knowledge, 3 endpoints), Push Notifications (completed on /notifications — old page called wrong `/v1/push-notifications/*` paths that 404'd; real mount is `/v1/push`, 6 endpoints), Semantic vector search (extended /search, 4 new endpoints distinct from hybrid search). **Backend fixes same day:** (a) SSO config endpoints required an SSO-issued token (issuer `alecrae-sso`) so admin session tokens 403'd — rerouted `/v1/sso/config` through standard authMiddleware + requireAdmin(); (b) `encryption:read`/`write` added to baseline scopes (were missing → all encryption endpoints 403'd). **Genuinely UI-less infra routes documented** in `docs/audits/ui-less-routes.md` (health, uptime, realtime, fbl, status — 6 endpoints, correctly excluded from product coverage). **Coverage-tracker caveat added:** the tracker under-counts because newer clients build URLs as `${API_BASE}${PREFIX}${path}` (prefix in a constant) so full paths never appear contiguously — real reachable coverage is materially above the reported number.

### 40% GAP CAMPAIGN — COMPLETE (2026-07-13→14)
Five tranches, 25 feature domains wired, ~200 endpoints. Reported endpoint coverage 46% → ~75%+ (true figure higher per the tracker caveat). Every tranche shipped green (typecheck + lint + build + coverage) and pushed. Bonus: unblocked CI (lint+typecheck red 9 days), fixed 1 SQL injection + 3 cross-tenant leaks + 4 scope-gap 403 classes discovered while wiring. Remaining unwired are mostly tracker false-negatives (voice/onboarding/workspace-import have pages) + the documented infra routes. Backend stub-quality findings logged as issues #73–76 for a future backend-hardening pass.
10. **Alerting + staging design** (Known Issue #72) — dedicated session; don't architect inline.
11. **Set up Stripe** live keys + webhook URL → api.alecrae.com/billing/webhook (Craig).
12. **Disable GitHub Default Setup CodeQL** — Settings → Code security (Craig).

---

## 🌀 FLYWHEEL BACKLOG — Make the moat visible (added 2026-05-08)

The platform's moat is the AI flywheel — every user action makes every AI feature better, and the data that compounds cannot be cloned by Gmail or Outlook. The signals + learning cycles are wired in `.ai-flywheel/config.json`. What's missing is **visibility, virality, and instrumentation**. These are the highest-leverage next builds AFTER the email-sending stack is operational:

| # | Item | Why | Effort |
|---|---|---|---|
| F1 | **"Your AlecRae" page** — voice-profile confidence over time, drafts accepted, time saved, words AI has learned | Without it, users can't see the wheel turning, which kills retention narrative + marketing screenshots | ~2 days |
| F2 | **Voice-primed referral loop** — "Invite a contact → they get 3 months free, you get 1 month free, AND their voice profile is primed from the emails you've already exchanged" | Ties acquisition to the moat; Gmail/Superhuman literally cannot ship the parenthetical | ~1 week |
| F3 | **AlecRae↔AlecRae network features** — real-time read/draft state, "Sarah is replying", presence in compose, calendar slot proposals that auto-resolve when both ends are on AlecRae | Turns AlecRae from "a client" into "a platform" — each new user makes existing users' product better | ~2 weeks |
| F4 | **Flywheel instrumentation** — wire `.ai-flywheel/config.json` signals to ClickHouse + surface weekly RPM in `/admin` | Right now the wheel turns but we can't measure it. Need weekly compose-acceptance, triage-accuracy, voice-edit-distance trend lines | ~3 days |

**Build order:** F4 → F1 → F2 → F3. Instrument first so we know the wheel is real, then make it visible, then tie it to virality, then to network effects. None of these block launch — but all four together are what turns AlecRae into a $2.5M MRR exit instead of a $10M lifestyle business.

---

## 📊 SESSION PROTOCOL

### At the START of every session:
1. Read this file (CLAUDE.md) end to end
2. Check the "Known Issues" section
3. Check the "Next Actions" section
4. Confirm what you're working on aligns with the build plan
5. If unclear, ask Craig

### At the END of every session:
1. Update the "Build Status" section with what got done
2. Update the "Known Issues" section with anything discovered
3. Update the "Next Actions" section with what's next
4. Update "Last updated" (date + time, UTC) at the bottom — run `bun run docs:check`
5. Commit and push everything
6. Leave the codebase in a runnable state

### When starting a NEW build:
1. Run the Pre-Flight Checklist
2. Build it
3. Run the Post-Build Checklist
4. Commit with conventional commit message
5. Push to development branch
6. Update CLAUDE.md

---

## 📝 ARCHITECTURE DECISION RECORDS (ADRs)

When making a significant architectural decision, document it in `docs/adrs/NNNN-title.md` with:
- **Context:** What's the problem?
- **Decision:** What did we decide?
- **Alternatives:** What did we consider?
- **Consequences:** What does this mean going forward?
- **Status:** Proposed / Accepted / Deprecated / Superseded

Major past decisions:
- ADR-0001: Use Neon over Supabase (serverless economics)
- ADR-0002: Use Cloudflare over Vercel (cost + edge presence)
- ADR-0003: Bun over Node.js (speed)
- ADR-0004: Hono over Express (4x faster, edge-compatible)
- ADR-0005: Tailwind over CSS-in-JS (zero runtime cost)
- ADR-0006: Drizzle over Prisma (smaller bundle, edge-friendly)
- ADR-0007: Claude over GPT (better instruction following, faster improvement)

---

## 🎯 THE COMPETITIVE MANDATE

**We are not building "another email client." We are building the LAST email client.**

Every feature must answer: "Why would someone switch from Gmail/Outlook for this?"

If the answer isn't compelling, don't build it. If it is, build it 10x better than the competition.

**Examples of compelling answers:**
- "AlecRae's grammar agent replaces Grammarly, which costs $30/mo. AlecRae includes it free."
- "AlecRae's dictation lets you reply by voice with email-aware commands. Dragon is dead. Nothing else does this."
- "AlecRae's email recall actually works. Outlook's is theater."
- "AlecRae's AI learns YOUR writing style. Gmail's AI sounds like a robot."
- "AlecRae runs on YOUR computer's GPU for free AI. Gmail charges $30/mo for Gemini."
- "AlecRae's commitments tracker catches every promise made in email. Gmail catches none."
- "AlecRae unifies Gmail + Outlook + Yahoo + iCloud in one inbox. Superhuman is Gmail-only."

**Examples of bad answers (don't build):**
- "It would be cool"
- "Other apps have it"
- "It's a small change"

---

## 📅 STATUS

### 🚨 PRODUCTION DEPLOYMENT STATE — READ FIRST (updated 2026-07-13)

**Production is the Jarvis box at `66.42.121.161`** — access via Tailscale SSH
(`ssh root@jarvis`, identity-based, no keys; if it breaks check `tailscale status`
locally first). The old Vapron box `149.28.119.158` is **DEPRECATED for compute**;
mail DNS (SPF/PTR) still references it pending the mail-box decision in
`docs/infra/multi-platform-mail-plan.md`.

- **Box layout:** Coolify/Traefik owns 80/443 (route file
  `/data/coolify/proxy/dynamic/alecrae.yaml`: web/mail/www → `10.0.1.1:4200`,
  api → `10.0.1.1:4100`). systemd services `alecrae-api` (:4100) and
  `alecrae-web` (:4200) bind `0.0.0.0`; env lives in `/opt/alecrae/.env` —
  **Bun auto-loads `.env` and it silently OVERRIDES systemd unit env** (this
  caused the July outage; don't set the same var in both places).
- **Drift incident (found + fixed 2026-07-14):** the box served a **July-4
  build for 10 days**. Cause: an unpushed local hotfix (`e480456`, PlanBadge
  colors) made the box diverge, so the deploy ritual's `git merge --ff-only`
  refused to pull — and nothing alerted on it. Every wiring tranche and fix
  merged 07-04→07-13 was invisible in production (this is why "fixed" things
  kept looking broken). Remedied via `git reset --hard origin/main` (hotfix
  was already re-made in main). **Rule: never commit on the box; if the
  ritual fails on divergence, that's the signal to stop and reconcile.**
- **Verified live 2026-07-13:** alecrae.com, mail.alecrae.com, and
  api.alecrae.com/health all return 200. `status.alecrae.com` is 503 (no app
  deployed, no route — Known Issue #71).
- **Deployed 2026-07-20 (Craig-authorized):** box was found 31 commits
  behind `origin/main` (predating this session) while installing issue
  #78's drift-check timer. Ran the full deploy ritual — `git pull
  --ff-only`, `bun install`, `db:migrate` (through migration 0007),
  web rebuild with `NEXT_PUBLIC_COMMIT_SHA`, restarted `alecrae-api` +
  `alecrae-web`, health-checked both. Now at `614ddff`, confirmed via
  `api.alecrae.com/health` and `mail.alecrae.com/api/version`. The
  `alecrae-drift-check.timer` (15-min, root, see issue #78) is live and
  `/v1/health`'s `deployDrift` is populated — this class of silent
  staleness now self-reports going forward.
- **Outage 2026-07-07 → 07-11:** three stacked failures (missing Traefik route,
  services disabled, 127.0.0.1 bind) — postmortem:
  `docs/postmortems/2026-07-07-alecrae-503-outage.md`.
- **"Deployed" means:** merged to main **AND** pulled+built on the box
  (`scripts/box-deploy.sh` or the manual ritual). Anything merged after the last
  pull is NOT live.
- **Mail services** (`alecrae-mta` outbound worker, `services/inbound` MX
  listener) are **not running anywhere yet** — phased bring-up is the mail plan.
- **Deployed 2026-08-06 (Craig-authorized):** box moved `4260525` → `f9d1528`
  (main, 100 commits — the whole #120–#163 audit campaign, which had been
  invisible in production since 2026-07-19). Ran the full ritual: pull,
  install, `db:migrate` (0008–0011 applied — see issue #155, this is the
  deploy that would have skipped three of them), web rebuild at the deployed
  SHA, restart both units, health-verified from outside. `API_URL` added to
  `/opt/alecrae/.env` first (issue #140 prerequisite — the API refuses to boot
  without it) and the issue #104 stray space fixed in the same pass.
  `/v1/health` correctly reports `degraded` for the MTA: it is stopped, and
  the probe now says so instead of reporting `ok`.

**Last updated:** 2026-08-06 07:20 UTC
**Shipped 2026-08-05 — full production-readiness audit (Craig: "a serious audit, no code or webpage unchecked", website-in-sync-with-code):** Five parallel deep audits — doc-vs-code verification, frontend↔backend contract drift, the never-audited surfaces (desktop/mobile/SDK/five orphan services/CI/DB), the live public website, and a ground-truth deploy check. The doc claims held up far better than history (9/10 recent "FIXED" rows verified with wiring + tests), but the surface sweep found six new HIGH/CRITICAL defects, two of which detonate on the next deploy. Fixed everything bounded, with tests: **#155** (CRITICAL — migration journal out of order, next `db:migrate` silently skips 0008/0009/0010, reverting the dedup index, dropping `provider_message_id`, breaking Stripe webhooks; no gate saw it); **#156** (CRITICAL — RCE via `POST /v1/scripts/:id/test`'s `new Function()` fake-sandbox, reachable by any signup since `register` grants owner; endpoint 501'd, executor disconnected + structurally guarded); **#157** (HIGH — the marketing send path was still unsatisfiable after #152 because the compliance gate hardcoded unsubscribe/address to false; now read from the real body); **#158** (HIGH — outbound virus scanner reported `clean:true` for files it never scanned, the #141 class, live in the send path; unscanned is now honestly not-clean). Logged in full and deliberately **not** half-fixed: **#159** (warm-up bounce/complaint auto-pause has zero callers — the ramp advances blind and two of three send paths ignore the hard pause) and **#160** (DKIM private keys stored plaintext under comments claiming encryption). Smaller fixes: the meaningless `mta:ok` health probe (now verifies a worker consumes the queue — the #149 silent-vanish mode); the attachments "virus scanned" header; **nine legal pages including the CCPA-required California-notice and Do-Not-Sell that bounced logged-out visitors to /login**; dead footer links; the undefined `accent`/`text-text-*` colour utilities (~109 uses); `box-deploy.sh` restarting the emergency-stopped MTA as a side effect; the service-drift checker blind to IPv6 wildcard binds; route-coverage.md regenerated. Opened **draft PR #92** so the four required CI checks validate the branch for the first time — all pass. **Standout operational fact surfaced: production is 115 commits stale (runs 07-19 code), so the entire post-07-20 security campaign — open relay, open redirect, header injection, E2EE, the send-path 422 blocker — is invisible in the running build; a deploy is now the highest-leverage single action.** The live-site audit also found ~12 false/inoperable public claims and legal pages naming an unverifiable US corporation with a 99.99% SLA nothing supports — all Boss Rule #9/#10, logged for Craig, none changed. 12 new tests. typecheck 36/36, lint 0 errors, apps/api 444, monorepo green.
**Shipped 2026-08-04 — business email as the driving priority:** Craig has many platforms needing an organisation each with mail on its own domain, which reframes the critical path as send **and receive** on customer domains. Traced that path in code rather than from the docs. Two corrections to the received wisdom came out of it: `services/inbound` is a **complete, bootable receive pipeline** (the "unfinished placeholder" warning everyone repeats is about `services/mta`'s duplicate receiver, a different file), and the real blocker was simply that **no UI anywhere created a mailbox** — provisioning `info@customer.com` required a hand-written curl. Built `/mailboxes` (create, edit, pause, delete, gated on verified domains as the API requires) plus `PATCH /v1/mailboxes/:id`. Then closed **#151**, extracting the pre-send stack into one shared gate — which surfaced the session's worst finding: **#152, `POST /v1/messages` returned 422 for every non-transactional email**, because everything not explicitly transactional was classified as a marketing campaign and the real compliance engine produces ten critical violations for one. It had been invisible because the test suite mocks that engine. Also **#153** (received spam filed into the inbox), the removal of an agent hook that put **every AI-drafted reply on the real send queue with no user approval**, and **#154** (the mailto unsubscribe never sent anything and reported success). 19 new tests, deliberately including ones that run the real compliance engine rather than a stub. apps/api 441/441, monorepo 48/48, typecheck 36/36, lint 0 errors.
**Shipped 2026-07-29 (iterations 5-7):** Closed the E2EE fabrication (issue #129 — the server minted its own keypair while the browser kept an unrelated one, so nothing encrypted to a user could ever have been decrypted; plus a no-KDF passphrase-as-raw-AES-key and a settings page claiming mail was "encrypted automatically" when nothing encrypts anything). Then the last two broken journeys: Security Center Overview (two endpoints simply did not exist, so the default tab 404'd for every user — now built over real threat/phishing rows with **no invented score**; the Trust Settings toggles were removed rather than backed, because they controlled nothing) and Integrations (API-key generation 422'd every time; Connected Apps removed rather than rebuilt, since the connector API behind it has no dispatcher per #103). **All seven journeys the 2026-07-22 audit listed as broken-today are now closed, and the fabricated count is 0.** 20 new tests. apps/api 298/298, monorepo 48/48, typecheck 36/36.
**Shipped 2026-07-29 (iteration 4):** Two more spam-track items. (1) Found the root cause behind the open relay: `services/mta` is the OUTBOUND service, but it shipped its own port-25 SMTP receiver — duplicating `services/inbound`, which already has the real pipeline AND its own relay control — with a handler that logged an accepted message and discarded it. That duplicate is what got abused. The receiver is now opt-in and OFF by default (issue #128), so the outbound service opens no listening socket at all; the runbook that told operators to deploy it is corrected. (2) Closed the second half of issue #117: `/v1/auth/*` had only a per-IP rate limit, blind to a botnet trying one password against one account from a thousand addresses. Per-account and per-IP FAILURE counters now catch both credential stuffing and spraying — relevant to spam because a taken-over account is the usual way a legitimate sender starts emitting it, i.e. the upstream cause of what send-anomaly.ts catches downstream. Counters hash the email so Redis cannot be read as a customer list; lockouts always expire; success clears the account counter but deliberately NOT the IP one. 10 new tests. apps/api 280/280, MTA 275/275, monorepo 48/48.
**Shipped 2026-07-28 (iteration 3) — the open relay was never actually closed:** Auditing the RCPT TO handler found that Known Issue #105, recorded as "FIXED same day", was not fixed at all: the July remediation stopped the service and added TLS + per-IP throttling, neither of which is a relay control. `handleRcptTo` still accepted ANY recipient and answered 250 OK. **The only thing preventing a repeat of the 9-day abuse was that the service is switched off** — restarting it would have restored an open relay. Now closed and failing closed (issue #127). Also closed issue #82(a): the envelope sender was the customer's own address, so async bounce DSNs went to their inbox and never reached suppression — we would keep mailing addresses already reported dead, the clearest spammer signature there is. VERP return path now used on both delivery paths, with per-customer bounce domains chosen so SPF aligns under DMARC alongside DKIM. Inbound DSN attribution upgraded from a recency heuristic to exact. 22 new tests; MTA suite 275/275, monorepo 48/48.
**Shipped 2026-07-28 (iteration 2) — spam protection raised to top priority by Craig:** "it's critical that we have the best spam protection so we are not banned at all." Audited the whole outbound path. The existing pre-send gate is genuinely strong (suppression, CAN-SPAM/GDPR/CASL, warm-up hard-enforcement, quota, attachment scanning, DKIM, per-ISP throttles) — but two holes would have made *us* look like the spammer. **(1) Header injection, CRITICAL (issue #124):** `subject` and recipient display names went straight into header lines with no CRLF check, so `"Hi\r\nBcc: victim@example.com"` added a hidden recipient to mail we DKIM-sign and relay from our own IP. The validator built for this threat only ever guarded the `headers` object. **(2) Nothing read the message (issue #125):** a complete, tested `SpamClassifier` sat in the repo with zero callers — the same built-but-never-wired pattern this audit keeps finding. Both fixed, plus send-volume anomaly detection (issue #117's first half) comparing each account to its own weekly baseline rather than a flat ceiling, and the connected-account fast path (issue #126) which bypassed every one of these — a concern because spamming through connected Gmail risks Google suspending our OAuth client, breaking Gmail for every customer at once. Two items deliberately left for Craig rather than changed silently: unsubscribe suppression on the connected path (list-scoped consent vs. blocking a personal reply) and quota on that path (billing behaviour, Boss Rule #10). 27 new tests, all deliberately covering the false-positive direction too — a spam control that blocks legitimate customer mail is its own outage. apps/api 270/270, monorepo typecheck 36/36.
**Shipped 2026-07-28 — brutal full-code production-readiness audit (in progress):** Craig asked for a no-patchwork audit of all code, explicitly distrusting the markdown, targeting consumer + commercial readiness. **The first finding was that main's test suite was red and had been for some time: 31 failing tests across 9 files.** Root cause was a packaging defect (issue #120) that made `@alecrae/ai-engine` resolvable only by Bun, which had silently disabled all executable test coverage of the core send path — recorded previously as an "unrelated environmental gap". Fixing it took apps/api from 180 passing/31 failing to 226/0. Then closed four of the seven journeys the 2026-07-22 audit lists as broken-today (AI Triage, Translation, Files, Drafts), each of which turned out to have a deeper root cause than the journey audit described — a component contract rather than a page bug in two cases (`PlanGate`, `ComposeEditor.onSaveDraft`). Found and fixed three defects the journey audit's method could not see: unenforced storage quota (issue #121 — unbounded object-storage spend), AI quota charged for non-AI reads, and Vapron transport tests still asserting the replaced transport (issue #123). Doc corrections: known issue #8 was **wrong** (the `draft` enum value has always existed — and that wrong note is the likely reason Drafts was built against `"queued"`), and the stack table claimed Biome while the repo runs ESLint + Prettier. Full monorepo green: typecheck 36/36, tests 48/48 tasks, apps/api 243/243. **Not yet done — see the journey audit's build plan:** 3 broken journeys remain (Achievements, Security Overview, Integrations), Phase 2's fabricated Encryption claim, and Phase 3's systemic dispatcher gap.
**Shipped 2026-07-22 — full journey audit + build plan:** Craig's frustration was explicit and specific: features get added but nobody follows through on what actually drives them (the DNS/domains issue he hit live is the proof), and existing "audits" have relied on markdown docs and grep-based coverage tools that don't verify real behavior — he asked for a fresh, ground-truth audit of every tab, live-verified, producing a real build plan. Ran a 49-agent parallel workflow, one per dashboard nav tab, each explicitly instructed to distrust CLAUDE.md's own claims and trace the real frontend→API→DB/AI path independently. Result: `docs/audits/2026-07-22-full-journey-audit.md` — 14 real, 22 partial, 7 broken-today, 5 never-wired, 1 outright fabricated (Encryption's "auto-encrypted" claim: the server generates an unrelated keypair instead of using the client's real key, so nothing "encrypted" to a user could ever be decrypted by them). The standout finding: 11 separate features (Knowledge Graph, Hygiene, Productivity, Sentiment, Attachments, Scheduling, Scripts, Programs, Auto-Responder, A/B Testing, Mail Merge) all promise to "run automatically on new mail," and none of them do, because nothing in the real email-ingestion pipeline ever dispatches to any of them — one real fix, not eleven. Craig's own Domains blocker was root-caused live in the same session: adding a domain never touches DNS at all, and the one auto-config path relevant to his setup (Vapron, whose nameservers his domain actually uses) is broken on the same unverified transport bug fixed for email/AI/storage earlier the same day — deliberately left unfixed pending real Vapron DNS docs, which only Craig can obtain. This audit doc is now the authoritative status source for these 49 journeys per the Ground-Truth Sources table — supersedes prior "FIXED" claims for anything it covers, and is checked into git so it can't be lost the way the 2026-07-19 audit was.
**Shipped 2026-07-21:** Continued straight through the open audit backlog with no check-in pauses (standing instruction — see memory). Closed issue #116(c): dunning state transitions (payment failed / downgraded / recovered) now actually email the customer, gated behind an opt-in env var, with 13 new tests covering both "fires exactly once" and "correctly silent" cases (a voluntary cancel never gets a false "payment failed" email; a mid-cycle retry doesn't re-send). Separately, Craig supplied working Vapron platform API docs + a live key, which surfaced that issue #83 was bigger than a wrong key prefix — `lib/vapron.ts`'s entire email/AI/storage transport had been built against a guessed, unpublished tRPC shape that never matched the real REST API. Rewrote that transport against the real docs (DNS methods left untouched — no docs supplied for those, not re-guessed) and used the new `storage.getUploadUrl()` to fully fix issue #29's file-upload and voice-message stubs (previously an honest 501; now a real presigned-upload flow, honest 502/503 on failure, never a fabricated URL). Could not live-verify against the real API from this sandbox — outbound calls carrying a live secret are blocked by the permission classifier by design; verification is pending either Craig running the curl himself or the next box deploy. All changes typecheck clean across all 36 workspace tasks.
**Shipped 2026-07-20 (continued) — audit backlog, round 2:** Fixed 9 more of the findings logged as issues #108-119 (see those rows for full detail), same standard as round 1 (real tests, full typecheck, individually committed/pushed): applied `ssrf-guard.ts` to the 3 real gaps found (webhook delivery, integration test-send, transcription — extended `safeFetch` to support POST+body); closed delegation's cross-workspace grant + cross-account inbox leak, and made push-subscription re-parenting explicit; verified migration 0008's unique constraint against live production data (0 duplicates, confirmed safe); surveyed and fixed `lib/ai.ts`'s prompt-injection framing gap (all 4 callers are single-turn, safe to apply the same fix as `ai-writing.ts`); wired the dead-code bounce classifier into real delivery events; added service/port drift detection (`scripts/check-service-drift.sh`) — the actual gap class behind issue #105, since #78 only ever caught code-version drift; fixed the incident-response runbook's undocumented JWT_SECRET-rotation side effect; closed the duplicate-Stripe-subscription risk on repeat checkout; fixed Outlook sync to handle delta-query deletion markers instead of mis-parsing them as garbage messages. 4 findings remain open (#110 web token storage — architectural, #114 HTML rendering pipeline — deliberately not rushed given the stakes, #116(b)(c)(d) multi-currency/dunning-notification/GDPR export, #117 observability — blocked on #72's alerting pipeline not existing yet).
**Shipped 2026-07-20 (continued) — security incident + comprehensive audit (issue #107):** While checking mail-plan Phase 1 status, discovered live (not via code review) that `alecrae-mta` had been running as an **unauthenticated open SMTP relay for 9 days**, actively abused for spam (issue #105 — Craig-authorized emergency stop, confirmed no spam actually delivered anywhere via the real outbound queue + a clean blocklist check). In direct response, ran a 6-agent parallel audit against both a user-supplied robustness checklist and an independently-generated one (network exposure, multi-tenant boundaries, MIME/deliverability, AI-specific risk, data integrity/DR, billing/observability) — not another code-correctness pass, this one specifically hunting for the class of gap that let the incident happen unnoticed. Per Craig's explicit "proper DevOps and full-stack development, not patching" directive, fixed the 14 most severe and best-bounded findings same-day, each with real regression tests, typecheck clean, individually committed and pushed: three cross-tenant IDOR/leak bugs (`chat.ts` zero auth, `account.ts` cross-tenant passkey deletion, `gamification.ts` leaderboard leak); wired the MTA's already-built TLS manager (mail was transiting in plaintext) and added per-IP abuse throttling (the direct enabler of #105); fixed the offline-compose banner's false "auto-queues" promise (three separate breaks, all fixed); wired one-click unsubscribe to real suppression (was CAN-SPAM-exposed despite perfect headers); wired the already-built-and-tested DSN bounce processor (async bounces never suppressed anything before this); closed an email-dedup TOCTOU race with a real DB constraint and made Gmail-reported deletions actually apply locally (needed a new `providerMessageId` column — no correlation existed at all); added prompt-injection framing across all of `ai-writing.ts`; added PII redaction before the automatic AI-triage call; added a Redis-independent AI spend circuit breaker; fixed account deletion to honor this file's own 30-day soft-delete rule (Forbidden List #13 — schema support already existed, unused); added Stripe webhook idempotency. Remaining findings (13 more, ranging CRITICAL-adjacent to LOW) logged individually as issues #108–#119 rather than silently dropped — see those rows for the full detail CLAUDE.md's "no silent caps" standard requires. Full itemized fix list with file paths and commit hashes is in issue #107's row above; this entry is the narrative version.
**Shipped 2026-07-20 (continued) — production deploy:** Craig authorized connecting to Jarvis over Tailscale to finish issue #78 for real. Found the box 31 commits behind `origin/main` (predates this session), so ran the full deploy ritual first: `git pull --ff-only`, `bun install`, `db:migrate` (through migration 0007), web rebuild, restarted `alecrae-api` + `alecrae-web`, health-checked both — confirmed live via `api.alecrae.com/health` and `mail.alecrae.com/api/version`. Then installed the `alecrae-drift-check` systemd timer, which surfaced two real bugs in the drift-check script itself (fixed, redeployed, reverified): the status file was written `600 root:root` and unreadable by the `alecrae-api` service (runs as a different user, so `deployDrift` silently read `null`), and a shared fixed `/tmp` path for fetch-error capture broke across users. Also confirmed live: only `root` has GitHub SSH access on Jarvis, and found a real (low-severity) formatting bug in `/opt/alecrae/.env` — logged as **Known Issue #104**. `/v1/health`'s `deployDrift` is now genuinely populated in production.
**Shipped 2026-07-20:** (1) Deploy-drift detection (Known Issue #78) — `/health` and `/v1/health` now report the running process's own git commit SHA (`apps/api/src/lib/deploy-info.ts`), and a new scheduled script (`scripts/check-deploy-drift.sh`, installed on the box as a 15-minute systemd timer per `docs/infra/deploy-drift-check.md`) compares box HEAD to `origin/main` and writes a status file the API reads back — a drifted box now flips `/health`'s status to `degraded` instead of silently serving stale code for 10 days like it did before. Code + docs are done and pushed; the systemd timer itself still needs to be installed on Jarvis during the next box session (an on-box step, not code). (2) Known Issue #66 follow-up (Craig-authorized) — discovered main had **zero GitHub branch protection**, so the CI red-for-9-days incident wasn't a CI bug, it was that nothing was ever wired to block a bad push. Enabled branch protection on main: PR + 4 required checks (Lint, GateTest Quality Gate, Schema/Migration Drift, Route Coverage) must pass before merge, `enforce_admins` deliberately left off (Craig's call, emergency bypass for admins). **This changes the standing workflow: direct `git push origin main` no longer works for non-admins going forward — everything now needs a branch + PR + green CI + merge.** (3) Known Issue #29's two data-loss stubs, Craig-authorized honest-degradation fix — investigated first and found zero working object-storage integration anywhere in the repo (Vapron's client only has listBuckets/createBucket; the documented R2 env vars are read by nothing; a real fix needs a new, unapproved dependency). `POST /v1/files/upload` and `POST /v1/voice-messages/record` + `/:id/reply` now return `501 storage_unavailable` instead of silently accepting an upload and losing it. Picking a real storage backend is a separate follow-up. (4) Known Issue #70 — wired a real `email.received` webhook event: `storeReceivedEmail()` now writes an `events` row and enqueues delivery through the existing HMAC-signed webhook-dispatcher (migration `0007_numerous_valkyrie.sql` adds the enum value). Found and fixed a real bug in the same code path: the public webhook-subscription schema used bare event names (`"opened"`) while the DB/dispatcher use dotted names (`"email.opened"`), so any webhook that filtered to specific events could never match — fixed both `types.ts` and the `/settings/developer` UI to the dotted form. Tracing this also surfaced a bigger, previously undocumented gap — logged as **Known Issue #103**: the Integrations (Zapier/Make/n8n) and Programs features both advertise an `email.received` trigger in their UI with zero dispatcher behind it (CRUD-only, `runProgram` only ever runs from its own test endpoint) — a real feature build for a future session, not a quick fix, especially since Programs means running user-authored TS against every inbound email and deserves a safety pass first.
**Shipped 2026-07-19:** Full six-domain code audit (Gmail/Workspace import, core journeys, AI quality, design, production infra — no shortcuts, docs ignored in favor of reading the actual code) surfaced a much larger gap between "code-complete" and "works" than this file previously reflected; see issues #84-87 below and the audit's "Big Five" for the worst of it. Same session, fixed the five most severe: (1) the Gmail/Outlook sync accountId bug that made every connected mailbox permanently empty (issue #79, root cause was worse than previously documented); (2) added a background mailbox re-sync worker + send-path token refresh, since nothing ever kept mail in sync after the first connect; (3) stopped security-intelligence.ts from fabricating phishing/reputation data with `Math.random()` on a shipped UI (issue #84); (4) made workspace invitations actually reachable end to end — auth-wildcard bug, no password/session on accept, no way to surface the invite link without email sending (issue #85); (5) added server-side plan-tier enforcement for Pro-gated AI routes, which was previously UI-only (issue #86), and fixed `/v1/ai-intelligence` having no auth middleware at all in the process (issue #87). All changes typecheck clean across all 26 workspace packages (`bun run typecheck`).
**Current phase:** Phase 1 — Beta Launch in Progress. Web/API live on Jarvis (66.42.121.161); mail stack bring-up is the critical path.
**Current focus:** Continuing straight through the audit's roadmap per Craig's standing instruction not to pause for check-ins on directed work. 23 fixes shipped this session (issues #84-102 above, full list) — the Big Five, then straight through most of the rest of the audit roadmap: real message state (read/starred/folder), snooze, undo-send, token encryption, the AI-rules execution engine, semantic-search indexing, the local WebGPU grammar engine, every fabricated `Math.random()` "AI" surface rebuilt on real Claude calls, a comprehensive scope-grant audit that unblocked 8+ feature areas silently 403ing for every session, dictation wired end-to-end, and AI usage quotas (previously unbounded spend). What's left is genuinely large net-new feature work rather than audit-finding fixes: the Google Workspace import UI (needs a new admin-OAuth wizard + per-user provisioning flow), the outbound email design/HTML framework (Tiptap install + a new email-template package — the owner's stated #1 priority, currently zero infrastructure), the `smart_label_rules` half of the rules-execution gap (a differently-shaped table from `email_rules`, not covered by #93), and password reset (blocked on transactional email actually sending — see issue #83). Each warrants its own scoped session rather than folding into this stretch. Full findings + prioritized roadmap in the session's audit artifact — not yet transcribed into a docs/ file. Mail plan unchanged: Phase 1 (MTA bring-up on 158) is next on the mail track.
**Build completion:** TIER 1-4 (36/36) + 7 bonus + 31 advanced (S10/10 + A7/7 + B8/8 + C6/10) + 20 expansion (Tier 5) + 9 platform (Tier 6) + 6 intelligence (Tier 7) + 6 deep AI (Tier 8)

**Next review:** Before any major architectural change, before any production deployment, at the start of every session.

---

## ⚖️ THE BIBLE RULE (REPRISE)

**This file is the source of truth.**

If something contradicts this file, this file wins.
If you don't know what to do, this file tells you.
If you want to change this file, Craig has to approve it.
If you ship something not in this file, you broke the rules.

**No scatter-gun. No drift. No "just this once."**

**AlecRae dominates or AlecRae dies. There is no second place.**


---

## 🚀 FEATURE STATE — SUMMARY

Tier S (10/10), Tier A (7/7), Tier B (8/8) all DONE; Tier C polish/trust 6/10 (C4 SOC 2, C5 GDPR DPA workflow, C6 bug bounty, C7 public roadmap NOT STARTED — process tasks, not code). The full feature tables, competitive snapshot, and completeness matrix are archived in `docs/archive/claude-bible-archive-2026-07-13.md`. For what's actually reachable in the product today, trust `docs/audits/route-coverage.md` over any checklist.
## 🎯 CRAIG'S CONFIRMED ACCOUNTS

- ✅ **Apple Developer account** — DONE
- ⏳ **Google Play Developer** — needed for Android
- ✅ **Domain** — alecrae.com confirmed
- ⏳ **Stripe account** — needed before charging
- ⏳ **Anthropic API key** — needed for AI features in production
- ⏳ **OpenAI API key** — needed for Whisper transcription
- ⏳ **Google Cloud project** — for Gmail OAuth
- ⏳ **Microsoft Azure project** — for Outlook OAuth

---

## 🚀 FASTEST PATH TO LIVE URL

**Production is the Jarvis box at `66.42.121.161`.** Deployment process:
1. SSH to box: `ssh root@jarvis` (Tailscale SSH — no keys; check `tailscale status` if it fails)
2. Pull and migrate: `cd /opt/alecrae && git pull --ff-only origin main && bun install && bun run -C packages/db build && bun run db:migrate` (or run `scripts/box-deploy.sh`)
3. Restart: `sudo systemctl restart alecrae-api alecrae-web`
4. Verify: `curl https://api.alecrae.com/health` (Traefik route: `/data/coolify/proxy/dynamic/alecrae.yaml`)

Full checklist (env vars, Google OAuth, DNS, Vultr PTR): **`docs/infra/morning-setup.md`**

Note: Vercel is fully removed as a deployment target. The Vercel GitHub App must be uninstalled from repo settings (it fires on every push and fails, creating noise in CI).

---

## 🏗️ THE BIGGER PICTURE — ALECRAE AS FLAGSHIP

Craig is also building a **Render+Vercel+AI hybrid platform** (the "Back to the Future" infrastructure). AlecRae will eventually deploy on this platform — making AlecRae both:
1. **A standalone product** that generates revenue
2. **The flagship reference app** that proves the underlying platform works

This is why we move with discipline: every architectural choice in AlecRae informs the platform underneath. We don't build AlecRae in a way that requires the platform to ship first — AlecRae deploys to Cloudflare today, and migrates to the new platform when it's ready, with zero rewrites needed (because the new platform supports the same primitives).



---

# APPENDED 2026-08-07 — Spine audit session (fixed issues #164–#165, full record)

> **Last updated:** 2026-08-07 02:30 UTC (this appended section; the snapshot above is frozen)

## Session narrative — 2026-08-07 spine verification ("run checks through our spine")

Craig asked for comprehensive checks of the core ("spine") for stubs, broken code, and efficiency.
Five tracks ran: full test suite forced-uncached (48/48 tasks, 0 cached, all green — apps/api 450,
MTA 275+), typecheck 36/36, lint 0 errors, plus three parallel sweeps: stub/fabrication hunt,
built-but-unwired importer mapping, and an end-to-end producer↔consumer contract trace of both
mail chains. The send chain verified structurally sound (queue names, `{email:{…}}` job shape,
DKIM/suppression/warm-up/VERP all present and matching). The receive chain had six real breaks —
found in the service that has never run anywhere and is next to deploy for business email. The
sweeps also surfaced a fresh fabricated-output batch (logged #166), a dead/duplicate-module set
(#167), and wiring gaps. Everything bounded was fixed same-session by three parallel workstreams
with strict file ownership, each with regression tests.

## Issue #164 — FIXED 2026-08-07: inbound receive path had six breaks + three traps (bundle)

**Findings (all verified in code, none reachable in production only because `services/inbound`
has never been deployed):**
- **(a) Defer silently accepted:** the filter pipeline returns `defer` when any stage throws
  (SPF DNS blip, DKIM verifier error, AI failure), and `index.ts` branched only on `reject` — so
  a filter failure delivered unfiltered mail to the inbox with `250 OK`, the opposite of the
  pipeline's own fail-closed comment. Same gap on both HTTP ingest paths.
- **(b) Reject answered 451 + oracle:** every throw became `451 Temporary failure: <reason>` —
  permanently-rejected spam was retried by senders for days, and the verbatim reason (spam score,
  stage) was a filter-tuning oracle on the wire.
- **(c) Multi-recipient dedup crash:** `emails` has a unique index on (accountId, messageId);
  delivering To:+Cc: on one account inserted the same pair twice → throw → 451 → the sender
  redelivered the first copy forever.
- **(d) STARTTLS fabricated:** advertised in EHLO, answered `220 Ready to start TLS`, set
  `session.secure = true`, never upgraded the socket — a conforming sender's ClientHello was
  parsed as SMTP garbage, and `requireTls` was defeatable by issuing STARTTLS and continuing in
  plaintext.
- **(e) VERP bounces refused at the door:** the MTA sends `bounces+<id>@bounce.<customer-domain>`;
  the domain verifier did exact-match lookup and no `domains` row exists for `bounce.*` — every
  async DSN got `550 Relay not permitted`, making the entire DSN-suppression loop (the thing VERP
  was built for, #82a) unreachable dead code.
- **(f) DSN handling missing from HTTP ingest** (SMTP path had it; HTTP paths stored bounces as
  ordinary messages).
- **(g) Forward rules silently discarded mail** (logged "Forwarding" then `continue` — nothing
  stored, nothing forwarded).
- **(h) HTTP ingest unauthenticated by default:** `INBOUND_WEBHOOK_SECRET` unset ⇒ open POST
  endpoint on `0.0.0.0:8025`.
- **(i) `emails.source` never set** on received mail.

**Fixes (all in `services/inbound`, 47 new tests, 67/67 passing):** typed `SmtpError` with
`smtpReject()` → generic `550 5.7.1` / `smtpDefer()` → generic `451 4.7.1` (detail to logs, never
the wire; unknown errors 451 generic); defer stores nothing on all three ingress paths; store uses
`onConflictDoNothing` on the unique index + merges the second recipient's mailbox tag into the
existing row (DB resolves the race; #153's folder==tags[0] invariant preserved); STARTTLS
de-advertised, answers `502 5.5.1`, `session.secure` never set without a real upgrade, and
`requireTls` now honestly refuses all mail with `530 5.7.0` (real TLS needs a cert — issue #168);
domain verifier accepts `bounce.<X>` iff `X` exactly matches a hosted row — scoped to the literal
`bounce.` label only, NO generic subdomain inheritance, inheriting the parent's active/verified
state; bounce-domain recipients run DSN processing and are never stored to a mailbox (non-DSN to
a bounce domain is logged + dropped with 250 — never bounce a bounce); DSN detection added to both
HTTP paths in SMTP-path order; forward rules deliver locally with a loud not-implemented warning
instead of losing mail (localized to the original hosted address so a foreign forward target can't
mint a `domains` row); secret-unset HTTP ingest fails closed 503 naming the env var; `source:
"inbound"` written. `handleInboundMessage` extracted to a testable factory in the process.

## Issue #165 — FIXED 2026-08-07: send-path traps (MTA + API bundle)

**Findings:**
- **(a) Per-ISP daily counters never reset:** `optimizer.resetDailyCounters()`'s own comment says
  "call once per day"; zero callers — `messagesThisDay` only ever incremented, so a long-lived MTA
  process saturated every ISP's daily cap and silently throttled itself to zero. (The hourly
  sibling was wired; the daily one wasn't.)
- **(b) Warm-up reset to day 1 on every restart:** unset `MTA_WARMUP_STARTED_AT` fell back to
  `Date.now()` per process — every restart pinned the IP to week-1 caps forever.
- **(c) Signing-domain lookup unscoped:** `worker.ts` resolved `domains` by name only (no unique
  constraint on `domain`) — two accounts registering the same domain string could get the wrong
  account's DKIM key and suppression list.
- **(d) Agent-mail had zero delivery retries:** `messages.ts` enqueued with `attempts: 8` but
  `agent-send.ts` inherited the queue default of 1 — one greylist deferral permanently failed an
  agent-drafted message. (Worker throws on deferral; attempts are load-bearing.)
- **(e) Redis fallback asymmetry:** API queue/webhook/health honoured `UPSTASH_REDIS_URL` as a
  fallback while the MTA read `REDIS_URL` only — an operator setting only the former would split
  producer and consumer across two Redis instances, silently (the #149 class). Same pattern in the
  shared `redis.ts` client and the DNS liveness job.
- **(f) Daily DNS liveness job never registered:** `registerDnsLivenessJob` had zero callers while
  `messages.ts`'s stale-DNS gate comment assumed it ran — a customer deleting their SPF/DKIM
  records was never detected.
- **(g) MTA queue-depth/SMTP-listener health checks built but unregistered** — `/readyz` reported
  healthy with a backed-up queue. **(h)** no `start` script in the MTA package. **(i)**
  `emails.source` never set on first-party outbound. **(j)** env templates/docs instructed setting
  `DKIM_PRIVATE_KEY`/`DKIM_SELECTOR` env vars that NO code reads (keys live per-domain in the DB)
  — an operator following them got every message held with a correct-looking config; template also
  said `MTA_HOSTNAME=mx1` where production requires `smtp`. **(k)** stale `fly.toml` publishing
  four SMTP ports against a receiver that is default-off (would fail its own health checks).

**Fixes (30 new tests across MTA 329/329 + API 462/462):** `maybeResetDailyCounters` keyed on the
UTC calendar date, polled every 60s from the worker (a poll of the date, not a scheduled reset, so
cadence can't wipe counters mid-day); warm-up start date persisted via `SET NX` + `GET` in the
counters' Redis (env override wins; no-Redis mode logs process-local); `senderDomainWhere()` binds
domain AND the job's accountId; retry policy moved into `defaultJobOptions` so every producer
inherits attempts 8 + exponential/60s (drift-guard test pins messages.ts's explicit options to the
same constants); `addedAt` now sent by agent-send (shape truthful); `UPSTASH_REDIS_URL` fallback
removed from queue.ts, webhook-dispatcher.ts, redis.ts, and the DNS liveness job — `REDIS_URL` is
the single source, with a structural test forbidding reintroduction, and `routes/health.ts` now
resolves URL + queue name through `lib/queue.ts` so health can never report on a different Redis
than the producer; `registerDnsLivenessJob()` wired at server startup (gated on Redis configured,
`.catch` so boot can't crash) with `closeDnsLivenessQueue()` in shutdown; MTA health checks
registered (listener check conditional on `MTA_ENABLE_SMTP_RECEIVER` so the correct default
deployment isn't reported unhealthy); `start` script added; `source: "outbound"` on the send
insert; all five doc files corrected re DKIM env vars; fly.toml given a prominent
not-the-production-method warning.

**Known residuals logged as open issues #166–#170 in CLAUDE.md.**

## Issue #166 — FIXED 2026-08-08: fabricated-output batch (~16 sites, full record)

Found by the 2026-08-07 spine-audit stub sweep; fixed in one pass with 51 new tests
(apps/api 508/508, sentinel 47/47, apps/web typecheck/lint clean). Dispositions:

**Made real (the data existed):** search-intelligence `/trending` (aggregation over the account's
own search_history, 7-day windows with real trend vs the prior week) and `/suggestions/generate`
(frequency-derived from 30 days of real history, honest relevance = term share); ab-tests
completion now runs a genuine one-sided pooled two-proportion z-test (no deps) between winner and
runner-up — stores/returns null when untestable, never a constant, and `reply_rate` now actually
maps to replies; inbox `/follow-ups` feeds the detector the account's real sent mail (reply
resolution via In-Reply-To against sent Message-IDs, bracketed and bare).

**Honest 501s (nothing real behind them):** search `/related` + `/natural-language` (were
200-with-empty as if they ran); attachment `/extract-text` OCR (persisted placeholder prose then
locked the row via alreadyExtracted — poisoned rows now self-heal: placeholder-matching text reads
as null everywhere and clears on next touch; no migration); calendar `/schedule-from-text` (always
returned tomorrow-noon with confidence 0.75 regardless of input); ai-categorization `/retrain`
(fake queued job); onboarding `/sync-contacts` AND `/import-settings` (the second found during the
fix — fabricated import of canned label/filter/signature lists, both marked their step complete and
unretryable; steps stay retryable now).

**Honesty repairs:** workflows runs record `status: "skipped"` + actionsExecuted 0 + notice (were
persisting success rows for actions never executed — a fabricated audit trail); context-intelligence
stops attributing invented per-item confidences (0.85/0.95/0.80 → stored 0 per the #99 convention,
omitted from responses; commitments-page confidence chips removed); contact-enrichment relabelled
`derived_from_address` with confidence 0 (title-casing an email address is not enrichment);
calendar `/prep` drops the canned agenda + confidence, `/find-time` drops invented per-slot
confidence and the false availability claim; ai-writing degraded compose returns an EMPTY body
(no placeholder prose can sit in a sendable field); rule-engine returns `skippedActions` and logs
loudly per rule (users no longer believe a forward/auto-reply happened); sentinel excludes
no-data signals from the weighted spam score instead of feeding hardcoded 60/50/65 defaults in as
measurements (three signals, not the one the sweep found).

**Left noted (folded into #170):** calendar `/today`'s `aiAgenda` field name overclaims for a
template sentence; context-intelligence hardcodes `isExplicit: true` on every deadline.

## Issue #143 — FIXED 2026-08-08 (PR #96): the e2e suite executes in CI at last

New "E2E API" job in ci.yml: pgvector/pg16 + redis:7 service containers, migrations from source,
idempotent fixture seeder (account, SHA-256-hashed API key matching the suite's literal key,
Argon2id user + workspace membership; domain deliberately unseeded — the messages suite asserts
the 422 path), the API's workspace-dependency closure built (db/shared/crypto/email-parser export
only ./dist), API booted with bounded health wait, then all 7 files / 117 tests against the live
server. Boot log dumps + uploads on failure; no || true anywhere.

Two truths surfaced while building it: the old `test:e2e` script collected ZERO tests and exited 0
(the app vitest config's exclude beat the CLI path filter — the #150/#161 do-nothing-control
class; fixed with a dedicated e2e vitest config), and the never-run tests had drifted (bare
webhook event names that 422 since #70/#103; a templates suite that authenticated with an API key
which structurally cannot carry templates:* scopes — now logs in for a session JWT). Three CI
rounds to green, each catching a real environmental requirement: pgvector extension, then the
dist-only workspace packages. Not yet a required status check (branch-protection change is
Craig's call — worth doing once it proves stable for a week).
